function_declaration($name, $body) as $decl where {
  $name <: `runWithConcurrency`,
  $body <: contains `const results: T[] = new Array(tasks.length)`,
  $body <: contains `await Promise.all(workers)`,
  $decl => `__GRIT_INSERT_PLACEHOLDER__`
}
