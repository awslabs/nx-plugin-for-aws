{
  "$schema": "https://json-schema.org/schema",
  "$id": "InternalRollBackVersions",
  "title": "Internal version rollback generator",
  "description": "Rolls the versions a workspace pins below those this release vends, so the version sync migration has something to upgrade",
  "type": "object",
  "properties": {},
  "required": []
}
