{
  "$schema": "https://json-schema.org/schema",
  "$id": "InternalSyncVendedVersions",
  "title": "Internal version sync generator",
  "description": "Runs the version sync migration directly, so end to end tests can exercise it without a release",
  "type": "object",
  "properties": {},
  "required": []
}
