import "./handler.js";

using TypeSpec.Reflection;

model HandlerOptions {
  language: <%- handlerLanguages.map((l) => `"${l}"`).join(" | ") %>
}

extern dec handler(target: Operation, options: HandlerOptions);
