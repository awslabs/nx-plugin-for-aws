{
  "$schema": "https://json-schema.org/schema",
  "$id": "PyLambdaHandler",
  "title": "Create a Python Lambda Handler project",
  "description": "Create a Python Lambda Handler project",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "project name.",
      "x-priority": "important",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like your handler to have? i.e: MyHandler"
    },
    "directory": {
      "type": "string",
      "description": "The directory to store the application in.",
      "default": "packages",
      "x-prompt": "Which directory do you want to create the project in?",
      "x-priority": "important"
    }
  },
  "required": ["name"]
}
