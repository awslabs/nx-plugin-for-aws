import os

from aws_lambda_powertools import Logger, Metrics, Tracer

os.environ["POWERTOOLS_METRICS_NAMESPACE"] = "<%= lambdaHandlerClassName %>"
os.environ["POWERTOOLS_SERVICE_NAME"] = "<%= lambdaHandlerClassName %>"

logger: Logger = Logger()
metrics: Metrics = Metrics()
tracer: Tracer = Tracer()
