// Keep-alive for the <%- nameClassName %> serve targets. The gateway runs no
// local server of its own — this process holds the target open so Nx keeps
// the attached MCP servers' continuous serve targets running alongside it.
console.log('<%- nameKebabCase %>: holding attached MCP servers open');
setInterval(() => {}, 1 << 30);
