{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "LicenseCheck",
  "title": "License Check Executor",
  "description": "Scans workspace dependencies and fails when a license is not in the configured allowlist",
  "type": "object",
  "properties": {
    "collectors": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Names of collectors to use (default: ['npm', 'python']). Built-in collectors: npm, python."
    }
  }
}
