{
  "$schema": "https://json-schema.org/schema",
  "$id": "OpenApiOperationsMetadata",
  "title": "OpenAPI Operations Metadata",
  "description": "Generate an operations metadata JSON file from an OpenAPI specification",
  "type": "object",
  "properties": {
    "openApiSpecPath": {
      "type": "string",
      "description": "Path to the OpenAPI specification relative to the monorepo root",
      "x-priority": "important"
    },
    "outputPath": {
      "type": "string",
      "description": "Path to the directory in which to generate the metadata relative to the monorepo root",
      "x-priority": "important"
    }
  },
  "required": ["openApiSpecPath", "outputPath"]
}
