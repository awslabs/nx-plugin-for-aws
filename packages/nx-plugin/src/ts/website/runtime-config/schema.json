{
  "$schema": "https://json-schema.org/schema",
  "cli": "nx",
  "$id": "Website Runtime Config",
  "title": "Adds runtime config to a website",
  "description": "Adds runtime config to a website application",
  "type": "object",
  "properties": {
    "project": {
      "description": "The target project.",
      "type": "string",
      "alias": "dir",
      "x-priority": "important",
      "x-prompt": "the project to target",
      "x-dropdown": "projects"
    }
  },
  "required": ["project"]
}
