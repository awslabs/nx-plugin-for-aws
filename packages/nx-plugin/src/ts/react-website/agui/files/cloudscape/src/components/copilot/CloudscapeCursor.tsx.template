import type { FC } from 'react';
import LoadingBar from '@cloudscape-design/chat-components/loading-bar';

/**
 * Typing indicator rendered with the Cloudscape {@link LoadingBar} in its
 * dedicated generative-AI variant.
 */
export const CloudscapeCursor: FC = () => (
  <div aria-label="Generating response" role="status">
    <LoadingBar variant="gen-ai-masked" />
  </div>
);

export default CloudscapeCursor;
