/* Spacing between messages in the Cloudscape-themed chat view. */
.copilotkit-cloudscape-messages {
  display: flex;
  flex-direction: column;
  gap: var(--space-scaled-s-pzyw94, 12px);
  padding: var(--space-scaled-m-nllvnc, 16px);
}
