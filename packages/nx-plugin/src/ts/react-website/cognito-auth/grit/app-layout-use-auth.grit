or {
  `const AppLayout: $type = ($params) => { $body }` => raw`const AppLayout: $type = ($params) => {
  const { user, removeUser, signoutRedirect, clearStaleState } = useAuth();
  $body
}` where { $program <: not contains `useAuth()` },
  `const AppLayout = ($params) => { $body }` => raw`const AppLayout = ($params) => {
  const { user, removeUser, signoutRedirect, clearStaleState } = useAuth();
  $body
}` where { $program <: not contains `useAuth()` }
}