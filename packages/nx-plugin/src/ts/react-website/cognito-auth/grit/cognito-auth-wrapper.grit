or {
  `return ($jsx)` where {
    $jsx <: `<RuntimeConfigProvider>$children</RuntimeConfigProvider>` => `<RuntimeConfigProvider><CognitoAuth>$children</CognitoAuth></RuntimeConfigProvider>`,
    $program <: not contains `<CognitoAuth>$_</CognitoAuth>`
  },
  `return $jsx` where {
    $jsx <: `<RuntimeConfigProvider>$children</RuntimeConfigProvider>` => `<RuntimeConfigProvider><CognitoAuth>$children</CognitoAuth></RuntimeConfigProvider>`,
    $program <: not contains `<CognitoAuth>$_</CognitoAuth>`
  }
}