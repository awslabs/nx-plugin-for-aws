{
  "$schema": "https://json-schema.org/schema",
  "$id": "TsLambdaFunction",
  "title": "Add a TypeScript Lambda Function to a project",
  "description": "Add a TypeScript Lambda Function to a project",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The project to add the lambda function to",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "Select the project to add the lambda function to",
      "x-dropdown": "projects"
    },
    "functionName": {
      "type": "string",
      "description": "The name of the function to add",
      "x-priority": "important",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like your Lambda Function to have? i.e: MyFunction"
    },
    "functionPath": {
      "type": "string",
      "description": "The path within the project source directory to add the function to",
      "x-prompt": "Enter the path within the project source directory to add the function to (e.g. 'lambda-functions/')"
    },
    "handlerType": {
      "type": "string",
      "description": "The AWS Lambda handler type to use for the lambda function",
      "enum": [
        "APIGatewayProxyHandler",
        "APIGatewayProxyHandlerV2",
        "S3Handler",
        "SQSHandler",
        "SNSHandler",
        "DynamoDBStreamHandler",
        "KinesisStreamHandler",
        "CloudWatchLogsHandler",
        "ALBHandler",
        "LambdaFunctionURLHandler",
        "APIGatewayAuthorizerHandler",
        "S3BatchHandler",
        "SESHandler",
        "ScheduledHandler",
        "EventBridgeHandler",
        "Handler"
      ],
      "default": "APIGatewayProxyHandler",
      "x-prompt": "Select the AWS Lambda handler type for your function"
    }
  },
  "required": ["project", "functionName", "handlerType"]
}