module "py-mcp-server" {
  source = "../../../core/agent-core"
  agent_runtime_name = "<%= nameClassName %>"
  docker_image_tag = "<%= dockerImageTag %>"
  server_protocol = "<%= serverProtocol %>"
#  customJWTAuthorizer = {
#    discoveryUrl = "https://xxx/.well-known/openid-configuration",
#    allowedClients = [ "xxx" ]
#  }
}