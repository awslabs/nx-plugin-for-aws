// Our Game and Inventory entities only use the primary index, so there are no
// Global Secondary Indexes to synchronize locally or provision in infrastructure.
export const GLOBAL_SECONDARY_INDEXES: {
  indexName: string;
  partitionKey: string;
  sortKey: string;
}[] = [];
