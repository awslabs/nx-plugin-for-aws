---
/**
 * Renders the pinned Trivy image version.
 * Sourced from the plugin's versions.ts so docs stay in sync with generators.
 */
import { CONTAINER_VERSIONS } from '../../../packages/nx-plugin/src/utils/versions';
---

{CONTAINER_VERSIONS.trivy}
