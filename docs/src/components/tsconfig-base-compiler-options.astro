---
import { Code } from '@astrojs/starlight/components';
import { BASE_TSCONFIG_COMPILER_OPTIONS } from '../../../packages/nx-plugin/src/utils/base-tsconfig';

const code = JSON.stringify(
  { compilerOptions: BASE_TSCONFIG_COMPILER_OPTIONS },
  null,
  2,
);
---
<Code lang="json" title="tsconfig.base.json" code={code} />
