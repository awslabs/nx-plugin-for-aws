---
import { getRelativeLocaleUrl } from 'astro:i18n';
const { path } = Astro.props;
const resolvedPath = getRelativeLocaleUrl(Astro.currentLocale || 'en', path);
---
<a href={resolvedPath}><slot/><a/>
