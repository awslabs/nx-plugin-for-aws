---
import PackageManagerExecCommand from './package-manager-exec-command.astro';

const { commands, postCommands = [], highlights = [], env } = Astro.props;
const nxCommands = commands.map(c => `nx ${c}`);

---
<PackageManagerExecCommand highlights={highlights} commands={nxCommands} postCommands={postCommands} env={env} />
