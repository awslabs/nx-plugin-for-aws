---
import PackageManagerCommand from './package-manager-command.astro';
import { buildInstallDependenciesCommand } from '../../../packages/nx-plugin/src/utils/commands';

const { highlights = [] } = Astro.props;

const buildCommand = (pm) => buildInstallDependenciesCommand(pm);
---
<PackageManagerCommand highlights={highlights} buildCommand={buildCommand} />
