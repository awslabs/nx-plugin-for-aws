---
title: FastAPI vers DynamoDB
description: Connecter un FastAPI à un projet Python DynamoDB
when:
  sourceType: py#fast-api
  targetType: py#dynamodb
---
import Link from '@components/link.astro';
import RunGenerator from '@components/run-generator.astro';
import GeneratorParameters from '@components/generator-parameters.astro';
import Snippet from '@components/snippet.astro';

Le générateur `connection` relie un <Link path="guides/fastapi">FastAPI</Link> à un projet <Link path="guides/py-dynamodb">Python DynamoDB</Link>, en configurant le développement local pour que les deux démarrent ensemble automatiquement.

## Prérequis

Avant d'utiliser ce générateur, assurez-vous d'avoir :

1. Un projet FastAPI (généré avec `py#api --framework=fastapi`), voir le guide <Link path="guides/fastapi">`py#api`</Link>
2. Un projet <Link path="guides/py-dynamodb">`py#dynamodb`</Link>

## Utilisation

### Exécuter le générateur

<RunGenerator generator="connection" />

Sélectionnez votre projet FastAPI comme source et votre projet DynamoDB comme cible.

### Options

<GeneratorParameters generator="connection" />

## Sortie du générateur

Le générateur met à jour le `project.json` du FastAPI pour ajouter une dépendance de sa cible `dev` vers la cible `dev` du projet DynamoDB, et ajoute le package DynamoDB comme dépendance de workspace. Aucun fichier source n'est modifié.

## Utiliser DynamoDB dans les gestionnaires de routes

Importez les classes d'entités du package DynamoDB et utilisez-les dans vos gestionnaires de routes :

```python title="packages/my_api/my_api/api.py"
from my_scope_my_table.entities.example import ExampleModel

@app.get("/examples")
def list_examples():
    return list(ExampleModel.scan())
```

`my_scope_my_table` est le nom du module de votre projet DynamoDB — un module plat unique nommé `<scope>_<name>`, visible comme répertoire de package à l'intérieur du projet.

## Infrastructure

<Snippet name="connection/lambda-dynamodb-access" />

## Développement local

<Snippet name="connection/py-dynamodb-local-development" />
