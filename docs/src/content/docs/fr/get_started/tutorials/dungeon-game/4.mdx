---
title: Construire l'Interface Utilisateur
description: Un guide pas à pas pour construire un jeu d'aventure de donjon alimenté par une IA agentique en utilisant le @aws/nx-plugin.
---

import { Aside, Code, FileTree, Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';
import Link from '@components/link.astro';
import Drawer from '@components/drawer.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';
import PackageManagerShortCommand from '@components/package-manager-short-command.astro';
import InstallCommand from '@components/install-command.astro';
import E2ECode from '@components/e2e-code.astro';
import E2EDiff from '@components/e2e-diff.astro';

import dungeonAdventureArchitecturePng from '@assets/dungeon-game-architecture.png'
import baselineWebsitePng from '@assets/baseline-website.png'
import baselineGamePng from '@assets/baseline-game.png'
import nxGraphPng from '@assets/nx-graph.png'
import gameSelectPng from '@assets/game-select.png'
import gameConversationPng from '@assets/game-conversation.png'

## Tâche 1 : Configurer le serveur de développement local

Récupérez le fichier `runtime-config.json` déployé localement afin que le serveur de développement puisse communiquer avec le backend réel :

<NxCommands commands={["run game-ui:load:runtime-config"]} />

Ensuite, démarrez le serveur de développement :

<NxCommands commands={["serve game-ui"]} />

Ouvrez le site local dans un navigateur, connectez-vous et suivez les étapes pour créer un nouvel utilisateur. Vous devriez voir le site de base Shadcn :

<Image src={baselineWebsitePng} alt="baseline-website.png" width="800" height="600" />

<Aside type="caution" title="Garder le serveur de dev actif">
Laissez le serveur de développement en fonctionnement pour le reste de ce module — il recharge automatiquement chaque modification ci-dessous.
</Aside>

## Tâche 2 : Où CopilotKit est déjà configuré

Lorsque vous avez exécuté le générateur `connection` pour `game-ui → story` dans le <Link path="get_started/tutorials/dungeon-game/1">Module 1</Link>, l'intégration AG-UI du site Shadcn a été générée pour vous. Cela vaut la peine d'y jeter un coup d'œil rapide :

<FileTree>
- packages/game-ui/src/
  - components/
    - AguiProvider.tsx Single `CopilotKitProvider` registered with every AG-UI agent.
    - copilot/
      - index.tsx Re-exports themed `CopilotChat` / `CopilotSidebar` / `CopilotPopup`.
      - ShadcnAssistantMessage.tsx, ShadcnUserMessage.tsx, ShadcnChatInput.tsx, ShadcnCursor.tsx, copilot.css
  - hooks/
    - useAguiStoryAgent.tsx Instantiates an `@ag-ui/client` `HttpAgent` pointing at the deployed Story Agent and pads `threadId` to AgentCore's 33-character minimum session id.
  - main.tsx Wraps `<App />` in `<AguiProvider>`
</FileTree>

Tout ce que nous devons faire est d'insérer un `<CopilotChat agentId="agent" threadId={...} />` dans une route. Pour plus de détails sur la façon dont l'intégration est assemblée, consultez le <Link path="guides/connection/react-agui">guide de connexion React → AG-UI</Link>.

## Tâche 3 : Restyle pour le donjon

Remplacez `packages/game-ui/src/styles.css` — c'est le *seul* fichier que nous modifions pour le style. Il importe les globaux Shadcn partagés, remplace la palette par un thème de donjon éclairé aux torches, et fait en sorte que CopilotKit hérite de ces couleurs (CopilotKit réinitialise sinon `--background` et ses amis à l'intérieur de sa surface de chat) :

<E2ECode lang="css" path="dungeon-adventure/4/styles.css.template" />

## Tâche 4 : Créer les routes du jeu

Nous avons besoin de deux routes — une pour choisir un héros, une pour jouer. Les deux utilisent les composants shadcn et le chat CopilotKit ; il n'y a pas d'interface de chat codée à la main.

<Tabs>
<TabItem label="routes/index.tsx">
<E2EDiff before="dungeon-adventure/4/routes/index.tsx.old.template" after="dungeon-adventure/4/routes/index.tsx.template" lang="tsx" />

C'est le sélecteur de jeu : formulaire de nouveau jeu (shadcn `Input` + `Button` + `Card`) plus une liste "Continuer" alimentée par `useGameApi().games.query`. Démarrer un jeu `saveGame` la paire `(playerName, genre)` (pour qu'elle apparaisse la prochaine fois) et navigue vers la route de jeu.
</TabItem>
<TabItem label="routes/game/$playerName.tsx">
<E2ECode path="dungeon-adventure/4/routes/game/$playerName.tsx.template" lang="tsx" />

C'est la route de jeu. Elle construit le `threadId` déterministe (``{player}-{genre}`` complété à 33 caractères — le hook AG-UI l'enverra tel quel comme id de session AgentCore), rend `<CopilotChat agentId="agent" threadId={threadId} />`, et superpose l'inventaire de `useGameApi().inventory.query` par-dessus. Sur un thread frais (pas encore de messages stockés), la route utilise `useAgent` pour envoyer un message d'amorçage avec le nom du joueur et le genre ; l'invite système de l'agent lui dit d'utiliser cela pour planter le décor. Les visites ultérieures atteignent le `FileSessionManager` de l'agent, qui rejoue l'historique de sorte que le `useEffect` d'amorçage ne fait rien.

<Aside type="tip" title="Les threadIds déterministes rendent /game/$playerName?genre=... partageable">
Revisiter la même URL — que ce soit après un rechargement, un redéploiement ou sur un autre appareil — se résout au même `threadId`, donc l'agent continue de raconter la même histoire.
</Aside>

<Aside type="tip" title="Paramètres de chemin">
La syntaxe `$playerName` indique à `@tanstack/react-router` de traiter `playerName` comme un [paramètre de chemin](https://tanstack.com/router/v1/docs/framework/react/guide/path-params). `validateSearch` rend `genre` fortement typé par rapport à notre enum.
</Aside>
</TabItem>
</Tabs>

Vous pouvez supprimer le répertoire échafaudé `packages/game-ui/src/routes/welcome` — nous utilisons `/` comme sélecteur de jeu.

Une fois enregistré, le serveur de développement à `http://localhost:4200/` devrait maintenant vous permettre de démarrer une aventure et de discuter avec l'Agent Narratif.

<Drawer title="Construction et déploiement" trigger="Vous pouvez également construire & déployer votre code sur CloudFront si vous préférez.">

## Tâche 5 : Construire et déployer

### Construire votre code
<PackageManagerShortCommand commands={["build"]} />

### Déployer votre application
<NxCommands commands={['deploy infra dungeon-adventure-infra-sandbox/*']} />

Une fois déployé, accédez à votre URL CloudFront (`GameUIDistributionDomainName` dans les sorties CDK).

</Drawer>

<Image src={gameSelectPng} alt="game-select.png" width="500" height="400" />
<div style="margin-top: -100px; margin-left: 100px;">
<Image src={gameConversationPng} alt="game-conversation.png" width="500" height="400" />
</div>

Félicitations. Vous avez construit et déployé votre Jeu d'aventure en donjon agentique ! 🎉🎉🎉