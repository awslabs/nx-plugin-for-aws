---
title: Ajouter à un projet existant
description: Comment adopter @aws/nx-plugin dans un espace de travail Nx existant ou un monorepo non-Nx.
---
import { Steps, Aside } from '@astrojs/starlight/components';
import Link from '@components/link.astro';
import Snippet from '@components/snippet.astro';
import NxCommands from '@components/nx-commands.astro';
import NxInitCommand from '@components/nx-init-command.astro';
import GeneratorParameters from '@components/generator-parameters.astro';

Tous les projets ne commencent pas avec `pnpm create @aws/nx-workspace`. Si vous avez déjà un [espace de travail Nx](https://nx.dev) — ou un monorepo auquel vous pouvez ajouter Nx — vous pouvez adopter `@aws/nx-plugin` de manière incrémentale sans recréer votre projet.

<Aside type="caution" title="Avant de commencer">
Enregistrez et validez d'abord tout travail en cours, afin que les modifications apportées à chaque étape puissent être examinées — et annulées — à tout moment.
</Aside>

## Prérequis

<Snippet name="prerequisites" />

## Ajouter Nx à un projet non-Nx

Si votre projet n'utilise pas encore Nx, ajoutez-le d'abord avec [`nx init`](https://nx.dev/recipes/adopting-nx/adding-to-existing-project). C'est une étape que Nx lui-même gère ; le plugin se construit au-dessus d'un espace de travail Nx fonctionnel. Cela fonctionne sur un paquet simple, un espace de travail npm/pnpm/yarn/bun, un Turborepo ou un monorepo Lerna :

<NxInitCommand />

`nx init` est l'outil propre à Nx pour adopter Nx — il détecte vos outils, écrit `nx.json` et connecte vos scripts existants à Nx. Suivez les invites. Lors de l'exécution en mode non interactif (CI ou agent IA), passez `--interactive=false` ; si Nx détecte des plugins d'outils pour votre stack (par exemple `@nx/vite`), il demandera également un choix explicite — passez `--plugins=skip` pour reporter (les plugins peuvent être ajoutés plus tard avec `nx add`) ou `--plugins=all` pour les accepter. Une fois terminé et que votre espace de travail se construit sous Nx, [ajoutez le plugin](#adding-the-plugin) comme ci-dessous.

### Projets non-Node (Python, Go, Java, Rust, …)

Nx et le plugin sont distribués sous forme de paquets npm, donc un projet sans outillage Node.js a besoin d'un `package.json` racine minimal avant que `nx init` puisse faire quoi que ce soit d'utile :

```json
{
  "name": "my-project",
  "private": true,
  "type": "module"
}
```

Créez ce fichier, puis exécutez `nx init` et [ajoutez le plugin](#adding-the-plugin) comme d'habitude. Votre outillage de langage existant reste intact — les projets Nx générés par le plugin (y compris les <Link path="guides/py-project">projets Python</Link>) vivent aux côtés de votre code existant, et vous pouvez intégrer votre construction existante dans Nx de manière incrémentale.

### Projets à paquet unique

Le plugin est conçu pour les monorepos — il génère chaque projet dans son propre répertoire sous `packages/`. Si vous partez d'un **projet à paquet unique** (un `package.json` à la racine avec votre source directement en dessous, pas d'espaces de travail), déplacez votre paquet existant dans `packages/` avant d'adopter le plugin afin que votre projet se trouve aux côtés de ceux que le plugin génère :

<Steps>

1. Créez un répertoire `packages/<your-package>/` et déplacez-y votre source, `package.json` et `tsconfig.json`.
2. Créez un nouveau `package.json` racine qui agit comme manifeste d'espace de travail plutôt que comme un projet lui-même, par exemple `{ "name": "<your-workspace>", "private": true }`.
3. Déclarez l'espace de travail : pour `pnpm`, ajoutez un `pnpm-workspace.yaml` avec `packages: ['packages/*']` ; pour `npm`/`yarn`/`bun`, ajoutez `"workspaces": ["packages/*"]` au nouveau `package.json` racine.
4. Exécutez `nx init` (si vous ne l'avez pas fait), puis [ajoutez le plugin](#adding-the-plugin).

</Steps>

Garder la racine comme manifeste d'espace de travail (plutôt qu'un projet) est ce qui permet à `nx run-many --target build --all` de construire chaque projet sans qu'une tâche ne se réfère à elle-même — voir [la note sur la récursion](#recursive-task-invocation-detected) ci-dessous.

<Aside type="note" title="Gestionnaire de paquets et disposition">
Les projets du plugin sont générés sous un répertoire `packages/`. `pnpm` lit les globs d'espace de travail depuis `pnpm-workspace.yaml` ; les autres gestionnaires de paquets lisent le champ `workspaces` du `package.json` racine — `init` met à jour celui que votre espace de travail utilise. Si votre espace de travail utilise une disposition différente (comme `apps/` et `libs/`), les projets du plugin vivent sous `packages/` aux côtés de votre structure existante.
</Aside>

## Ajouter le plugin

Utilisez [`nx add`](https://nx.dev/nx-api/nx/documents/add), qui installe le plugin dans une version compatible avec votre installation Nx, puis exécute son générateur `init` pour configurer votre espace de travail :

<NxCommands commands={['add @aws/nx-plugin']} />

Une fois terminé, votre espace de travail est prêt — choisissez les générateurs dont vous avez besoin dans les <Link path="guides/index">guides</Link> et commencez à générer des projets.

### Ce que configure le générateur `init`

Le générateur `init` effectue les modifications déterministes dont un espace de travail a besoin pour exécuter les générateurs du plugin. Il est **idempotent** — l'exécuter à nouveau est une opération sans effet — et il préserve le format de module de votre espace de travail : un espace de travail dont le `package.json` racine a `type: "module"` reste en ESM, tout le reste reste en CommonJS, et le code généré suit le même modèle. Il ne **réécrit pas** un `tsconfig.base.json` existant, ne migre pas votre gestionnaire de paquets et ne restructure pas vos projets existants.

Il effectue les opérations suivantes :

- Crée `aws-nx-plugin.config.mts` en enregistrant votre <Link path="guides/typescript-infrastructure">fournisseur d'infrastructure as code</Link> choisi (CDK ou Terraform) et le moteur de conteneurs. Les générateurs lisent `iac.provider` depuis ce fichier.
- Enregistre les générateurs de synchronisation (`@nx/js:typescript-sync` et `@aws/nx-plugin:ts#sync`) sur la cible `compile` dans `nx.json`, afin que les références de projet TypeScript restent synchronisées lorsque vous ajoutez des projets.
- S'assure qu'un `tsconfig.json` racine et un `tsconfig.base.json` existent (la synchronisation TypeScript de Nx et les projets du plugin en ont besoin). Les fichiers manquants sont créés ; **les fichiers existants sont laissés intacts** afin de ne pas perturber vos autres projets. Un `tsconfig.base.json` créé contient les options du compilateur dont les projets TypeScript du plugin dépendent — si vous conservez le vôtre, voici les options à aligner :

  ```json
  {
    "compilerOptions": {
      "composite": true,
      "declarationMap": true,
      "emitDeclarationOnly": true,
      "importHelpers": true,
      "isolatedModules": true,
      "lib": ["es2022"],
      "module": "nodenext",
      "moduleResolution": "nodenext",
      "noEmitOnError": true,
      "noFallthroughCasesInSwitch": true,
      "noImplicitOverride": true,
      "noImplicitReturns": true,
      "noUnusedLocals": true,
      "skipLibCheck": true,
      "strict": true,
      "target": "es2022"
    }
  }
  ```

- Pour les espaces de travail `pnpm`, ajoute à la liste blanche les scripts de construction dont les dépendances du plugin ont besoin (`@swc/core`, `esbuild`, `nx`, `sharp`) dans `pnpm-workspace.yaml`.
- Ajoute des scripts de commodité (sans écraser les vôtres) au `package.json` racine, plus les dépendances de développement `nx`, `@nx/js`, `@nx/workspace`, `typescript` et Biome, et écrit un `biome.json` par défaut s'il n'en existe aucun.
- Configure le <Link path="get_started/building-with-ai">serveur MCP</Link> pour les agents de codage pris en charge (sauf si désactivé).

### Options

<GeneratorParameters generator="init" />

<Aside type="note" title="Mise à niveau ultérieure">
Pour mettre à niveau le plugin (et récupérer toutes les migrations de code automatisées qu'il fournit), utilisez [`nx migrate`](https://nx.dev/features/automate-updating-dependencies) plutôt que de modifier la version manuellement :

<NxCommands commands={['migrate @aws/nx-plugin']} />
</Aside>

## Adoption incrémentale

Vous n'avez pas besoin de convertir tout votre espace de travail d'un coup. Un bon chemin est :

<Steps>

1. Ajoutez le plugin, puis générez un seul projet (par exemple un <Link path="guides/typescript-project">projet TypeScript</Link> ou une <Link path="guides/ts-api">API</Link>) et construisez-le.
2. Ajoutez d'autres projets selon vos besoins — sites web, API, infrastructure — en utilisant les <Link path="guides/index">guides de générateurs</Link>.
3. Migrez votre code existant dans l'espace de travail au fil du temps, si vous le souhaitez. Les projets du plugin se trouvent aux côtés de vos projets existants ; ils ne vous obligent pas à déplacer quoi que ce soit.

</Steps>

## Dépannage

#### `The workspace is out of sync`

Les références de projet TypeScript nécessitent une synchronisation. `init` enregistre les générateurs de synchronisation ; exécutez :

<NxCommands commands={['sync']} />

#### `ERR_PNPM_IGNORED_BUILDS` pendant l'installation

pnpm ignore les scripts d'installation pour les paquets qui ne sont pas dans la liste blanche. `init` ajoute à la liste blanche ceux dont l'outillage du plugin a besoin (`@swc/core`, `esbuild`, `nx`, `sharp`) dans `pnpm-workspace.yaml`, mais le blocage peut se déclencher pendant `nx add @aws/nx-plugin` lui-même — avant que `init` ne soit exécuté. Exécutez `pnpm approve-builds` et approuvez les paquets listés (ou ajoutez-les sous `allowBuilds:`), puis relancez l'installation.

#### `nx sync` se bloque, ou `plugin worker ... exited before the connection was established`

Si deux versions différentes de `nx` sont présentes dans le même arbre node_modules (exécutez `npm ls nx` pour confirmer), leur IPC plugin-worker se bloque. Le générateur `init` épingle la devDependency `nx` racine à la version que les propres paquets `@nx/*` du plugin résolvent, mais une installation ultérieure d'un autre paquet `@nx/*` à une version de correctif différente peut réintroduire l'incompatibilité. Alignez chaque entrée `nx` et `@nx/*` dans votre `package.json` racine sur une seule version et réinstallez.

#### `Recursive task invocation detected`

Votre `package.json` **racine** est enregistré comme un projet Nx (il a une clé `"nx"`, que `nx init` ajoute pour un dépôt à paquet unique) *et* porte un script `build` de `nx run-many --target build`. Nx infère alors une cible `build` sur la racine qui s'appelle elle-même. Déplacez votre source dans `packages/<your-package>/` afin que la racine devienne un manifeste d'espace de travail plutôt qu'un projet — voir [Projets à paquet unique](#single-package-projects). (Les propres préréglages autonomes de Nx évitent cela en définissant `"nx": { "includedScripts": [] }` sur le paquet racine ; vous pouvez faire de même comme alternative plus rapide.)

#### Erreurs TypeScript dans vos projets **existants** après l'ajout du plugin (`TS6059`, `TS7016`, `TS5011`, `NG4006`)

Les propres projets générés par le plugin portent les paramètres TypeScript dont ils ont besoin dans leur `tsconfig.lib.json` par projet, donc ils se construisent indépendamment de votre `tsconfig.base.json`. Ces erreurs apparaissent plutôt dans un **projet préexistant à vous** qui hérite d'un `tsconfig.base.json` créé par `init` (avec `composite`/`emitDeclarationOnly`/`nodenext`) mais n'est pas compatible avec ces paramètres — par exemple, le compilateur Angular rejette `emitDeclarationOnly` (`NG4006`), ou un projet qui définit `outDir` sans `rootDir` en a maintenant besoin d'un (`TS5011`). Ajoutez des remplacements `tsconfig` par projet en redéclarant les options conflictuelles pour ces projets (par exemple `"rootDir": "src"`), ou pointez les projets du plugin et vos projets vers des configurations de base séparées.

`init` ne réécrit jamais un `tsconfig.base.json` existant, précisément pour qu'il ne puisse pas casser silencieusement les projets qui en héritent — résoudre ces incompatibilités est une décision que vous seul pouvez prendre pour votre base de code.

<Aside type="note" title="ESM et CommonJS">
Le format de module est à l'échelle de l'espace de travail, lu depuis le champ `type` du `package.json` racine : `type: "module"` signifie ESM, tout le reste (y compris l'absence de champ `type`) signifie CommonJS. `init` préserve celui que votre espace de travail utilise, et les générateurs TypeScript du plugin émettent du code correspondant. Si vous souhaitez changer plus tard, modifiez le `type` racine — mais c'est une migration de votre code existant, pas quelque chose que `init` fait pour vous.
</Aside>

<Aside type="tip">
Lorsque vous travaillez avec un assistant IA, le <Link path="get_started/building-with-ai">serveur MCP Nx Plugin for AWS</Link> expose un outil `add-to-existing-project` qui fournit ce guide directement à votre assistant.
</Aside>
