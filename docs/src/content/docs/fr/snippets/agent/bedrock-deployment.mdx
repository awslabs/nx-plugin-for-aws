---
title: "Déploiement d'Agent Bedrock"
---

import Link from '@components/link.astro';
import Infrastructure from '@components/infrastructure.astro';

### Infrastructure as Code

Si vous avez sélectionné `BedrockAgentCoreRuntime` pour `computeType`, l'infrastructure CDK ou Terraform correspondante est générée, que vous pouvez utiliser pour déployer votre Strands Agent sur [Amazon Bedrock AgentCore Runtime](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/agents-tools-runtime.html).

<Infrastructure>
<Fragment slot="cdk">
Un construct CDK est généré pour votre agent, nommé en fonction du `name` que vous avez choisi lors de l'exécution du générateur, ou `<ProjectName>Agent` par défaut.

Vous pouvez utiliser ce construct CDK dans une application CDK :

```ts
import { MyProjectAgent } from ':my-scope/common-constructs';

export class ExampleStack extends Stack {
  constructor(scope: Construct, id: string) {
    new MyProjectAgent(this, 'MyProjectAgent');
  }
}
```

:::note[Construct CDK Alpha]
Ce construct utilise le [module `@aws-cdk/aws-bedrock-agentcore-alpha`](https://docs.aws.amazon.com/cdk/api/v2/docs/aws-bedrock-agentcore-alpha-readme.html).
:::
</Fragment>
<Fragment slot="terraform">
Un module Terraform est généré pour vous, nommé en fonction du `name` que vous avez choisi lors de l'exécution du générateur, ou `<ProjectName>-agent` par défaut.

Vous pouvez utiliser ce module terraform dans un projet Terraform :

```terraform
# Agent
module "my_project_agent" {
  # Relative path to the generated module in the common/terraform project
  source = "../../common/terraform/src/app/agents/my-project-agent"
}
```
</Fragment>
</Infrastructure>

:::note[Permissions des modèles Bedrock]
Le construct d'agent généré accorde par défaut la permission d'invoquer tous les modèles de fondation et profils d'inférence Bedrock (`bedrock:InvokeModel` et `bedrock:InvokeModelWithResponseStream` sur `arn:aws:bedrock:*:*:foundation-model/*` et `arn:aws:bedrock:*:*:inference-profile/*`).

Vous pouvez souhaiter restreindre cela aux modèle(s) spécifiques utilisés par votre agent. Les agents Strands utilisent [Claude 4 Sonnet comme modèle par défaut](https://strandsagents.com/latest/documentation/docs/user-guide/concepts/model-providers/amazon-bedrock/#default-model) ; consultez le [guide des fournisseurs de modèles Strands](https://strandsagents.com/latest/documentation/docs/user-guide/concepts/model-providers/) pour configurer un modèle différent.

<Infrastructure>
<Fragment slot="cdk">
Pour restreindre, modifiez le construct CDK d'agent généré et remplacez les ressources `PolicyStatement` par défaut par les ARN des modèles de fondation ou profils d'inférence spécifiques que vous utilisez.
</Fragment>
<Fragment slot="terraform">
Pour restreindre, modifiez le module Terraform d'agent généré et remplacez les ARN `Resource` des modèles bedrock par défaut par les ARN des modèles de fondation ou profils d'inférence spécifiques que vous utilisez.
</Fragment>
</Infrastructure>
:::

:::caution[Docker requis]
[Docker](https://www.docker.com/) est requis pour construire et déployer votre Strands Agent. Assurez-vous d'avoir installé et lancé Docker pour éviter des erreurs telles que :

```
ERROR: Cannot connect to the Docker daemon at unix://path/to/docker.sock.
```
:::

### Authentification

Le générateur fournit une option `auth` pour configurer l'authentification de votre Strands Agent. Vous pouvez choisir entre l'authentification `IAM` (par défaut) ou `Cognito` lors de la génération de votre agent.

#### IAM

Par défaut, votre Strands Agent sera sécurisé à l'aide de l'authentification IAM, déployez-le simplement sans aucun argument :

<Infrastructure>
<Fragment slot="cdk">
```ts {5}
import { MyProjectAgent } from ':my-scope/common-constructs';

export class ExampleStack extends Stack {
  constructor(scope: Construct, id: string) {
    new MyProjectAgent(this, 'MyProjectAgent');
  }
}
```

Vous pouvez accorder l'accès pour invoquer votre agent sur Bedrock AgentCore Runtime en utilisant la méthode `grantInvokeAccess`, par exemple :

```ts {8}
import { MyProjectAgent } from ':my-scope/common-constructs';

export class ExampleStack extends Stack {
  constructor(scope: Construct, id: string) {
    const agent = new MyProjectAgent(this, 'MyProjectAgent');
    const lambdaFunction = new Function(this, ...);

    agent.grantInvokeAccess(lambdaFunction);
  }
}
```
</Fragment>
<Fragment slot="terraform">
```terraform
# Agent
module "my_project_agent" {
  # Relative path to the generated module in the common/terraform project
  source = "../../common/terraform/src/app/agents/my-project-agent"
}
```

Pour accorder l'accès pour invoquer votre agent, vous devrez ajouter une politique telle que la suivante, en référençant la sortie `module.my_project_agent.agent_core_runtime_arn` :

```terraform
{
  Effect = "Allow"
  Action = [
    "bedrock-agentcore:InvokeAgentRuntime"
  ]
  Resource = [
    module.my_project_agent.agent_core_runtime_arn,
    "${module.my_project_agent.agent_core_runtime_arn}/*"
  ]
}
```
</Fragment>
</Infrastructure>

#### Authentification Cognito

Lorsque vous sélectionnez l'authentification `Cognito`, le générateur configure l'agent pour utiliser Cognito pour l'authentification.

<Infrastructure>
<Fragment slot="cdk">
Le construct généré accepte une prop `identity` qui configure l'authentification Cognito :

```ts {8}
import { MyProjectAgent, UserIdentity } from ':my-scope/common-constructs';

export class ExampleStack extends Stack {
  constructor(scope: Construct, id: string) {
    const identity = new UserIdentity(this, 'Identity');

    new MyProjectAgent(this, 'MyProjectAgent', {
      identity,
    });
  }
}
```

Le construct `UserIdentity` peut être généré en utilisant le <Link path="/guides/react-website-auth">générateur `ts#react-website#auth`</Link>, ou vous pouvez créer votre propre `UserPool` et `UserPoolClient` CDK.
</Fragment>
<Fragment slot="terraform">
Le module généré accepte les variables `user_pool_id` et `user_pool_client_ids` pour l'authentification Cognito :

```terraform
module "user_identity" {
  source = "../../common/terraform/src/core/user-identity"
}

module "my_project_agent" {
  source = "../../common/terraform/src/app/agents/my-project-agent"

  user_pool_id         = module.user_identity.user_pool_id
  user_pool_client_ids = [module.user_identity.user_pool_client_id]
}
```
</Fragment>
</Infrastructure>

:::note[Fournisseurs OIDC personnalisés]
Si vous avez besoin d'une authentification JWT personnalisée avec un fournisseur OIDC non-Cognito, vous pouvez modifier directement le construct CDK ou le module Terraform généré pour votre agent. Notez que le générateur de connexion ne prendra en charge que l'authentification `IAM` ou `Cognito`.
:::