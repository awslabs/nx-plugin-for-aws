---
title: Configuration WAF Terraform
---

#### WAF

Pour les API REST, le module Terraform généré associe par défaut une Web ACL AWS WAFv2 à l'étape API Gateway. La Web ACL utilise l'ensemble de règles par défaut géré par AWS (`AWSManagedRulesCommonRuleSet` et `AWSManagedRulesKnownBadInputsRuleSet`), offrant une protection contre les exploits web courants, y compris le Top 10 OWASP.

Pour désactiver cette fonctionnalité (par exemple, pour attacher votre propre Web ACL), définissez `enable_waf` sur `false` :

```hcl {4}
module "my_api" {
  source = "../../common/terraform/src/app/apis/my-api"

  enable_waf = false
}
```
