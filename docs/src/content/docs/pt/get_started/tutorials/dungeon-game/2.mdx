---
title: "Implementar a API do Jogo e o servidor MCP de Inventário"
description: "Um guia passo a passo de como construir um jogo de aventura de dungeon com IA agêntica usando o @aws/nx-plugin."
---

import { Aside, Code, FileTree, Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';
import Link from '@components/link.astro';
import Drawer from '@components/drawer.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';
import PackageManagerShortCommand from '@components/package-manager-short-command.astro';
import InstallCommand from '@components/install-command.astro';
import E2EDiff from '@components/e2e-diff.astro';
import E2ECode from '@components/e2e-code.astro';
import Snippet from '@components/snippet.astro';

import dungeonAdventureArchitecturePng from '@assets/dungeon-game-architecture.png'
import baselineWebsitePng from '@assets/baseline-website.png'
import baselineGamePng from '@assets/baseline-game.png'
import nxGraphPng from '@assets/nx-graph.png'
import gameSelectPng from '@assets/game-select.png'
import gameConversationPng from '@assets/game-conversation.png'

import { getDungeonAdventureElectroDbDependencies } from '../../../../../../../../e2e/src/utils';

## Tarefa 1: Implementar a API do Jogo

Vamos implementar as seguintes APIs nesta seção:

1. `saveGame` - criar ou atualizar um jogo.
2. `queryGames` - retornar uma lista paginada de jogos salvos anteriormente.
3. `queryInventory` - retornar uma lista paginada de itens no inventário de um jogador.
4. `queryActions` - retornar o histórico de conversação para um determinado jogo.

:::note[Onde cada parte do estado reside]
- `games` e `inventory` estão no **DynamoDB** (via ElectroDB) — as lambdas da Game API e o servidor MCP de Inventário os leem/escrevem.
- `actions` (transcrição do chat) está no **S3** — o `S3SessionManager` do Story Agent escreve um blob JSON por turno, e `queryActions` na Game API os lê de volta para que a UI possa reproduzir o histórico ao recarregar a página. Não há procedimento `saveAction`; o agente é o único escritor.
:::

### Esquema da API

Para definir as entradas e saídas da nossa API, vamos criar nosso esquema usando [Zod](https://zod.dev/) no arquivo `packages/game-api/src/schema/index.ts` da seguinte forma:

<E2EDiff lang="typescript" before="dungeon-adventure/2/schema/index.ts.old.template" after="dungeon-adventure/2/schema/index.ts.template" />

Exclua o arquivo `packages/game-api/src/schema/echo.ts` pois não o usaremos neste projeto.

<Aside type="tip" title="Inferência automática de tipos">
Para cada esquema que definimos no Zod, também exportamos uma interface usando a sintaxe `z.TypeOf`. Isso converte nossa definição Zod em uma interface TypeScript sem duplicação de esforço.
</Aside>

### Modelagem de entidades

Este é o diagrama ER para nossa aplicação.

```d2
direction: right

game: Game {
  shape: sql_table
  playerName: "string  PK"
  genre: string
  lastUpdated: string
}

item: Item {
  shape: sql_table
  playerName: "string  PK"
  itemName: "string  SK"
  emoji: "string  (optional)"
  lastUpdated: string
  quantity: number
}

game -> item: 1..*
```

O gerador `ts#dynamodb` configurou o [ElectroDB](https://electrodb.dev/en/core-concepts/introduction/), que usaremos para modelar nossos dados. Vamos persistir o histórico de conversação no S3, então adicionamos uma dependência no cliente S3:

<InstallCommand pkg={getDungeonAdventureElectroDbDependencies()} project="@dungeon-adventure/game-api" />

<Aside title="Dependências do Projeto">
O cliente S3 é adicionado ao `package.json` do próprio projeto `game-api`, já que o código-fonte desse projeto o importa. As versões ainda são alinhadas em todo o workspace através do catálogo de dependências do gerenciador de pacotes (onde suportado) ([política de versão única](https://nx.dev/concepts/decisions/dependency-management#single-version-policy)). Para mais informações, consulte o <Link path="guides/typescript-project#dependencies">guia ts#project</Link>.
</Aside>

Substitua a entidade de exemplo gerada em `packages/dungeon-db/src/entities/index.ts` por nossas entidades `Game` e `Inventory`, e exclua `packages/dungeon-db/src/entities/example.ts`:

<E2ECode lang="typescript" path="dungeon-adventure/2/dungeon-db/entities/index.ts.template" />

O ElectroDB nos permite não apenas definir nossos tipos, mas também fornecer valores padrão para certos campos como timestamps. Além disso, o ElectroDB segue o [design de tabela única](https://electrodb.dev/en/core-concepts/single-table-relationships/), que é a melhor prática ao usar DynamoDB.

<Aside title="Coleções ElectroDB">
Embora o ElectroDB suporte [coleções](https://electrodb.dev/en/modeling/collections/), optamos por não usá-las neste tutorial para simplificar.
</Aside>

### Definindo nossos procedimentos

Para implementar os métodos da API, faça as seguintes alterações em `packages/game-api/src/procedures`:

<Tabs>
  <TabItem label="games.ts">
<E2ECode lang="typescript" path="dungeon-adventure/2/procedures/games.ts.template" />
  </TabItem>
  <TabItem label="inventory.ts">
<E2ECode lang="typescript" path="dungeon-adventure/2/procedures/inventory.ts.template" />
  </TabItem>
  <TabItem label="actions.ts">
<E2ECode lang="typescript" path="dungeon-adventure/2/procedures/actions.ts.template" />
  </TabItem>
</Tabs>

Exclua o arquivo `echo.ts` (de `packages/game-api/src/procedures`) pois não o usaremos neste projeto.

### Configuração do roteador

Depois de definir nossos procedimentos, para conectá-los à nossa API, atualize o seguinte arquivo:

<Tabs>
  <TabItem label="packages/game-api/src/router.ts">
<E2EDiff lang="typescript" before="dungeon-adventure/2/router.ts.old.template" after="dungeon-adventure/2/router.ts.template" />
</TabItem>
</Tabs>

## Tarefa 2: Criar um servidor MCP de Inventário

Vamos criar um servidor MCP que permitirá ao nosso agente gerenciar itens no inventário de um jogador.

Definiremos as seguintes ferramentas para nosso agente:

- `list-inventory-items` para recuperar os itens atuais do inventário do jogador
- `add-to-inventory` para adicionar itens ao inventário do jogador
- `remove-from-inventory` para remover itens do inventário do jogador

Para economizar tempo, vamos definir todas as ferramentas inline:

<Tabs>
  <TabItem label="packages/inventory/src/mcp-server/server.ts">
<E2EDiff lang="typescript" before="dungeon-adventure/2/mcp/server.ts.old.template" after="dungeon-adventure/2/mcp/server.ts.template" />
</TabItem>
</Tabs>

Conforme o número de ferramentas crescer, você pode refatorá-las em arquivos separados se preferir.

Exclua os diretórios `tools` e `resources` em `packages/inventory/src/mcp-server` pois não serão utilizados.

## Tarefa 3: Atualizar a infraestrutura

O construto `DungeonDb` gerado pelo `ts#dynamodb` já provisiona nossa tabela, então só precisamos instanciá-lo em nossa stack e conceder à Game API e ao servidor MCP de Inventário as permissões necessárias. Atualize `packages/infra/src/stacks/application-stack.ts` conforme segue:

<E2EDiff lang="typescript" before="dungeon-adventure/1/application-stack.ts.template" after="dungeon-adventure/2/stacks/application-stack.ts.template" />

:::note[Procedimentos isolados]
Observe que como cada procedimento é atendido por uma função lambda individual, podemos seguir o princípio de menor privilégio e atribuir apenas as permissões de leitura/escrita necessárias com base na implementação do procedimento.
:::

:::tip[Configuração de Runtime]
O construto `DungeonDb` registra o nome da tabela implantada na <Link path="guides/runtime-config">Configuração de Runtime</Link> sob o namespace `dynamodb`, armazenado no AWS AppConfig. A Game API e o servidor MCP de Inventário o recuperam em tempo de execução usando Lambda Powertools, em vez de precisar ser passado como uma variável de ambiente. Um benefício dessa abordagem é que, à medida que o número de tabelas ou outros identificadores de infraestrutura cresce, você precisa apenas de uma única variável de ambiente `RUNTIME_CONFIG_APP_ID` ao executar contra a infraestrutura implantada.
:::

## Tarefa 4: Testar a Game API localmente

Não há necessidade de implantar na AWS para testar nossa API — o target `serve-local` executa a Game API contra o [DynamoDB Local](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.html). Como conectamos a Game API ao projeto `DungeonDb` no Módulo 1, este target também inicia o DynamoDB Local automaticamente.

Primeiro, corrija quaisquer problemas de lint:

<PackageManagerShortCommand commands={["lint"]} />

Depois construa a base de código:

<PackageManagerShortCommand commands={["build"]} />

### Iniciar o servidor local

Inicie a Game API localmente com o target `dev`, que também inicializa o DynamoDB Local:

<NxCommands commands={["dev game-api"]} />

<Aside type="tip" title="Primeira execução">
A primeira execução baixa a imagem de contêiner do DynamoDB Local, o que pode levar um momento. Deixe o servidor em execução neste terminal e abra um segundo terminal para os comandos `curl` abaixo.
</Aside>

### Testar a API

Assim que seu servidor estiver em execução, consulte a lista (vazia) de jogos:

```bash
curl -X GET 'http://localhost:2022/games.query?input=%7B%7D'
```

:::note[Codificação de URL]
O `%7B%7D` que passamos para testar a API é um objeto JSON vazio codificado em URI (`{}`).
:::

Você verá uma lista vazia:

```json
{"result":{"data":{"items":[],"cursor":null}}}
```

Agora salve um jogo:

```bash
curl -X POST 'http://localhost:2022/games.save' \
  -H 'Content-Type: application/json' \
  -d '{"playerName":"Alice","genre":"zombie"}'
```

O save retorna o jogo persistido (com o timestamp `lastUpdated` que a entidade define para você):

```json
{"result":{"data":{"playerName":"Alice","genre":"zombie","lastUpdated":"..."}}}
```

Consulte novamente para confirmar que está persistido no DynamoDB Local:

```bash
curl -X GET 'http://localhost:2022/games.query?input=%7B%7D'
```

Esta resposta agora inclui o jogo salvo:

```json
{"result":{"data":{"items":[{"playerName":"Alice","genre":"zombie","lastUpdated":"..."}],"cursor":null}}}
```

Você pode parar o servidor local (`Ctrl+C`) quando terminar.

## Tarefa 5: Testar o servidor MCP de Inventário localmente

Podemos testar as ferramentas do servidor MCP com o [MCP Inspector](https://github.com/modelcontextprotocol/inspector) usando o target `mcp-server-inspect` gerado:

<NxCommands commands={["mcp-server-inspect inventory"]} />

Isso serve o servidor MCP localmente (inicializando o DynamoDB Local também) e lança o MCP Inspector em `http://localhost:6274` pré-configurado para se conectar a ele. Clique em **Connect**, mude para a aba **Tools**, clique em **List Tools**, e experimente `add-to-inventory` (por exemplo, `playerName: Alice`, `itemName: Rusty Sword`, `emoji: ⚔️`) seguido de `list-inventory-items` para vê-lo persistido no DynamoDB Local. Pare o servidor (`Ctrl+C`) quando terminar.

Parabéns, você construiu e testou sua primeira API tRPC e servidor MCP contra uma tabela DynamoDB local!  🎉🎉🎉
