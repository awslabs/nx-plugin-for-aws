---
title: "Guia de Início Rápido"
description: "Um início rápido sobre como usar @aws/nx-plugin."
---



import { Steps } from '@astrojs/starlight/components';
import Link from '@components/link.astro';
import Snippet from '@components/snippet.astro';
import CreateNxWorkspaceCommand from '@components/create-nx-workspace-command.astro';
import InstallCommand from '@components/install-command.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';

Este guia orienta você através dos fundamentos da instalação e uso do `@aws/nx-plugin` para construir projetos rapidamente na AWS.

:::tip
Para um tutorial detalhado sobre a construção de uma aplicação full-stack, consulte o <Link path="get_started/tutorials/dungeon-game/overview">Tutorial Dungeon Adventure</Link>.
:::

## Pré-requisitos

Antes de prosseguir, instale estas dependências globais:

<Snippet name="prerequisites" />

## Passo 1: Inicializar um novo Workspace Nx

Para criar um workspace Nx com o gerenciador de pacotes de sua escolha, execute o comando:

<CreateNxWorkspaceCommand workspace="my-project" />

Navegue até o diretório do projeto:

```sh
cd my-project
```

## Passo 2: Adicionar o Plugin Nx para AWS

Instale o plugin usando seu gerenciador de pacotes.

<InstallCommand dev pkg="@aws/nx-plugin" />

## Passo 3: Usar Geradores para Estruturar seu Projeto

Para fins deste guia rápido, adicionaremos uma API tRPC, um site React, autenticação Cognito e infraestrutura CDK ao projeto.
Dependendo do tipo de projeto que você está construindo, você pode escolher qualquer combinação de geradores para inicializar seu projeto. Para ver a lista completa de opções, consulte a seção __Guias__ na barra de navegação à esquerda.

### Adicionar uma API tRPC

<RunGenerator generator="ts#trpc-api" requiredParameters={{ apiName: 'demo-api' }} />

Isso criará a API dentro da pasta `packages/demo-api`.

### Adicionar um Site React

<RunGenerator generator="ts#cloudscape-website" requiredParameters={{ name: 'demo-website' }} />

Isso estrutura um novo site React em `packages/demo-website`.

### Adicionar Autenticação Cognito

<RunGenerator generator="ts#cloudscape-website#auth" requiredParameters={{ project: '@my-project/demo-website', cognitoDomain: 'my-demo' }} />

Isso configura a infraestrutura necessária e o código React para adicionar autenticação Cognito ao seu site.

### Conectar Frontend ao Backend

<RunGenerator generator="api-connection" requiredParameters={{ sourceProject: '@my-project/demo-website', targetProject: '@my-project/demo-api-backend' }} />

Isso configura os provedores necessários para garantir que seu site possa chamar sua API tRPC.

### Adicionar Infraestrutura CDK

<RunGenerator generator="ts#infra" requiredParameters={{ name: 'infra' }} />

Isso configura uma Aplicação CDK que você pode usar para implantar sua infraestrutura na AWS.

## Passo 4: Definir Recursos Cloud e Implantar na AWS

Abra `packages/infra/src/stacks/application-stack.ts` e adicione o seguinte código:

```typescript
import * as cdk from 'aws-cdk-lib';
import { DemoApi, DemoWebsite, UserIdentity } from ':my-project/common-constructs';
import { Construct } from 'constructs';

export class ApplicationStack extends cdk.Stack {
  constructor(scope: Construct, id: string, props?: cdk.StackProps) {
    super(scope, id, props);

    const identity = new UserIdentity(this, 'identity');
    const api = new DemoApi(this, 'api');
    api.grantInvokeAccess(identity.identityPool.authenticatedRole);

    new DemoWebsite(this, 'website');
  }
}
```
__Observação__: Este é todo o código CDK necessário para implantar nossa aplicação full-stack.

### Compilar e implantar a infraestrutura

Para compilar seu projeto, execute o comando:

<NxCommands commands={['run-many --target build --all']} />

:::tip
Se encontrar erros de lint, execute o seguinte comando para corrigi-los automaticamente.

<NxCommands commands={['run-many --target lint --configuration=fix --all']} />
:::

## Passo 5: Executar o Frontend Localmente

<Steps>
1. Obter o arquivo `runtime-config.json`

    <NxCommands commands={['run @demo/demo-website:load:runtime-config']} />

2. Iniciar o servidor web local

    <NxCommands commands={['run @demo/demo-website:serve']} />
</Steps>

Você pode visualizar seu site em `http://localhost:4200` no navegador.

---

Parabéns! 🎉 Você implantou com sucesso uma aplicação full-stack usando `@aws/nx-plugin`!