---
title: Aviso sobre Serve Local no Windows
---

Parar o `serve-local` (por exemplo, com `Ctrl+C`) remove automaticamente o contêiner DynamoDB Local, mas preserva o volume nomeado para que seus dados persistam entre reinicializações.

:::caution[Windows]
Devido a limitações no tratamento de sinais no Windows, o contêiner não é removido automaticamente quando o `serve-local` é interrompido. Você precisará removê-lo manualmente:

```bash
<engine> rm -f <scope>-dynamodb
```

Substitua `<engine>` pelo seu motor de contêiner (`docker` ou `finch`) e `<scope>` pelo escopo do seu workspace Nx (por exemplo, `proj`).
:::
