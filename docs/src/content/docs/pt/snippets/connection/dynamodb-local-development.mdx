---
title: Desenvolvimento Local com DynamoDB
---

O gerador `connection` configura o target `serve-local` do seu projeto para depender do target `serve-local` do projeto DynamoDB. O DynamoDB Local será iniciado automaticamente junto com o seu projeto ao executar `serve-local`.

A variável de ambiente `SERVE_LOCAL=true` é definida automaticamente, então `getDynamoDBClient()` e `resolveTableName()` conectam-se à instância local do DynamoDB Local em vez da AWS.
