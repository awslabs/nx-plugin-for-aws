---
title: Desenvolvimento Local com Python DynamoDB
---

O gerador `connection` configura o target `serve-local` do seu projeto para depender do target `serve-local` do projeto DynamoDB. O DynamoDB Local será iniciado automaticamente junto com seu projeto ao executar `serve-local`.

A variável de ambiente `SERVE_LOCAL=true` é definida automaticamente, então `is_local()` retorna `True` e suas entidades PynamoDB se conectam à instância local do DynamoDB em vez da AWS.
