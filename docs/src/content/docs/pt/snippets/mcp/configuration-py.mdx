---
title: "Configuração do Servidor MCP"
---



### Arquivos de Configuração

A maioria dos assistentes de IA que suportam MCP usa uma abordagem de configuração semelhante. Você precisará criar ou atualizar um arquivo de configuração com os detalhes do seu servidor MCP:

```json
{
  "mcpServers": {
    "your-mcp-server": {
      "command": "uv",
      "args": [
        "run",
        "python",
        "-m",
        "my_module.mcp_server.stdio"
      ],
      "env": {
        "VIRTUAL_ENV": "/path/to/your/project/.venv"
      }
    }
  }
}
```

:::caution
Se você receber um erro como `ENOENT uv` ao conectar ao servidor, talvez precise especificar o caminho completo para o `uv`, que pode ser obtido executando `which uv` no terminal.
:::

### Configuração Específica do Assistente

Consulte a documentação a seguir para configurar o MCP com assistentes de IA específicos:

import Snippet from '@components/snippet.astro';

<Snippet name="mcp/assistant-docs" />

:::tip
Alguns assistentes de IA, como o Amazon Q Developer, permitem especificar uma configuração de servidor MCP no nível do workspace, o que é particularmente útil para definir os servidores MCP relevantes para um projeto específico.
:::