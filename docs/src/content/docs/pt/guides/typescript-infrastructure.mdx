---
title: "Infraestrutura CDK"
description: "Documentação de referência para Infraestrutura CDK"
---



import { FileTree } from '@astrojs/starlight/components';
import RunGenerator from '@components/run-generator.astro';
import Link from '@components/link.astro';
import GeneratorParameters from '@components/generator-parameters.astro';
import NxCommands from '@components/nx-commands.astro';

O [AWS CDK](https://docs.aws.amazon.com/cdk/v2/guide/home.html) é um framework para definir infraestrutura em nuvem via código e provisioná-la através do AWS CloudFormation.

O gerador de infraestrutura TypeScript cria uma aplicação AWS CDK de infraestrutura escrita em TypeScript. A aplicação gerada inclui práticas recomendadas de segurança através de verificações do [Checkov](https://www.checkov.io/).

## Uso

### Gerar um projeto de infraestrutura

Você pode gerar um novo projeto de infraestrutura de duas formas:

<RunGenerator generator="ts#infra" />

### Opções

<GeneratorParameters generator="ts#infra" />

## Saída do Gerador

O gerador criará a seguinte estrutura de projeto no diretório `<directory>/<name>`:

<FileTree>

  - src
    - main.ts Ponto de entrada da aplicação que instancia estágios CDK para implantação
    - stages Definições de estágios CDK
      - application-stage.ts Define uma coleção de stacks para implantar em um estágio
    - stacks Definições de stacks CDK
      - application-stack.ts Stack principal da aplicação
  - cdk.json Configuração do CDK
  - project.json Configuração do projeto e targets de build
  - .checkov.yml Arquivo de configuração do Checkov

</FileTree>

:::dica
Sua infraestrutura é um projeto TypeScript, portanto você pode consultar a <Link path="guides/typescript-project">documentação de projetos TypeScript</Link> para detalhes sobre o uso geral.
:::

## Implementando sua Infraestrutura CDK

Você pode começar a escrever sua infraestrutura CDK no arquivo `src/stacks/application-stack.ts`, por exemplo:

```ts title="src/stacks/application-stack.ts" {9-10}
import { Stack, StackProps } from 'aws-cdk-lib';
import { Bucket } from 'aws-cdk-lib/aws-s3'
import { Construct } from 'constructs';

export class ApplicationStack extends Stack {
  constructor(scope: Construct, id: string, props?: StackProps) {
    super(scope, id, props);

    // Declare sua infraestrutura aqui
    new Bucket(this, 'MyBucket');
  }
}
```

### Estágios e Stacks

Você notará que o arquivo principal `src/main.ts` instancia um [Stage](https://docs.aws.amazon.com/cdk/v2/guide/stages.html) CDK chamado `<namespace>-sandbox`. Este estágio é destinado ao seu próprio desenvolvimento e testes.

```ts title="src/main.ts"
new ApplicationStage(app, 'project-sandbox', {
  env: {
    account: process.env.CDK_DEFAULT_ACCOUNT,
    region: process.env.CDK_DEFAULT_REGION,
  },
});
```

Você pode adicionar mais estágios, por exemplo, pode definir estágios `beta` e `prod` que implantam em ambientes separados:

```ts title="src/main.ts"
new ApplicationStage(app, 'project-beta', {
  env: {
    account: '123456789012', // conta beta
    region: 'us-west-2',
  },
});
new ApplicationStage(app, 'project-prod', {
  env: {
    account: '098765432109', // conta prod
    region: 'us-west-2',
  },
});
```

Um Stage define uma coleção de stacks que devem ser implantadas juntas para compor sua aplicação. Você pode instanciar quantas stacks quiser dentro de um estágio, por exemplo:

```ts title="src/stages/application-stage.ts"
import { Stage, StageProps } from 'aws-cdk-lib';
import { Construct } from 'constructs';
import { BackendStack } from '../stacks/backend-stack.js';
import { FrontendStack } from '../stacks/frontend-stack.js';

/**
 * Define uma coleção de CDK Stacks que compõem sua aplicação
 */
export class ApplicationStage extends Stage {
  constructor(scope: Construct, id: string, props?: StageProps) {
    super(scope, id, props);

    new BackendStack(this, 'Backend', {
      crossRegionReferences: true,
    })

    new FrontendStack(this, 'Frontend', {
      crossRegionReferences: true,
    });
  }
}
```

### Infraestrutura de API

Se você usou os geradores <Link path="guides/trpc">tRPC API</Link> ou <Link path="guides/fastapi">FastAPI</Link> para criar APIs, notará que já tem alguns constructs disponíveis em `packages/common/constructs` para implantá-los.

Se, por exemplo, você criou uma API tRPC chamada `my-api`, pode simplesmente importar e instanciar o construct para adicionar toda a infraestrutura necessária:

```ts title="src/stacks/application-stack.ts" {3, 9-12}
import * as cdk from 'aws-cdk-lib';
import { Construct } from 'constructs';
import { MyApi } from ':my-scope/common-constructs';

export class ApplicationStack extends cdk.Stack {
  constructor(scope: Construct, id: string, props?: cdk.StackProps) {
    super(scope, id, props);

    // Adicione infraestrutura para sua API
    new MyApi(this, 'MyApi', {
      integrations: MyApi.defaultIntegrations(this).build(),
    });
  }
}
```

### Infraestrutura de Website

Se você usou o gerador <Link path="guides/react-website">CloudScape website</Link>, notará que já tem um construct em `packages/common/constructs` para implantá-lo. Por exemplo:

```ts title="src/stacks/application-stack.ts" {3, 9-10}
import * as cdk from 'aws-cdk-lib';
import { Construct } from 'constructs';
import { MyWebsite } from ':my-scope/common-constructs';

export class ApplicationStack extends cdk.Stack {
  constructor(scope: Construct, id: string, props?: cdk.StackProps) {
    super(scope, id, props);

    // Adicione infraestrutura para seu website
    new MyWebsite(this, 'MyWebsite');
  }
}
```

:::cuidado
É importante garantir que o website seja declarado _após_ quaisquer constructs de API para que a <Link path="guides/react-website#runtime-configuration">Configuração de Runtime</Link> do website inclua todas as configurações de API.
:::

## Sintetizando sua Infraestrutura

Como parte do target `build`, além de executar os <Link path="guides/typescript-project#building">targets padrão de compilação, lint e testes</Link>, seu projeto de infraestrutura é _sintetizado_ para CloudFormation. Isso também pode ser executado separadamente através do target `synth`:

<NxCommands commands={['run <my-infra>:synth']} />

Você encontrará sua cloud assembly sintetizada na pasta raiz `dist`, em `dist/packages/<my-infra-project>/cdk.out`.

## Testes de Segurança

Um target `checkov` é adicionado ao seu projeto para executar verificações de segurança usando o [Checkov](https://www.checkov.io/).

<NxCommands commands={['run <my-infra>:checkov']} />

Você encontrará os resultados dos testes de segurança na pasta raiz `dist`, em `dist/packages/<my-infra-project>/checkov`.

:::note
O Checkov é executado usando `uvx`, portanto você precisa [instalar o `uv` caso ainda não tenha](https://docs.astral.sh/uv/getting-started/installation/).
:::

:::caution
Algumas regras do Checkov são suprimidas por padrão via arquivo `.checkov.yml`. Recomenda-se revisá-las com base no seu caso de uso.
:::

### Suprimindo Verificações do Checkov

Pode haver casos onde você queira suprimir certas regras em recursos. Isso pode ser feito de duas formas:

#### Suprimir uma regra em um construct específico

```typescript
import { suppressRules } from ':my-scope/common-constructs';

// suprime a CKV_AWS_XXX para o construct especificado
suppressRules(construct, ['CKV_AWS_XXX'], 'Motivo');
```

#### Suprimir uma regra em um construct descendente

```typescript
import { suppressRules } from ':my-scope/common-constructs';

// Suprime a CKV_AWS_XXX para o construct ou qualquer descendente se for uma instância de Bucket
suppressRules(construct, ['CKV_AWS_XXX'], 'Motivo', (construct) => construct instanceof Bucket);
```

## Bootstrapping da sua Conta AWS

Se você está implantando uma aplicação CDK em uma conta AWS pela primeira vez, é necessário fazer o bootstrap primeiro.

Primeiro, certifique-se de ter [configurado credenciais para sua conta AWS](https://docs.aws.amazon.com/sdkref/latest/guide/access.html).

Em seguida, use o comando `cdk bootstrap`:

```bash
npx cdk bootstrap aws://<account-id>/<region>
```

Para mais detalhes, consulte a [documentação do CDK](https://docs.aws.amazon.com/cdk/v2/guide/bootstrapping-env.html).

## Implantação na AWS

Após um build, você pode implantar sua infraestrutura na AWS usando o target `deploy`.

:::cuidado
Use o target `deploy-ci` para implantações em pipelines CI/CD. Veja abaixo para detalhes.
:::

Primeiro, certifique-se de ter [configurado credenciais para sua conta AWS](https://docs.aws.amazon.com/sdkref/latest/guide/access.html).

Em seguida, execute o target deploy:

<NxCommands commands={['run <my-infra>:deploy <my-infra>-sandbox/*']} />

:::dica
O comando acima implanta _todas_ as stacks do estágio `<my-infra>-sandbox`. Você pode especificar outros estágios desde que estejam definidos em `main.ts`.

Também é possível implantar stacks individuais especificando o nome completo da stack, por exemplo:

<NxCommands commands={['run <my-infra>:deploy <my-infra>-sandbox/Application']} />
:::

## Implantação na AWS em Pipeline CI/CD

Use o target `deploy-ci` para implantações na AWS como parte de um pipeline CI/CD.

<NxCommands commands={['run <my-infra>:deploy-ci my-stage/*']} />

Este target difere ligeiramente do `deploy` normal por garantir que a cloud-assembly pré-sintetizada seja implantada, em vez de sintetizar durante a execução. Isso ajuda a evitar problemas de não-determinismo devido a alterações em versões de pacotes, garantindo que todos os estágios do pipeline usem a mesma cloud-assembly.

## Remoção de Infraestrutura AWS

Use o target `destroy` para remover seus recursos:

<NxCommands commands={['run <my-infra>:destroy <my-infra>-sandbox/*']} />

## Mais Informações

Para mais informações sobre CDK, consulte o [Guia do Desenvolvedor CDK](https://docs.aws.amazon.com/cdk/v2/guide/core_concepts.html) e [Referência da API](https://docs.aws.amazon.com/cdk/api/v2/docs/aws-construct-library.html).