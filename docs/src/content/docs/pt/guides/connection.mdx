---
title: "Conexão"
description: "Documentação de referência para conexões"
---

import Astro from '@astrojs/react';
import { LinkCard, CardGrid, LinkButton } from '@astrojs/starlight/components';
import Link from '@components/link.astro';

Este gerador é usado para conectar projetos entre si, como sites chamando APIs. Basta selecionar o projeto de origem (por exemplo, o projeto que irá chamar sua API) e o projeto de destino (por exemplo, seu projeto de API), e este gerador cuidará da integração entre os dois.

### Conexões Suportadas

O gerador de Conexão suporta as seguintes conexões:

<CardGrid>
  <LinkCard
    title="React para tRPC"
    description="Chame uma API tRPC de um site React"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/connection/react-trpc`}
  />
  <LinkCard
    title="React para FastAPI"
    description="Chame uma API FastAPI Python de um site React"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/connection/react-fastapi`}
  />
  <LinkCard
    title="React para API Smithy"
    description="Chame uma API Smithy de um site React"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/connection/react-smithy`}
  />
  <LinkCard
    title="TypeScript Strands Agent para MCP"
    description="Conecte um TypeScript Strands Agent a um servidor MCP"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/connection/ts-strands-agent-mcp`}
  />
</CardGrid>

:::note[Configuração de Runtime]
O gerador de conexão utiliza <Link path="guides/runtime-config">Configuração de Runtime</Link> para passar valores de tempo de implantação (como URLs de API, configurações do Cognito e ARNs de runtime de agentes) entre projetos e componentes gerados em tempo de execução para que possam se descobrir e conectar uns aos outros.
:::