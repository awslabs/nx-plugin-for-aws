---
title: "AI 던전 게임"
description: "@aws/nx-plugin을 사용하여 AI 기반 던전 모험 게임을 구축하는 방법에 대한 안내"
---



import { Aside, Code, FileTree, Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';
import Link from '@components/link.astro';
import Drawer from '@components/drawer.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';
import InstallCommand from '@components/install-command.astro';
import CreateNxWorkspaceCommand from '@components/create-nx-workspace-command.astro';
import dungeonAdventureArchitecturePng from '@assets/dungeon-game-architecture.png'
import dungeonAdventureErPng from '@assets/dungeon-adventure-er.png'
import baselineWebsitePng from '@assets/baseline-website.png'
import baselineGamePng from '@assets/baseline-game.png'
import nxGraphPng from '@assets/nx-graph.png'
import gameSelectPng from '@assets/game-select.png'
import gameConversationPng from '@assets/game-conversation.png'

## 모듈 1: 모노레포 설정

먼저 새로운 모노레포를 생성합니다. 모노레포는 잘 정의된 관계를 가진 여러 개의 독립적인 프로젝트를 포함하는 단일 저장소입니다.

원하는 디렉토리에서 다음 명령어를 실행하세요:

<CreateNxWorkspaceCommand workspace="dungeon-adventure" />

이 명령은 `dungeon-adventure` 디렉토리 내에 NX 모노레포를 설정합니다.
vscode에서 해당 디렉토리를 열면 다음 파일 구조를 확인할 수 있습니다:

<FileTree>
- .nx/
- .vscode/
- node_modules/
- packages/ 서브 프로젝트가 위치할 곳
- .gitignore
- .npmrc
- .prettierignore
- .prettierrc
- nx.json Nx CLI 및 모노레포 기본값 구성
- package.json 모든 노드 종속성 정의
- pnpm-lock.yaml 또는 bun.lock, yarn.lock, package-lock.json (패키지 매니저에 따라 다름)
- pnpm-workspace.yaml (pnpm 사용 시)
- README.md
- tsconfig.base.json 모든 노드 기반 서브 프로젝트가 상속함
- tsconfig.json
</FileTree>

`@aws/nx-plugin`의 컴포넌트를 모노레포에 추가하기 전에, 개발 종속성으로 설치해야 합니다.
이를 위해 `dungeon-adventure` 모노레포 루트에서 다음 명령어를 실행하세요:

<InstallCommand dev pkg="@aws/nx-plugin" />

이제 `@aws/nx-plugin`을 사용하여 다양한 서브 프로젝트 생성 준비가 완료되었습니다.

<Aside type="tip">생성기를 실행하기 전에 변경 사항을 Git에 커밋할 것을 권장합니다. 이렇게 하면 `git diff`를 통해 생성기 실행 후 변경된 내용을 확인할 수 있습니다.</Aside>

### Game API 생성

게임 상태 관리를 위한 Game API 생성부터 시작하겠습니다.
tRPC API인 `GameApi`를 생성하려면 다음 단계를 따르세요:

<RunGenerator generator="ts#trpc-api" requiredParameters={{apiName:"GameApi"}} noInteractive />

<br />

파일 트리에 새로운 파일들이 나타납니다.

<Drawer title="ts#trpc-api 업데이트된 파일" trigger="자세한 파일 내용 확인을 클릭하세요.">
`ts#trpc-api` 생성기에 의해 생성된 모든 파일 목록입니다. 파일 트리에서 강조된 주요 파일들을 살펴보겠습니다:
<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - app/ 애플리케이션 특화 CDK 구문
          - http-apis/
            - **game-api.ts** tRPC API 생성용 CDK 구문
            - index.ts
            - ...
          - index.ts
        - core/ 일반 CDK 구문
          - http-api.ts Http 기반 API 기본 구문
          - index.ts
          - runtime-config.ts
        - index.ts
      - project.json
      - ...
    - types/ 공유 유형
      - src/
        - index.ts
        - runtime-config.ts CDK와 웹사이트 모두에서 사용되는 인터페이스 정의
      - project.json
      - ...
  - game-api/
    - backend/ tRPC 구현 코드
      - src/
        - client/ 기계 간 호출용 기본 클라이언트
          - index.ts
          - sigv4.ts
        - middleware/ Powertools 계측
          - error.ts
          - index.ts
          - logger.ts
          - metrics.ts
          - tracer.ts
        - procedures/ API 절차/라우트 구현
          - **echo.ts**
        - index.ts
        - init.ts 컨텍스트 및 미들웨어 설정
        - local-server.ts 로컬 tRPC 서버 실행용
        - **router.ts** 모든 절차 정의 람다 핸들러 진입점
      - project.json
      - ...
    - schema/
      - src/
        - procedures/
          - **echo.ts**
        - index.ts
      - project.json
      - ...
- eslint.config.mjs
- vitest.workspace.ts
</FileTree>

주요 파일 분석:

```ts {5,12}
// packages/game-api/backend/src/router.ts
import {
  awsLambdaRequestHandler,
  CreateAWSLambdaContextOptions,
} from '@trpc/server/adapters/aws-lambda';
import { echo } from './procedures/echo.js';
import { t } from './init.js';
import { APIGatewayProxyEventV2WithIAMAuthorizer } from 'aws-lambda';

export const router = t.router;

export const appRouter = router({
  echo,
});

export const handler = awsLambdaRequestHandler({
  router: appRouter,
  createContext: (
    ctx: CreateAWSLambdaContextOptions<APIGatewayProxyEventV2WithIAMAuthorizer>,
  ) => ctx,
});

export type AppRouter = typeof appRouter;
```
라우터는 tRPC API의 진입점이며 모든 API 메서드를 선언하는 곳입니다. 
`echo` 메서드는 `./procedures/echo.ts` 파일에 구현되어 있습니다.

```ts {2-5}
// packages/game-api/backend/src/procedures/echo.ts
import { publicProcedure } from '../init.js';
import {
  EchoInputSchema,
  EchoOutputSchema,
} from ':dungeon-adventure/game-api-schema';

export const echo = publicProcedure
  .input(EchoInputSchema)
  .output(EchoOutputSchema)
  .query((opts) => ({ result: opts.input.message }));
```

이 파일은 입력 및 출력 데이터 구조를 강력하게 타입화한 `echo` 메서드 구현입니다. 스키마 프로젝트에 대한 [별칭](https://www.typescriptlang.org/tsconfig/paths.html)인 `:dungeon-adventure/game-api-schema` 프로젝트에서 정의를 가져옵니다.

<Aside type="tip">IDE 내에서 가져오기 오류가 보이면 백엔드의 `tsconfig.json`에 타입스크립트 참조가 설정되지 않았기 때문일 수 있습니다. Nx는 빌드/컴파일 실행 시 또는 `nx sync` 명령을 수동으로 실행할 때 [동적으로 참조를 생성](https://nx.dev/nx-api/js/generators/typescript-sync)하도록 구성되어 있습니다. 자세한 내용은 <Link path="guides/typescript-project#importing-your-library-code-in-other-projects">타입스크립트 가이드</Link>를 참조하세요.</Aside>

```ts
// packages/game-api/schema/src/procedures/echo.ts
import { z } from 'zod';

export const EchoInputSchema = z.object({
  message: z.string(),
});

export type IEchoInput = z.TypeOf<typeof EchoInputSchema>;

export const EchoOutputSchema = z.object({
  result: z.string(),
});

export type IEchoOutput = z.TypeOf<typeof EchoOutputSchema>;
```

모든 tRPC 스키마 정의는 [Zod](https://zod.dev/)를 사용하여 정의되며, `z.TypeOf` 구문을 통해 타입스크립트 타입으로 내보내집니다.

```ts
// packages/common/constructs/src/app/http-apis/game-api.ts
import { Construct } from 'constructs';
import * as url from 'url';
import { HttpApi } from '../../core/http-api.js';
import { HttpIamAuthorizer } from 'aws-cdk-lib/aws-apigatewayv2-authorizers';
import { Runtime } from 'aws-cdk-lib/aws-lambda';

export class GameApi extends HttpApi {
  constructor(scope: Construct, id: string) {
    super(scope, id, {
      defaultAuthorizer: new HttpIamAuthorizer(),
      apiName: 'GameApi',
      runtime: Runtime.NODEJS_LATEST,
      handler: 'index.handler',
      handlerFilePath: url.fileURLToPath(
        new URL(
          '../../../../../../dist/packages/game-api/backend/bundle',
          import.meta.url,
        ),
      ),
    });
  }
}
```

이 CDK 구문은 GameAPI를 정의하며, `cdk synth` 시 번들링이 발생하지 않도록 핸들러 파일 경로를 tRPC 백엔드 구현의 생성된 번들로 구성합니다([NodeJsFunction](https://docs.aws.amazon.com/cdk/api/v2/docs/aws-cdk-lib.aws_lambda_nodejs.NodejsFunction.html) 사용과 반대).

</Drawer>

<Aside>
루트 `package.json`은 이제 모든 노드 기반 서브 프로젝트에 대해 ESM을 기본 모듈 유형으로 사용하도록 `type`이 `module`로 구성되었습니다. 자세한 내용은 <Link path="guides/typescript-project">ts#project 가이드</Link>를 참조하세요.
</Aside>

### Story API 생성

다음으로 Amazon Bedrock을 사용한 스트리밍 응답용 FastAPI 생성기로 Story API를 생성하겠습니다.
`StoryApi`라는 Fast API를 생성하려면 다음 단계를 따르세요:

<RunGenerator generator="py#fast-api" requiredParameters={{name:"StoryApi"}} noInteractive />

파일 트리에 새로운 파일들이 나타납니다.

<Drawer title="py#fast-api 업데이트된 파일" trigger="자세한 파일 내용 확인을 클릭하세요.">
`py#fast-api` 생성기에 의해 생성된 모든 파일 목록입니다. 주요 파일 분석:
<FileTree>
- .venv/ 모노레포용 단일 가상 환경
- packages/
  - common/
    - constructs/
      - src/
        - app/ 애플리케이션 특화 CDK 구문
          - http-apis/
            - **story-api.ts** Fast API 생성용 CDK 구문
            - index.ts 새 story-api 내보내기로 업데이트
      - project.json story_api 빌드 종속성 추가로 업데이트
    - types/ 공유 유형
      - src/
        - **runtime-config.ts** StoryApi 추가로 업데이트
  - story_api/
    - story_api/ 파이썬 모듈
      - init.py Powertools, FastAPI 및 미들웨어 설정
      - **main.py** 모든 라우트 포함 람다 진입점
    - tests/
    - .python-version
    - project.json
    - pyproject.toml
    - project.json
- .python-version 고정 uv 파이썬 버전
- pyproject.toml
- uv.lock
</FileTree>

```ts
// packages/common/constructs/src/app/http-apis/story-api.ts
import { Construct } from 'constructs';
import * as url from 'url';
import { HttpApi } from '../../core/http-api.js';
import { HttpIamAuthorizer } from 'aws-cdk-lib/aws-apigatewayv2-authorizers';
import { Runtime } from 'aws-cdk-lib/aws-lambda';

export class StoryApi extends HttpApi {
  constructor(scope: Construct, id: string) {
    super(scope, id, {
      defaultAuthorizer: new HttpIamAuthorizer(),
      apiName: 'StoryApi',
      runtime: Runtime.PYTHON_3_12,
      handler: 'story_api.main.handler',
      handlerFilePath: url.fileURLToPath(
        new URL(
          '../../../../../../dist/packages/story_api/bundle',
          import.meta.url,
        ),
      ),
    });
  }
}
```

이 CDK 구문은 StoryApi를 정의하며, 핸들러 파일 경로를 Fast API 백엔드 구현의 생성된 번들로 구성합니다. 이는 [PythonFunction](https://docs.aws.amazon.com/cdk/api/v2/docs/@aws-cdk_aws-lambda-python-alpha.PythonFunction.html) 사용과 달리 `cdk synth` 시 번들링이 발생하지 않음을 의미합니다.

```diff lang="ts"
// packages/common/types/src/runtime-config.ts
export type ApiUrl = string;
// eslint-disable-next-line @typescript-eslint/no-empty-object-type, @typescript-eslint/no-empty-interface
export interface IRuntimeConfig {
  httpApis: {
    GameApi: ApiUrl;
+    StoryApi: ApiUrl;
  };
}
```

생성기가 기존 코드를 보존하고 업데이트를 수행하는 AST 변환 예시입니다. `StoryApi`가 `IRuntimeConfig` 정의에 추가되어 프론트엔드에서 사용 시 타입 안전성을 보장합니다!

```py
// packages/story_api/story_api/main.py
from .init import app, lambda_handler, tracer

handler = lambda_handler

@app.get("/")
@tracer.capture_method
def read_root():
    return {"Hello": "World"}
```

모든 API 메서드가 정의되는 곳입니다. `GET /` 라우트에 매핑된 `read_root` 메서드가 있으며, [Pydantic](https://docs.pydantic.dev/latest/)을 사용하여 입력 및 출력을 선언하여 타입 안전성을 보장할 수 있습니다.

</Drawer>

### Game UI 웹사이트 추가

CloudScape 웹사이트 생성기를 사용하여 게임 상호작용용 UI를 생성하세요.
`GameUI` 웹사이트를 생성하려면 다음 단계를 따르세요:

<RunGenerator generator="ts#cloudscape-website" requiredParameters={{name:"GameUI"}} noInteractive />

파일 트리에 새로운 파일들이 나타납니다.

<Drawer title="ts#cloudscape-website 업데이트된 파일" trigger="자세한 파일 내용 확인을 클릭하세요.">
`ts#cloudscape-website` 생성기에 의해 생성된 주요 파일 분석:
<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - app/ 애플리케이션 특화 CDK 구문
          - static-websites/
            - **game-ui.ts** Game UI 생성용 CDK 구문
        - core/
          - static-website.ts 일반 정적 웹사이트 구문
  - game-ui/
    - public/
    - src/
      - components/
        - AppLayout/
          - index.ts 전체 페이지 레이아웃: 헤더, 푸터, 사이드바 등
          - navitems.ts 사이드바 네비게이션 항목
      - hooks/
        - useAppLayout.tsx 알림, 페이지 스타일 등 동적 설정 허용
      - routes/ @tanstack/react-router 파일 기반 라우트
        - index.tsx 루트 '/' 페이지에서 '/welcome'으로 리디렉션
        - __root.tsx 모든 페이지의 기본 컴포넌트
        - welcome/
          - **index.tsx**
        - config.ts
        - **main.tsx** React 진입점
        - routeTree.gen.ts @tanstack/react-router에 의해 자동 업데이트
        - styles.css
    - index.html
    - project.json
    - vite.config.ts
    - ...
</FileTree>

```ts
// packages/common/constructs/src/app/static-websites/game-ui.ts
import * as url from 'url';
import { Construct } from 'constructs';
import { StaticWebsite } from '../../core/index.js';

export class GameUI extends StaticWebsite {
  constructor(scope: Construct, id: string) {
    super(scope, id, {
      websiteFilePath: url.fileURLToPath(
        new URL(
          '../../../../../../dist/packages/game-ui/bundle',
          import.meta.url,
        ),
      ),
    });
  }
}
```

이 CDK 구문은 GameUI를 정의하며, Vite 기반 UI의 생성된 번들 경로가 이미 구성되어 있습니다. 이는 빌드 시 번들링이 game-ui 프로젝트의 빌드 대상 내에서 발생함을 의미합니다.

```tsx
// packages/game-ui/src/main.tsx
import React from 'react';
import { createRoot } from 'react-dom/client';
import { I18nProvider } from '@cloudscape-design/components/i18n';
import messages from '@cloudscape-design/components/i18n/messages/all.en';
import { RouterProvider, createRouter } from '@tanstack/react-router';
import { routeTree } from './routeTree.gen';

import '@cloudscape-design/global-styles/index.css';

const router = createRouter({ routeTree });

// 타입 안전성을 위해 라우터 인스턴스 등록
declare module '@tanstack/react-router' {
  interface Register {
    router: typeof router;
  }
}

const root = document.getElementById('root');
root &&
  createRoot(root).render(
    <React.StrictMode>
      <I18nProvider locale="en" messages={[messages]}>
        <RouterProvider router={router} />
      </I18nProvider>
    </React.StrictMode>,
  );
```

React가 마운트되는 진입점입니다. [`파일 기반 라우팅`](https://tanstack.com/router/v1/docs/framework/react/routing/file-based-routing) 구성으로 `@tanstack/react-router`를 초기 설정합니다. 개발 서버가 실행 중인 동안 `routes` 폴더 내에 파일을 생성하면 `@tanstack/react-router`가 보일러플레이트 파일 설정을 자동으로 생성하고 `routeTree.gen.ts` 파일을 업데이트합니다. 이 파일은 모든 라우트를 타입 안전 방식으로 유지하므로 `<Link>` 사용 시 `to` 옵션에 유효한 라우트만 표시됩니다. 자세한 내용은 [`@tanstack/react-router` 문서](https://tanstack.com/router/v1/docs/framework/react/quick-start) 참조.

```tsx
// packages/game-ui/src/routes/welcome/index.tsx
import {
  ContentLayout,
  Header,
  SpaceBetween,
  Container,
} from '@cloudscape-design/components';
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/welcome/')({
  component: RouteComponent,
});

function RouteComponent() {
  return (
    <ContentLayout header={<Header>Welcome</Header>}>
      <SpaceBetween size="l">
        <Container>Welcome to your new Cloudscape website!</Container>
      </SpaceBetween>
    </ContentLayout>
  );
}
```

`/welcome` 라우트로 이동할 때 렌더링될 컴포넌트입니다. 개발 서버가 실행 중인 동안 파일 생성/이동 시 `@tanstack/react-router`가 `Route`를 관리합니다. 이는 이후 튜토리얼 섹션에서 확인됩니다.

</Drawer>

### Game UI 인증 구성

Amazon Cognito를 통한 인증 액세스로 Game UI를 구성하려면 다음 단계를 따르세요:

<RunGenerator generator="ts#cloudscape-website#auth" requiredParameters={{cognitoDomain:"game-ui", project:"@dungeon-adventure/game-ui", allowSignup:true}} noInteractive />

파일 트리에 새로운 파일들이 나타납니다.

<Drawer title="ts#cloudscape-website#auth 업데이트된 파일" trigger="자세한 파일 내용 확인을 클릭하세요.">
`ts#cloudscape-website#auth` 생성기에 의해 생성/업데이트된 주요 파일 분석:
<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - core/
          - user-identity.ts 사용자/ID 풀 생성용 CDK 구문
    - types/
      - src/
        - runtime-config.ts cognitoProps 추가로 업데이트
  - game-ui/
    - src/
      - components/
        - AppLayout/
          - index.tsx 헤더에 로그인 사용자/로그아웃 추가
        - CognitoAuth/
          - index.ts Cognito 로그인 관리
        - RuntimeConfig/
          - index.tsx `runtime-config.json` 가져와 컨텍스트를 통해 자식 제공
      - hooks/
        - useRuntimeConfig.tsx
      - **main.tsx** Cognito 추가로 업데이트
</FileTree>

```diff lang="tsx"
// packages/game-ui/src/main.tsx
+import CognitoAuth from './components/CognitoAuth';
+import RuntimeConfigProvider from './components/RuntimeConfig';
import React from 'react';
import { createRoot } from 'react-dom/client';
import { I18nProvider } from '@cloudscape-design/components/i18n';
import messages from '@cloudscape-design/components/i18n/messages/all.en';
import { RouterProvider, createRouter } from '@tanstack/react-router';
import { routeTree } from './routeTree.gen';
import '@cloudscape-design/global-styles/index.css';
const router = createRouter({ routeTree });
// 타입 안전성을 위해 라우터 인스턴스 등록
declare module '@tanstack/react-router' {
  interface Register {
    router: typeof router;
  }
}
const root = document.getElementById('root');
root &&
  createRoot(root).render(
    <React.StrictMode>
      <I18nProvider locale="en" messages={[messages]}>
+        <RuntimeConfigProvider>
+          <CognitoAuth>
            <RouterProvider router={router} />
+          </CognitoAuth>
+        </RuntimeConfigProvider>
      </I18nProvider>
    </React.StrictMode>,
  );
```

`RuntimeConfigProvider`와 `CognitoAuth` 컴포넌트가 AST 변환을 통해 `main.tsx` 파일에 추가되었습니다. 이는 `CognitoAuth` 컴포넌트가 Amazon Cognito와 인증할 수 있도록 `runtime-config.json`을 가져와 올바른 대상에 대한 백엔드 호출 구성을 가능하게 합니다.

</Drawer>

### Game UI와 Story API 연결 구성

이전에 생성한 Story API에 Game UI를 연결하려면 다음 단계를 따르세요:

<RunGenerator generator="api-connection" requiredParameters={{sourceProject:"@dungeon-adventure/game-ui", targetProject:"dungeon_adventure.story_api"}} noInteractive />

파일 트리에 새로운 파일들이 나타납니다.

<Drawer title="UI -> FastAPI api-connection 업데이트된 파일" trigger="자세한 파일 내용 확인을 클릭하세요.">
`api-connection` 생성기에 의해 생성/업데이트된 주요 파일 분석:
<FileTree>
- packages/
  - game-ui/
    - src/
      - hooks/
        - useSigV4.tsx StoryApi 요청 서명용
        - useStoryApiClient.tsx StoryApi 클라이언트 생성 훅
        - useStoryApi.tsx TanStack Query 사용 StoryApi 상호작용 훅
      - components/
        - QueryClientProvider.tsx TanStack Query 클라이언트 제공자
        - StoryApiProvider.tsx StoryApi TanStack Query 훅 제공자
      - main.tsx QueryClientProvider 및 StoryApiProvider 계측
    - .gitignore 생성된 클라이언트 파일 무시
    - project.json OpenAPI 훅 생성 대상 추가로 업데이트
    - ...
  - story_api/
    - scripts/
      - generate_open_api.py
    - project.json openapi.json 파일 생성으로 업데이트

</FileTree>

```tsx {1,12-15}
// packages/game-ui/src/hooks/useStoryApiClient.tsx
import { StoryApi } from '../generated/story-api/client.gen';
import { useSigV4 } from './useSigV4';
import { useRuntimeConfig } from './useRuntimeConfig';
import { useMemo } from 'react';

export const useStoryApi = (): StoryApi => {
  const runtimeConfig = useRuntimeConfig();
  const apiUrl = runtimeConfig.httpApis.StoryApi;
  const sigv4Client = useSigV4();
  return useMemo(
    () =>
      new StoryApi({
        url: apiUrl,
        fetch: sigv4Client,
      }),
    [apiUrl, sigv4Client],
  );
};
```

이 훅을 사용하여 `StoryApi`에 인증된 API 요청을 할 수 있습니다. 빌드 시 생성되는 `StoryApi`를 사용하며, 코드 빌드 전까지 IDE에서 오류가 표시됩니다. 클라이언트 생성 방식 및 API 사용 방법은 <Link path="guides/api-connection/react-fastapi">React to FastAPI 가이드</Link> 참조.

```tsx
// packages/game-ui/src/components/StoryApiProvider.tsx
import { createContext, FC, PropsWithChildren, useMemo } from 'react';
import { useStoryApiClient } from '../hooks/useStoryApiClient';
import { StoryApiOptionsProxy } from '../generated/story-api/options-proxy.gen';

export const StoryApiContext = createContext<StoryApiOptionsProxy | undefined>(
  undefined,
);

export const StoryApiProvider: FC<PropsWithChildren> = ({ children }) => {
  const client = useStoryApiClient();
  const optionsProxy = useMemo(
    () => new StoryApiOptionsProxy({ client }),
    [client],
  );

  return (
    <StoryApiContext.Provider value={optionsProxy}>
      {children}
    </StoryApiContext.Provider>
  );
};

export default StoryApiProvider;
```

이 제공자 컴포넌트는 `useStoryApiClient` 훅을 사용하며, `StoryApiOptionsProxy`를 인스턴스화합니다. 이 프록시는 TanStack Query 훅 옵션을 구성하는 데 사용되며, `useStoryApi` 훅을 사용하여 FastAPI와 상호작용할 수 있습니다.

`useStoryApiClient`가 스트리밍 API용 async iterator를 제공하므로 본 튜토리얼에서는 기본 클라이언트를 직접 사용합니다.

<Aside type="caution">
`src/generated/story-api/*.gen.ts` 파일은 수동으로 수정하면 안 되며, API 빌드 시마다 재생성됩니다.
</Aside>

</Drawer>

### Game UI와 Game API 연결 구성

Game API에 Game UI를 연결하려면 다음 단계를 따르세요:

<RunGenerator generator="api-connection" requiredParameters={{sourceProject:"@dungeon-adventure/game-ui", targetProject:"@dungeon-adventure/game-api-backend"}} noInteractive />

파일 트리에 새로운 파일들이 생성/변경됩니다.

<Drawer title="UI -> tRPC api-connection 업데이트된 파일" trigger="자세한 파일 내용 확인을 클릭하세요.">
`api-connection` 생성기에 의해 생성/업데이트된 주요 파일 분석:
<FileTree>
- packages/
  - game-ui/
    - src/
      - components/
        - TrpcClients/
          - index.tsx
          - TrpcApis.tsx 모든 구성된 tRPC API
          - TrpcClientProviders.tsx tRPC API별 클라이언트 제공자 생성
          - TrpcProvider.tsx
      - hooks/
        - **useGameApi.tsx** GameApi 호출 훅
      - **main.tsx** tRPC 클라이언트 제공자 주입
- package.json

</FileTree>

```tsx
// packages/game-ui/src/hooks/useGameApi.tsx
import { TrpcApis } from '../components/TrpcClients';

export const useGameApi = () => TrpcApis.GameApi.useTRPC();
```

이 훅은 tRPC의 최신 [React Query 통합](https://trpc.io/blog/introducing-tanstack-react-query-client)을 사용하여 추가 추상화 계층 없이 `@tanstack/react-query`와 직접 상호작용할 수 있습니다. tRPC API 호출 예시는 <Link path="guides/api-connection/react-trpc#using-the-generated-code">tRPC 훅 사용 가이드</Link> 참조.

<Aside>
`useGameApi` 훅은 빌드 없이 변경 사항이 즉시 반영되는 점에서 `useStoryApi` 훅과 다릅니다. 이는 tRPC의 [타입스크립트 추론](https://trpc.io/docs/concepts) 덕분에 개발자가 백엔드 변경 사항을 프론트엔드에 즉시 반영할 수 있습니다!
</Aside>

```diff lang="tsx"
// packages/game-ui/src/main.tsx
+import TrpcClientProviders from './components/TrpcClients';
+import QueryClientProvider from './components/QueryClientProvider';
import CognitoAuth from './components/CognitoAuth';
import RuntimeConfigProvider from './components/RuntimeConfig';
import React from 'react';
import { createRoot } from 'react-dom/client';
import { I18nProvider } from '@cloudscape-design/components/i18n';
import messages from '@cloudscape-design/components/i18n/messages/all.en';
import { RouterProvider, createRouter } from '@tanstack/react-router';
import { routeTree } from './routeTree.gen';
import '@cloudscape-design/global-styles/index.css';
const router = createRouter({ routeTree });
// 타입 안전성을 위해 라우터 인스턴스 등록
declare module '@tanstack/react-router' {
  interface Register {
    router: typeof router;
  }
}
const root = document.getElementById('root');
root &&
  createRoot(root).render(
    <React.StrictMode>
      <I18nProvider locale="en" messages={[messages]}>
        <RuntimeConfigProvider>
          <CognitoAuth>
+            <QueryClientProvider>
+              <TrpcClientProviders>
                <RouterProvider router={router} />
+              </TrpcClientProviders>
+            </QueryClientProvider>
          </CognitoAuth>
        </RuntimeConfigProvider>
      </I18nProvider>
    </React.StrictMode>,
  );
```

`main.tsx` 파일이 AST 변환을 통해 tRPC 제공자 주입으로 업데이트되었습니다.

</Drawer>

### Game UI CDK 인프라 생성

최종 서브 프로젝트로 CDK 인프라를 생성하려면 다음 단계를 따르세요:

<RunGenerator generator="ts#infra" requiredParameters={{name:"infra"}} noInteractive />

파일 트리에 새로운 파일들이 생성/변경됩니다.

<Drawer title="ts#infra 업데이트된 파일" trigger="자세한 파일 내용 확인을 클릭하세요.">
`ts#infra` 생성기에 의해 생성/업데이트된 주요 파일 분석:
<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - core/
          - cfn-guard-rules/
            - *.guard
          - cfn-guard.ts
          - index.ts
  - infra
    - src/
      - stacks/
        - **application-stack.ts** CDK 리소스 정의
      - index.ts
      - **main.ts** 모든 스택 정의 진입점
    - cdk.json
    - project.json
    - ...
  - package.json
  - tsconfig.json 참조 추가
  - tsconfig.base.json 별칭 추가

</FileTree>

```ts
// packages/infra/src/main.ts
import { ApplicationStack } from './stacks/application-stack.js';
import {
  App,
  CfnGuardValidator,
  RuleSet,
} from ':dungeon-adventure/common-constructs';

const app = new App({
  policyValidationBeta1: [new CfnGuardValidator(RuleSet.AWS_PROTOTYPING)],
});

// CLI 자격 증명 기반 샌드박스 환경 배포용
new ApplicationStack(app, 'dungeon-adventure-infra-sandbox', {
  env: {
    account: process.env.CDK_DEFAULT_ACCOUNT,
    region: process.env.CDK_DEFAULT_REGION,
  },
  crossRegionReferences: true,
});

app.synth();
```

CDK 애플리케이션 진입점입니다. [`cfn-guard`](https://github.com/cdklabs/cdk-validator-cfnguard)를 사용하여 구성된 규칙 세트 기반 인프라 유효성 검사를 실행하도록 구성됩니다.

<Aside type="tip">
특정 리소스에 대한 규칙 억제가 필요한 경우 두 가지 방법이 있습니다:

###### 특정 구문에 규칙 억제

```typescript
import { suppressRule } from ':dungeon-adventure/common-constructs';

...
// 주어진 구문에 대해 RULE_NAME 억제
suppressRule(construct, 'RULE_NAME');
```

###### 하위 구문에 규칙 억제

```typescript
import { suppressRule } from ':dungeon-adventure/common-constructs';

...
// Bucket 인스턴스일 경우 RULE_NAME 억제
suppressRule(construct, 'RULE_NAME', (construct) => construct instanceof Bucket);
```
</Aside>

```ts
// packages/infra/src/stacks/application-stack.ts
import * as cdk from 'aws-cdk-lib';
import { Construct } from 'constructs';

export class ApplicationStack extends cdk.Stack {
  constructor(scope: Construct, id: string, props?: cdk.StackProps) {
    super(scope, id, props);

    // 스택 정의 코드 위치
  }
}
```

던전 어드벤처 게임 구축을 위해 CDK 구문을 인스턴스화하는 곳입니다.

</Drawer>

#### CDK 인프라 업데이트

생성된 구문을 인스턴스화하려면 `packages/infra/src/stacks/application-stack.ts`를 업데이트하세요:

```diff lang="ts"
+import {
+  GameApi,
+  GameUI,
+  StoryApi,
+  UserIdentity,
+} from ':dungeon-adventure/common-constructs';
import * as cdk from 'aws-cdk-lib';
import { Construct } from 'constructs';

export class ApplicationStack extends cdk.Stack {
  constructor(scope: Construct, id: string, props?: cdk.StackProps) {
    super(scope, id, props);

-    // 스택 정의 코드 위치
+    const userIdentity = new UserIdentity(this, 'UserIdentity');
+
+    const gameApi = new GameApi(this, 'GameApi');
+    const storyApi = new StoryApi(this, 'StoryApi');
+
+    // 인증된 역할에 API 호출 권한 부여
+    [storyApi, gameApi].forEach((api) =>
+      api.grantInvokeAccess(userIdentity.identityPool.authenticatedRole),
+    );
+
+    // runtime-config.json 자동 구성 위해 마지막에 인스턴스화
+    new GameUI(this, 'GameUI');
  }
}

```

### 코드 빌드

<Drawer title="Nx 명령어" trigger="이제 처음으로 코드를 빌드할 시간입니다">
###### 단일 vs 다중 대상

`run-many` 명령은 여러 서브프로젝트에 대해 대상 작업을 실행합니다(`--all` 사용 시 모두 대상). 종속성이 올바른 순서로 실행되도록 보장합니다.

단일 프로젝트 대상에 대해 빌드(또는 기타 작업)를 트리거할 수도 있습니다. 예를 들어 `@dungeon-adventure/infra` 프로젝트 빌드는 다음 명령으로 실행 가능:

<NxCommands commands={['run @dungeon-adventure/infra:build']} />
###### 종속성 시각화

종속성 시각화:

<NxCommands commands={['graph']} />
<br/>

<Image src={nxGraphPng} alt="nx-graph.png" width="800" height="600" />

###### 캐싱

Nx는 [캐싱](https://nx.dev/concepts/how-caching-works)을 통해 이전 빌드 아티팩트 재사용으로 개발 속도 향상. 캐시 없이 빌드하려면 `--skip-nx-cache` 인수 추가:

<NxCommands commands={['run @dungeon-adventure/infra:build --skip-nx-cache']} />
캐시 삭제 시(.nx 폴더):

<NxCommands commands={['reset']} />

</Drawer>

<NxCommands commands={['run-many --target build --all']} />

다음 프롬프트가 표시됩니다:

```bash
 NX   워크스페이스 동기화 필요

[@nx/js:typescript-sync]: 일부 타입스크립트 구성 파일에 종속 프로젝트 참조가 누락되었거나 오래된 참조가 포함되어 있습니다.

이로 인해 CI에서 오류가 발생할 수 있습니다.

? 작업 실행 전 동기화하시겠습니까? …
예, 변경 사항 동기화 후 작업 실행
아니오, 동기화 없이 작업 실행
```

이 메시지는 NX가 자동 업데이트 가능한 파일을 감지했음을 나타냅니다. 이 경우 타입스크립트 참조가 누락된 `tsconfig.json` 파일을 의미합니다. **예, 변경 사항 동기화 후 작업 실행** 옵션 선택 진행. 동기화 생성기가 누락된 타입스크립트 참조를 자동 추가하므로 IDE 가져오기 오류가 해결됩니다!

<Aside type="tip">
린트 오류 발생 시 다음 명령으로 자동 수정 가능:

<NxCommands commands={['run-many --target lint --configuration=fix --all']} />
</Aside>

<Aside type="caution" title="Windows 빌드 실패">
<Drawer trigger="Windows에서 빌드 오류 발생 시 클릭하세요." title="Windows 빌드 실패">
`@dungeon-adventure/infra` 프로젝트 빌드/신스 오류 발생 시 `cfn-guard` 계측 라이브러리의 Windows 미지원 때문입니다. 기능 요청이 진행 중이며, 임시로 `packages/infra/src/main.ts` 파일을 다음과 같이 수정:

```diff lang="ts"
// packages/infra/src/main.ts
import { ApplicationStack } from './stacks/application-stack.js';
import {
   App,
-  CfnGuardValidator,
-  RuleSet,
} from ':dungeon-adventure/common-constructs';
-
-const app = new App({
-  policyValidationBeta1: [new CfnGuardValidator(RuleSet.AWS_PROTOTYPING)],
-});
+const app = new App();

// CLI 자격 증명 기반 샌드박스 환경 배포용
new ApplicationStack(app, 'dungeon-adventure-infra-sandbox', {
  env: {
    account: process.env.CDK_DEFAULT_ACCOUNT,
    region: process.env.CDK_DEFAULT_REGION,
  },
  crossRegionReferences: true,
});

app.synth();
```
</Drawer>
</Aside>

모든 빌드 아티팩트는 모노레포 루트의 `dist/` 폴더 내에서 사용 가능합니다. `@aws/nx-plugin` 생성 프로젝트에서는 생성 파일이 파일 트리에 흩어지지 않도록 표준 관행입니다. 생성 파일 정리 시 `dist/` 폴더 삭제 가능.

축하합니다! 던전 어드벤처 게임 코어 구현을 시작하는 데 필요한 모든 서브 프로젝트 생성 완료했습니다.  🎉🎉🎉