---
title: DynamoDB 로컬 개발
---

`connection` 생성기는 프로젝트의 `serve-local` 타겟이 DynamoDB 프로젝트의 `serve-local` 타겟에 의존하도록 구성합니다. `serve-local`을 실행하면 DynamoDB Local이 프로젝트와 함께 자동으로 시작됩니다.

`SERVE_LOCAL=true` 환경 변수가 자동으로 설정되므로, `getDynamoDBClient()`와 `resolveTableName()`은 AWS 대신 로컬 DynamoDB Local 인스턴스에 연결됩니다.
