---
title: MCP 서버 구성
---

### 구성 파일

MCP를 지원하는 대부분의 AI 어시스턴트는 유사한 구성 방식을 사용합니다. MCP 서버 세부 정보로 구성 파일을 생성하거나 업데이트해야 합니다:

```json
{
  "mcpServers": {
    "your-mcp-server": {
      "command": "npx",
      "args": ["tsx", "/path/to/your-mcp-server/stdio.ts"]
    }
  }
}
```

:::caution[경로 해석]
서버에 연결할 때 `ENOENT npx`와 같은 오류가 발생하면 `npx`의 전체 경로를 지정해야 할 수 있습니다. 터미널에서 `which npx`를 실행하여 경로를 얻을 수 있습니다.
:::

### 핫 리로드

MCP 서버를 개발하는 동안 AI 어시스턴트가 항상 최신 버전의 도구/리소스를 볼 수 있도록 `--watch` 플래그를 구성할 수 있습니다:

```json {5}
{
  "mcpServers": {
    "your-mcp-server": {
      "command": "npx",
      "args": ["tsx", "--watch", "/path/to/your-mcp-server/stdio.ts"]
    }
  }
}
```

:::note[도구 새로고침]
새로운 도구나 리소스를 추가하는 경우 AI 어시스턴트에서 MCP 서버를 새로고침해야 할 수 있습니다.
:::

### 어시스턴트별 구성

특정 AI 어시스턴트로 MCP를 구성하는 방법은 다음 문서를 참조하세요:

import Snippet from '@components/snippet.astro';

<Snippet name="mcp/assistant-docs" />

:::tip[워크스페이스 구성]
Kiro와 같은 일부 AI 어시스턴트는 워크스페이스 수준의 MCP 서버 구성을 지정할 수 있으며, 이는 특정 프로젝트에 관련된 MCP 서버를 정의하는 데 특히 유용합니다.
:::