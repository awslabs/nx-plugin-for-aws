---
title: DynamoDB
description: 创建 DynamoDB 项目
generator: ts#dynamodb
---

import { FileTree } from '@astrojs/starlight/components';
import Link from '@components/link.astro';
import RunGenerator from '@components/run-generator.astro';
import GeneratorParameters from '@components/generator-parameters.astro';
import Infrastructure from '@components/infrastructure.astro';
import NxCommands from '@components/nx-commands.astro';
import Snippet from '@components/snippet.astro';
import OptionFilter from '@components/option-filter.astro';

此生成器创建一个由 [Amazon DynamoDB](https://aws.amazon.com/dynamodb/) 支持的新 TypeScript 项目，使用 [ElectroDB](https://electrodb.dev/) 进行类型安全的实体建模。它生成应用程序代码和基础设施，用于使用 AWS CDK 或 Terraform 配置和管理 DynamoDB 表，支持单表设计，并通过 DynamoDB Local 内置本地开发功能。

## 用法

### 生成 DynamoDB 项目

<RunGenerator generator="ts#dynamodb" />

### 选项

<GeneratorParameters generator="ts#dynamodb" />

## 生成器输出

生成器在 `<directory>/<name>` 目录中创建以下项目结构：

<FileTree>
  - scripts
    - create-local-table.ts 在本地 DynamoDB Local 实例中创建 DynamoDB 表
    - pull-image.ts 拉取 DynamoDB Local 镜像
    - start-container.ts 启动 DynamoDB Local 容器
  - src
    - index.ts 项目入口点和导出
    - constants.ts 本地开发常量和运行时配置键
    - client.ts DynamoDB 客户端单例和表名解析
    - entities
      - example.ts 示例 ElectroDB 实体定义
      - index.ts 实体导出
  - project.json 项目配置和构建目标
</FileTree>

### 基础设施

<Snippet name="shared-constructs" />

<Infrastructure>
  <Fragment slot="cdk">
    <FileTree>
      - packages/common/constructs/src
        - app
          - dynamodb
            - \<name>.ts 特定于您的表的基础设施
        - core
          - dynamodb.ts 通用 DynamoDB 表构造
    </FileTree>
  </Fragment>
  <Fragment slot="terraform">
    <FileTree>
      - packages/common/terraform/src
        - app
          - dynamodb
            - \<name>.tf 特定于您的表的模块
        - core
          - dynamodb.tf 通用 DynamoDB 模块
    </FileTree>
  </Fragment>
</Infrastructure>

## 本地开发

### 启动本地 DynamoDB

生成器配置了一个 `serve-local` 目标，启动 [DynamoDB Local](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.html) 实例并创建表：

<NxCommands commands={['run <project-name>:serve-local']} />

这会自动：
1. 拉取 DynamoDB Local 镜像（`pull-image` 目标）
2. 启动容器
3. 创建带有预定义索引的本地表

### 数据建模

生成的项目使用 [ElectroDB](https://electrodb.dev/) 在单个 DynamoDB 表上进行类型安全的实体建模。在 `src/entities/` 下添加或更新实体文件，使用生成的示例实体作为起点。

示例实体定义：

```ts title="packages/my-table/src/entities/example.ts"
import { Entity } from 'electrodb';
import { getDynamoDBClient, resolveTableName } from '../client.js';

export const createExampleEntity = async () =>
  new Entity(
    {
      model: {
        entity: 'example',
        version: '1',
        service: 'MyTable',
      },
      attributes: {
        id: {
          type: 'string',
          required: true,
        },
        createdAt: {
          type: 'string',
          required: true,
          default: () => new Date().toISOString(),
          readOnly: true,
        },
        updatedAt: {
          type: 'string',
          required: true,
          default: () => new Date().toISOString(),
          watch: '*',
          set: () => new Date().toISOString(),
        },
      },
      indexes: {
        primary: {
          pk: {
            field: 'pk',
            composite: ['id'],
          },
          sk: {
            field: 'sk',
            composite: [],
          },
        },
      },
    },
    { client: getDynamoDBClient(), table: await resolveTableName() },
  );
```

有关更多详细信息，请参阅 [ElectroDB 实体文档](https://electrodb.dev/en/modeling/entities/)。

### 使用 DynamoDB 客户端

生成的 `src/client.ts` 导出两个关键实用程序：

- `getDynamoDBClient()` — 返回缓存的单例 `DynamoDBClient`。当 `SERVE_LOCAL=true` 时，连接到本地 DynamoDB Local 实例；否则使用默认凭证链创建 AWS 客户端。
- `resolveTableName()` — 返回 DynamoDB 表名。当 `SERVE_LOCAL=true` 时，返回本地表名常量；否则使用 `RUNTIME_CONFIG_APP_ID` 环境变量从 AWS AppConfig 获取名称，并为后续调用缓存它。

### 停止本地 DynamoDB

停止 `serve-local`（例如使用 `Ctrl+C`）会自动删除 DynamoDB Local 容器，但会保留命名卷，以便您的数据在重启后保持不变。

:::caution[Windows]
由于 Windows 上信号处理的限制，停止 `serve-local` 时不会自动删除容器。您需要手动删除它：

```bash
<engine> rm -f <scope>-dynamodb
```

将 `<engine>` 替换为您的容器引擎（`docker` 或 `finch`），将 `<scope>` 替换为您的 Nx 工作区作用域（例如 `proj`）。
:::

## 添加/删除全局二级索引

首先更新 `src/gsi.ts` 中的 `GLOBAL_SECONDARY_INDEXES` — 这是您的 GSI 定义的真实来源，在下次 `serve-local` 运行时，脚本将自动在本地表上添加或删除索引以反映新列表。使用 CDK 时，这些更改也会在下次部署时自动反映在您的 AWS 表中。

<OptionFilter when={{ iacProvider: 'Terraform' }}>
  与 CDK 不同，对 `src/gsi.ts` 的更改不会自动反映在您的 Terraform 模块中。在部署之前，您还必须手动更新 Terraform 模块以添加或删除相应的 GSI 定义。
</OptionFilter>

## 连接到表

在任何 TypeScript 项目中，从您的 DynamoDB 包导入实体工厂并直接使用它们：

```ts
import { createExampleEntity } from ':my-scope/my-table';

const entity = await createExampleEntity();
const result = await entity.query.primary({ id: '123' }).go();
```

:::note[运行时配置]
在 AWS 中运行时，`resolveTableName()` 使用 `RUNTIME_CONFIG_APP_ID` 环境变量从 AWS AppConfig 获取表名。使用此插件构建的项目（tRPC API、Smithy API、代理、MCP 服务器）已自动配置此变量。对于其他 TypeScript 项目，请确保在运行时环境中设置 `RUNTIME_CONFIG_APP_ID`，其值为您的基础设施配置的 AppConfig 应用程序 ID。
:::

### 连接生成器

对于特定的项目类型，使用 `connection` 生成器自动连接本地开发依赖项，以便 DynamoDB Local 与您的项目一起自动启动：

- <Link path="guides/connection/trpc-dynamodb">tRPC API → DynamoDB</Link>
- <Link path="guides/connection/smithy-dynamodb">Smithy API → DynamoDB</Link>
- <Link path="guides/connection/ts-agent-dynamodb">TypeScript Agent → DynamoDB</Link>
- <Link path="guides/connection/ts-mcp-server-dynamodb">MCP Server → DynamoDB</Link>

## 部署您的表

DynamoDB 生成器根据您选择的 `iacProvider` 创建 CDK 或 Terraform 基础设施。

<Infrastructure>
<Fragment slot="cdk">
CDK 构造在 `common/constructs` 中创建。示例用法：

```ts title="packages/infra/src/stacks/application-stack.ts"
import { MyTable } from ':my-scope/common-constructs';

export class ApplicationStack extends Stack {
  constructor(scope: Construct, id: string, props?: StackProps) {
    super(scope, id, props);

    const table = new MyTable(this, 'Table');
  }
}
```

这将配置一个具有以下特性的 DynamoDB 表：
- `pk`（分区键）和 `sk`（排序键），均为 `String` 类型
- 默认 2 个全局二级索引，如 `src/gsi.ts` 中定义
- 按需（`PAY_PER_REQUEST`）计费
- 客户管理的 KMS 加密，具有自动密钥轮换
- 启用时间点恢复
- 启用删除保护
- 表名在 AWS AppConfig 的 `dynamodb` 命名空间下注册到运行时配置中
</Fragment>
<Fragment slot="terraform">
Terraform 模块在 `common/terraform` 中创建。示例用法：

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source = "../../common/terraform/src/app/dynamodb/my-table"
}
```

这将配置一个具有以下特性的 DynamoDB 表：
- `pk`（分区键）和 `sk`（排序键），均为 `String` 类型
- 默认 2 个全局二级索引，如 `src/gsi.ts` 中定义
- 按需（`PAY_PER_REQUEST`）计费
- 客户管理的 KMS 加密，具有自动密钥轮换
- 启用时间点恢复
- 启用删除保护
- 表名注册到运行时配置中
</Fragment>
</Infrastructure>

### 授予访问权限

<Snippet name="connection/lambda-dynamodb-access" />

### 删除保护

默认情况下启用删除保护以防止意外删除表。

#### 禁用删除保护

对于预期删除表的环境（例如短期开发或预览堆栈），请禁用它。

<Infrastructure>
<Fragment slot="cdk">

```ts title="packages/infra/src/stacks/application-stack.ts"
import { MyTable } from ':my-scope/common-constructs';

const table = new MyTable(this, 'Table', {
  deletionProtection: false,
});
```
</Fragment>
<Fragment slot="terraform">

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source                      = "../../common/terraform/src/app/dynamodb/my-table"
  deletion_protection_enabled = false
}
```
</Fragment>
</Infrastructure>

### 计费模式

表默认为按需（`PAY_PER_REQUEST`）计费。对于可预测的高吞吐量工作负载，切换到预配置容量。

<Infrastructure>
<Fragment slot="cdk">

```ts title="packages/infra/src/stacks/application-stack.ts"
import { BillingMode } from 'aws-cdk-lib/aws-dynamodb';
import { MyTable } from ':my-scope/common-constructs';

const table = new MyTable(this, 'Table', {
  billingMode: BillingMode.PROVISIONED,
  readCapacity: 5,
  writeCapacity: 5,
});
```
</Fragment>
<Fragment slot="terraform">

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source       = "../../common/terraform/src/app/dynamodb/my-table"
  billing_mode = "PROVISIONED"
}
```
</Fragment>
</Infrastructure>

### 时间点恢复

默认情况下启用时间点恢复，允许您将表恢复到过去 35 天内的任何时间点。

#### 禁用时间点恢复

<Infrastructure>
<Fragment slot="cdk">

```ts title="packages/infra/src/stacks/application-stack.ts"
import { MyTable } from ':my-scope/common-constructs';

const table = new MyTable(this, 'Table', {
  pointInTimeRecoverySpecification: { pointInTimeRecoveryEnabled: false },
});
```
</Fragment>
<Fragment slot="terraform">

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source                         = "../../common/terraform/src/app/dynamodb/my-table"
  point_in_time_recovery_enabled = false
}
```
</Fragment>
</Infrastructure>

### 加密密钥轮换

用于加密表的 KMS 密钥默认启用自动密钥轮换。如果您的安全策略在外部管理轮换，请禁用它。

#### 禁用加密密钥轮换

<Infrastructure>
<Fragment slot="cdk">

```ts title="packages/infra/src/stacks/application-stack.ts"
import { MyTable } from ':my-scope/common-constructs';

const table = new MyTable(this, 'Table', {
  enableKeyRotation: false,
});
```
</Fragment>
<Fragment slot="terraform">

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source              = "../../common/terraform/src/app/dynamodb/my-table"
  enable_key_rotation = false
}
```
</Fragment>
</Infrastructure>
