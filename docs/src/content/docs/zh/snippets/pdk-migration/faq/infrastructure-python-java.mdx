---
title: "Python 或 Java 中的基础架构"
---



import { Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import Snippet from '@components/snippet.astro';
import CreateNxWorkspaceCommand from '@components/create-nx-workspace-command.astro';
import RunGenerator from '@components/run-generator.astro';
import Link from '@components/link.astro';
import NxCommands from '@components/nx-commands.astro';
import Drawer from '@components/drawer.astro';
import InstallCommand from '@components/install-command.astro';

当前版本的 Nx Plugin for AWS 暂不支持使用 Python 和 Java 编写的 PDK 兼容型 CDK 基础设施。

建议的解决方案是：将您的 CDK 基础设施迁移至 TypeScript，或使用我们的生成器并将通用构造包迁移至目标语言。您可以使用生成式 AI 加速此类迁移，例如 [Amazon Q CLI](https://aws.amazon.com/developer/learning/q-developer-cli/)。通过 AI 代理进行迭代迁移，直至生成的 CloudFormation 模板完全一致。

此原则同样适用于 Type Safe API 生成的 Python 或 Java 基础设施——您可以转换通用构造包中的 `rest-api.ts` 构造，并为目标语言实现简单的元数据生成器（参考 [基于 OpenAPI 建模的 API](#apis-modelled-with-openapi) 章节）。

对于 Python 项目，您可以使用 <Link path="/guides/python-project">`py#project` 生成器</Link> 作为基础项目来添加 CDK 代码（迁移 `cdk.json` 文件并添加相关构建目标）。Java 项目可使用 Nx 的 [`@nx/gradle`](https://nx.dev/technologies/java/introduction) 插件，Maven 项目可使用 [`@jnxplus/nx-maven`](https://github.com/gridatek/jnxplus/tree/main/packages/nx-maven)。