---
title: DynamoDB 本地开发
---

`connection` 生成器会配置您项目的 `serve-local` 目标,使其依赖于 DynamoDB 项目的 `serve-local` 目标。当运行 `serve-local` 时,DynamoDB Local 将与您的项目一起自动启动。

`SERVE_LOCAL=true` 环境变量会自动设置,因此 `getDynamoDBClient()` 和 `resolveTableName()` 会连接到本地 DynamoDB Local 实例而不是 AWS。
