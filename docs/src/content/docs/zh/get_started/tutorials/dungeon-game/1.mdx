---
title: "AI地牢游戏"
description: "使用 @aws/nx-plugin 构建人工智能驱动的地牢冒险游戏的演练。"
---



import { Aside, Code, FileTree, Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';
import Link from '@components/link.astro';
import Drawer from '@components/drawer.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';
import InstallCommand from '@components/install-command.astro';
import CreateNxWorkspaceCommand from '@components/create-nx-workspace-command.astro';
import E2EDiff from '@components/e2e-diff.astro';

import dungeonAdventureArchitecturePng from '@assets/dungeon-game-architecture.png'
import dungeonAdventureErPng from '@assets/dungeon-adventure-er.png'
import baselineWebsitePng from '@assets/baseline-website.png'
import baselineGamePng from '@assets/baseline-game.png'
import nxGraphPng from '@assets/nx-graph.png'
import gameSelectPng from '@assets/game-select.png'
import gameConversationPng from '@assets/game-conversation.png'

## 模块 1：Monorepo 初始化

我们将从创建新的 monorepo 开始。在目标目录中运行以下命令：

<CreateNxWorkspaceCommand workspace="dungeon-adventure" />

这将在 `dungeon-adventure` 目录中设置 NX monorepo，之后可以在 vscode 中打开。其结构应如下所示：

<FileTree>
- .nx/
- .vscode/
- node_modules/
- packages/ 子项目存放位置
- .gitignore
- .npmrc
- .prettierignore
- .prettierrc
- nx.json 配置 Nx CLI 和 monorepo 默认设置
- package.json 定义所有 Node 依赖
- pnpm-lock.yaml 或 bun.lock、yarn.lock、package-lock.json（取决于包管理器）
- pnpm-workspace.yaml（如果使用 pnpm）
- README.md
- tsconfig.base.json 所有基于 Node 的子项目继承此配置
- tsconfig.json
</FileTree>

为了开始将 `@aws/nx-plugin` 的组件添加到 monorepo 中，我们需要将其作为开发依赖安装。在 `dungeon-adventure` monorepo 根目录运行：

<InstallCommand dev pkg="@aws/nx-plugin" />

现在我们可以使用 `@aws/nx-plugin` 开始创建不同的子项目。

<Aside type="tip">最佳实践是在运行任何生成器之前将所有未暂存的文件提交到 Git。这允许通过 `git diff` 查看生成器运行后的变更。</Aside>

### 游戏 API

首先创建游戏 API。通过以下步骤创建名为 `GameApi` 的 tRPC API：

<RunGenerator generator="ts#trpc-api" requiredParameters={{apiName:"GameApi"}} noInteractive />

<br />

文件树中将出现新生成的文件。

<Aside>
根目录 `package.json` 现已配置为 `"type": "module"`，这意味着 `@aws/nx-plugin` 提供的所有基于 Node 的子项目默认使用 ESM 模块类型。更多细节请参考 <Link path="guides/typescript-project">ts#project 指南</Link>。
</Aside>

<Drawer title="ts#trpc-api 更新文件" trigger="点击查看文件详情">
以下是 `ts#trpc-api` 生成器生成的所有文件。我们将重点查看文件树中的关键文件：
<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - app/ 应用特定 CDK 构造
          - apis/
            - **game-api.ts** 创建 tRPC API 的 CDK 构造
            - index.ts
            - ...
          - index.ts
        - core/ 通用 CDK 构造
          - api/
            - rest-api.ts API Gateway Rest API 基类
            - trpc-utils.ts tRPC API CDK 工具
            - utils.ts API 构造工具
          - index.ts
          - runtime-config.ts
        - index.ts
      - project.json
      - ...
    - types/ 共享类型
      - src/
        - index.ts
        - runtime-config.ts CDK 和网站共用的接口定义
      - project.json
      - ...
  - game-api/
    - backend/ tRPC 实现代码
      - src/
        - client/ 用于 TS 机器间调用的客户端
          - index.ts
          - sigv4.ts
        - middleware/ Powertools 工具
          - error.ts
          - index.ts
          - logger.ts
          - metrics.ts
          - tracer.ts
        - procedures/ API 过程/路由实现
          - **echo.ts**
        - index.ts
        - init.ts 上下文和中间件初始化
        - local-server.ts 本地运行 tRPC 服务器
        - **router.ts** Lambda 处理程序入口点
      - project.json
      - ...
    - schema/
      - src/
        - procedures/
          - **echo.ts**
        - index.ts
      - project.json
      - ...
- eslint.config.mjs
- vitest.workspace.ts
</FileTree>

查看关键文件示例：

```ts {5,12}
// packages/game-api/backend/src/router.ts
import {
  awsLambdaRequestHandler,
  CreateAWSLambdaContextOptions,
} from '@trpc/server/adapters/aws-lambda';
import { echo } from './procedures/echo.js';
import { t } from './init.js';
import { APIGatewayProxyEvent } from 'aws-lambda';

export const router = t.router;

export const appRouter = router({
  echo,
});

export const handler = awsLambdaRequestHandler({
  router: appRouter,
  createContext: (
    ctx: CreateAWSLambdaContextOptions<APIGatewayProxyEvent>,
  ) => ctx,
  responseMeta: () => ({
    headers: {
      'Access-Control-Allow-Origin': '*',
      'Access-Control-Allow-Methods': '*',
    },
  }),
});

export type AppRouter = typeof appRouter;
```
路由文件定义了 tRPC API 入口点，所有 API 方法在此声明。当前示例包含在 `./procedures/echo.ts` 中实现的 `echo` 方法。

```ts {2-5}
// packages/game-api/backend/src/procedures/echo.ts
import { publicProcedure } from '../init.js';
import {
  EchoInputSchema,
  EchoOutputSchema,
} from ':dungeon-adventure/game-api-schema';

export const echo = publicProcedure
  .input(EchoInputSchema)
  .output(EchoOutputSchema)
  .query((opts) => ({ result: opts.input.message }));
```

该文件实现 `echo` 方法，通过导入 `:dungeon-adventure/game-api-schema` 项目的模式定义实现强类型。该路径是 [tsconfig 路径别名](https://www.typescriptlang.org/tsconfig/paths.html)。

<Aside type="tip">如果 IDE 中看到导入错误，这是因为后端尚未在 tsconfig.json 中设置类型引用。Nx 已配置为在构建/编译时[动态创建](https://nx.dev/nx-api/js/generators/typescript-sync)这些引用。更多信息请参考 <Link path="guides/typescript-project#importing-your-library-code-inother-projects">Typescript 指南</Link>。</Aside>

```ts
// packages/game-api/schema/src/procedures/echo.ts
import { z } from 'zod';

export const EchoInputSchema = z.object({
  message: z.string(),
});

export type IEchoInput = z.TypeOf<typeof EchoInputSchema>;

export const EchoOutputSchema = z.object({
  result: z.string(),
});

export type IEchoOutput = z.TypeOf<typeof EchoOutputSchema>;
```

所有 tRPC 模式使用 [Zod](https://zod.dev/) 定义，并通过 `z.TypeOf` 导出 TypeScript 类型。

```ts
// packages/common/constructs/src/app/apis/game-api.ts
import { Construct } from 'constructs';
import * as url from 'url';
import {
  Code,
  Runtime,
  Function,
  FunctionProps,
  Tracing,
} from 'aws-cdk-lib/aws-lambda';
import {
  AuthorizationType,
  Cors,
  LambdaIntegration,
} from 'aws-cdk-lib/aws-apigateway';
import { Duration, Stack } from 'aws-cdk-lib';
import {
  PolicyDocument,
  PolicyStatement,
  Effect,
  AccountPrincipal,
  AnyPrincipal,
} from 'aws-cdk-lib/aws-iam';
import {
  IntegrationBuilder,
  RestApiIntegration,
} from '../../core/api/utils.js';
import { RestApi } from '../../core/api/rest-api.js';
import { Procedures, routerToOperations } from '../../core/api/trpc-utils.js';
import { AppRouter, appRouter } from ':dungeon-adventure/game-api-backend';

// API 操作名称字符串联合类型
type Operations = Procedures<AppRouter>;

/**
 * GameApi 构造属性
 *
 * @template TIntegrations - 操作名称到集成的映射
 */
export interface GameApiProps<
  TIntegrations extends Record<Operations, RestApiIntegration>,
> {
  /**
   * 操作名称到 API Gateway 集成的映射
   */
  integrations: TIntegrations;
}

/**
 * 专为 GameApi 创建的 API Gateway REST API CDK 构造
 * @template TIntegrations - 操作名称到集成的映射
 */
export class GameApi<
  TIntegrations extends Record<Operations, RestApiIntegration>,
> extends RestApi<Operations, TIntegrations> {
  /**
   * 创建所有操作的默认集成（每个操作对应独立 Lambda 函数）
   *
   * @param scope - CDK 构造作用域
   * @returns 包含默认 Lambda 集成的 IntegrationBuilder
   */
  public static defaultIntegrations = (scope: Construct) => {
    return IntegrationBuilder.rest({
      operations: routerToOperations(appRouter),
      defaultIntegrationOptions: {
        runtime: Runtime.NODEJS_LATEST,
        handler: 'index.handler',
        code: Code.fromAsset(
          url.fileURLToPath(
            new URL(
              '../../../../../../dist/packages/game-api/backend/bundle',
              import.meta.url,
            ),
          ),
        ),
        timeout: Duration.seconds(30),
        tracing: Tracing.ACTIVE,
        environment: {
          AWS_CONNECTION_REUSE_ENABLED: '1',
        },
      } satisfies FunctionProps,
      buildDefaultIntegration: (op, props: FunctionProps) => {
        const handler = new Function(scope, `GameApi${op}Handler`, props);
        return { handler, integration: new LambdaIntegration(handler) };
      },
    });
  };

  constructor(
    scope: Construct,
    id: string,
    props: GameApiProps<TIntegrations>,
  ) {
    super(scope, id, {
      apiName: 'GameApi',
      defaultMethodOptions: {
        authorizationType: AuthorizationType.IAM,
      },
      defaultCorsPreflightOptions: {
        allowOrigins: Cors.ALL_ORIGINS,
        allowMethods: Cors.ALL_METHODS,
      },
      policy: new PolicyDocument({
        statements: [
          // 允许部署账户内的 AWS 凭证调用 API
          new PolicyStatement({
            effect: Effect.ALLOW,
            principals: [new AccountPrincipal(Stack.of(scope).account)],
            actions: ['execute-api:Invoke'],
            resources: ['execute-api:/*'],
          }),
          // 开放 OPTIONS 方法允许浏览器预检请求
          new PolicyStatement({
            effect: Effect.ALLOW,
            principals: [new AnyPrincipal()],
            actions: ['execute-api:Invoke'],
            resources: ['execute-api:/*/OPTIONS/*'],
          }),
        ],
      }),
      operations: routerToOperations(appRouter),
      ...props,
    });
  }
}
```

这是定义 GameApi 的 CDK 构造。其 `defaultIntegrations` 方法为每个 tRPC 过程自动创建 Lambda 函数，指向预打包的 API 实现。这意味着在 `cdk synth` 时不会执行打包操作（与使用 [NodeJsFunction](https://docs.aws.amazon.com/cdk/api/v2/docs/aws-cdk-lib.aws_lambda_nodejs.NodejsFunction.html) 不同），因为打包已在后端项目的构建阶段完成。

</Drawer>

### 故事 API

现在创建故事 API。通过以下步骤创建名为 `StoryApi` 的 Fast API：

<RunGenerator generator="py#fast-api" requiredParameters={{name:"StoryApi"}} noInteractive />

文件树中将出现新生成的文件。
<Drawer title="py#fast-api 更新文件" trigger="点击查看文件详情">
以下是 `py#fast-api` 生成器生成的关键文件：
<FileTree>
- .venv/ 单一虚拟环境
- packages/
  - common/
    - constructs/
      - src/
        - app/ 应用特定 CDK 构造
          - apis/
            - **story-api.ts** 创建 Fast API 的 CDK 构造
            - index.ts 更新导出新构造
      - project.json 添加对 story_api 的构建依赖
    - types/ 共享类型
      - src/
        - **runtime-config.ts** 添加 StoryApi 配置
  - story_api/
    - story_api/ Python 模块
      - init.py 初始化 Powertools 和 FastAPI
      - **main.py** Lambda 入口点包含所有路由
    - tests/
    - .python-version
    - project.json
    - pyproject.toml
- .python-version Python 版本锁定
- pyproject.toml
- uv.lock
</FileTree>

```ts
// packages/common/constructs/src/app/apis/story-api.ts
// （完整代码结构同上文 GameApi 类似）
```

这是定义 StoryApi 的 CDK 构造。其 `defaultIntegrations` 方法为每个 FastAPI 操作自动创建 Lambda 函数，指向预打包的 API 实现。

```diff lang="ts"
// packages/common/types/src/runtime-config.ts
export type ApiUrl = string;
export interface IRuntimeConfig {
  apis: {
    GameApi: ApiUrl;
+    StoryApi: ApiUrl;
  };
}
```

生成器通过 AST 转换更新了 `IRuntimeConfig` 接口，确保前端使用时保持类型安全。

```py
// packages/story_api/story_api/main.py
from .init import app, lambda_handler, tracer

handler = lambda_handler

@app.get("/")
@tracer.capture_method
def read_root():
    return {"Hello": "World"}
```

在此定义所有 API 方法。使用 [Pydantic](https://docs.pydantic.dev/latest/) 可确保输入输出的类型安全。

</Drawer>

### 游戏 UI：网站

现在创建游戏交互界面。通过以下步骤创建名为 `GameUI` 的网站：

<RunGenerator generator="ts#cloudscape-website" requiredParameters={{name:"GameUI"}} noInteractive />

文件树中将出现新生成的文件。

<Drawer title="ts#cloudscape-website 更新文件" trigger="点击查看文件详情">
关键文件结构：
<FileTree>
- packages/
  - game-ui/
    - public/
    - src/
      - components/ 布局组件
      - hooks/ React 钩子
      - routes/ 基于文件的路由
    - index.html
    - vite.config.ts
  - common/ 公共 CDK 构造
</FileTree>

```ts
// packages/common/constructs/src/app/static-websites/game-ui.ts
// （CDK 构造指向预构建的 Vite 包）
```

```tsx
// packages/game-ui/src/main.tsx
// React 入口配置路由和 Cloudscape 组件
```

```tsx
// packages/game-ui/src/routes/welcome/index.tsx
// /welcome 路由组件示例
```

</Drawer>

### 游戏 UI：认证

为 Game UI 配置 Amazon Cognito 认证：

<RunGenerator generator="ts#cloudscape-website#auth" requiredParameters={{cognitoDomain:"game-ui", project:"@dungeon-adventure/game-ui", allowSignup:true}} noInteractive />

<Drawer title="认证更新文件" trigger="点击查看详情">
更新内容包含 Cognito 认证组件和运行时配置注入。

```diff lang="tsx
// packages/game-ui/src/main.tsx 添加认证提供者
```

</Drawer>

### 游戏 UI：连接故事 API

配置 Game UI 连接 Story API：

<RunGenerator generator="api-connection" requiredParameters={{sourceProject:"@dungeon-adventure/game-ui", targetProject:"dungeon_adventure.story_api"}} noInteractive />

<Drawer title="API 连接更新" trigger="查看更新详情">
包含 OpenAPI 客户端生成和 React Query 集成。

```tsx
// packages/game-ui/src/hooks/useStoryApiClient.tsx
// 签名请求钩子实现
```

</Drawer>

### 游戏 UI：连接游戏 API

配置 Game UI 连接 Game API：

<RunGenerator generator="api-connection" requiredParameters={{sourceProject:"@dungeon-adventure/game-ui", targetProject:"@dungeon-adventure/game-api-backend"}} noInteractive />

<Drawer title="tRPC 连接更新" trigger="查看变更">
包含 tRPC 客户端提供者和 React Query 集成。

```tsx
// packages/game-ui/src/main.tsx 注入 tRPC 提供者
```

</Drawer>

### 基础设施

创建 CDK 基础设施项目：

<RunGenerator generator="ts#infra" requiredParameters={{name:"infra"}} noInteractive />

<Drawer title="基础设施更新" trigger="查看基础设施配置">
包含 CDK 应用入口和 CloudFormation Guard 验证配置。

```ts
// packages/infra/src/main.ts CDK 应用入口
```

```ts
// packages/infra/src/stacks/application-stack.ts 基础设施堆栈定义
```

</Drawer>

#### 更新基础设施

更新 `application-stack.ts` 实例化生成的构造：

<E2EDiff before="dungeon-adventure/1/application-stack.ts.original.template" after="dungeon-adventure/1/application-stack.ts.template" lang="ts" />

默认情况下，每个 API 操作映射到独立的 Lambda 函数。

### 构建代码

<Drawer title="Nx 命令" trigger="查看构建说明">
###### 多项目构建
使用 `run-many` 命令跨项目执行构建任务，依赖顺序自动管理。

###### 单项目构建示例
<NxCommands commands={['run @dungeon-adventure/infra:build']} />

###### 依赖可视化
<NxCommands commands={['graph']} />

###### 缓存机制
了解 Nx [缓存策略](https://nx.dev/concepts/how-caching-works) 和跳过缓存的方法。

</Drawer>

<NxCommands commands={['run-many --target build --all']} />

构建完成后，所有产物将位于 monorepo 根目录的 `dist/` 文件夹。恭喜完成核心项目搭建！🎉