---
title: "AI地牢游戏"
description: "使用 @aws/nx-plugin 构建人工智能驱动的地牢冒险游戏的演练。"
---



import { Aside, Code, FileTree, Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';
import Link from '@components/link.astro';
import Drawer from '@components/drawer.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';
import InstallCommand from '@components/install-command.astro';
import CreateNxWorkspaceCommand from '@components/create-nx-workspace-command.astro';
import dungeonAdventureArchitecturePng from '@assets/dungeon-game-architecture.png'
import dungeonAdventureErPng from '@assets/dungeon-adventure-er.png'
import baselineWebsitePng from '@assets/baseline-website.png'
import baselineGamePng from '@assets/baseline-game.png'
import nxGraphPng from '@assets/nx-graph.png'
import gameSelectPng from '@assets/game-select.png'
import gameConversationPng from '@assets/game-conversation.png'

## 模块 1：设置 Monorepo

首先创建一个新的 monorepo。monorepo 是包含多个独立项目的单一代码仓库，项目间具有明确定义的关联关系。

在目标目录中运行以下命令：

<CreateNxWorkspaceCommand workspace="dungeon-adventure" />

这将在 `dungeon-adventure` 目录中设置 NX monorepo。
在 vscode 中打开该目录，您将看到以下文件结构：

<FileTree>
- .nx/
- .vscode/
- node_modules/
- packages/ 子项目存放位置
- .gitignore
- .npmrc
- .prettierignore
- .prettierrc
- nx.json 配置 Nx CLI 和 monorepo 默认设置
- package.json 所有 node 依赖项在此定义
- pnpm-lock.yaml 或 bun.lock, yarn.lock, package-lock.json（取决于包管理器）
- pnpm-workspace.yaml（如使用 pnpm）
- README.md
- tsconfig.base.json 所有基于 node 的子项目继承此配置
- tsconfig.json
</FileTree>

在将 `@aws/nx-plugin` 的组件添加到 monorepo 之前，需先将其安装为开发依赖项。
在 `dungeon-adventure` monorepo 根目录运行以下命令：

<InstallCommand dev pkg="@aws/nx-plugin" />

现在可以开始使用 `@aws/nx-plugin` 创建不同子项目。

<Aside type="tip">建议在运行任何生成器前通过 Git 提交未暂存文件。这样可以通过 `git diff` 查看生成器运行后的变更。</Aside>

### 生成游戏 API

首先创建游戏 API，用于管理游戏状态。
通过以下步骤创建名为 `GameApi` 的 tRPC API：

<RunGenerator generator="ts#trpc-api" requiredParameters={{apiName:"GameApi"}} noInteractive />

<br />

文件树中将出现新文件。

<Drawer title="ts#trpc-api 更新文件" trigger="点击查看文件详情">
以下是 `ts#trpc-api` 生成器生成的所有文件列表。我们将重点查看文件树中标亮的关键文件：
<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - app/ 应用特定 CDK 构造
          - http-apis/
            - **game-api.ts** 创建 tRPC API 的 CDK 构造
            - index.ts
            - ...
          - index.ts
        - core/ 通用 CDK 构造
          - http-api.ts HTTP API 基础构造
          - index.ts
          - runtime-config.ts
        - index.ts
      - project.json
      - ...
    - types/ 共享类型
      - src/
        - index.ts
        - runtime-config.ts CDK 与网站共用的接口定义
      - project.json
      - ...
  - game-api/
    - backend/ tRPC 实现代码
      - src/
        - client/ 供 TS 机器间调用的原生客户端
          - index.ts
          - sigv4.ts
        - middleware/ Powertools 工具集
          - error.ts
          - index.ts
          - logger.ts
          - metrics.ts
          - tracer.ts
        - procedures/ API 过程/路由的具体实现
          - **echo.ts**
        - index.ts
        - init.ts 设置上下文和中间件
        - local-server.ts 本地运行 tRPC 服务器时使用
        - **router.ts** Lambda 处理程序的入口点，定义所有过程
      - project.json
      - ...
    - schema/
      - src/
        - procedures/
          - **echo.ts**
        - index.ts
      - project.json
      - ...
- eslint.config.mjs
- vitest.workspace.ts
</FileTree>

查看这些关键文件：

```ts {5,12}
// packages/game-api/backend/src/router.ts
import {
  awsLambdaRequestHandler,
  CreateAWSLambdaContextOptions,
} from '@trpc/server/adapters/aws-lambda';
import { echo } from './procedures/echo.js';
import { t } from './init.js';
import { APIGatewayProxyEventV2WithIAMAuthorizer } from 'aws-lambda';

export const router = t.router;

export const appRouter = router({
  echo,
});

export const handler = awsLambdaRequestHandler({
  router: appRouter,
  createContext: (
    ctx: CreateAWSLambdaContextOptions<APIGatewayProxyEventV2WithIAMAuthorizer>,
  ) => ctx,
});

export type AppRouter = typeof appRouter;
```
路由定义了 tRPC API 的入口点，用于声明所有 API 方法。
当前已声明 `echo` 方法，其实现位于 `./procedures/echo.ts` 文件。

```ts {2-5}
// packages/game-api/backend/src/procedures/echo.ts
import { publicProcedure } from '../init.js';
import {
  EchoInputSchema,
  EchoOutputSchema,
} from ':dungeon-adventure/game-api-schema';

export const echo = publicProcedure
  .input(EchoInputSchema)
  .output(EchoOutputSchema)
  .query((opts) => ({ result: opts.input.message }));
```

该文件是 `echo` 方法的实现，通过声明输入输出数据结构实现强类型。它从 `:dungeon-adventure/game-api-schema` 项目导入类型定义，该路径是 schema 项目的[类型别名](https://www.typescriptlang.org/tsconfig/paths.html)。

<Aside type="tip">如果在 IDE 中发现导入错误，可能是因为后端尚未在其 `tsconfig.json` 中设置 TypeScript 引用。Nx 已配置为在运行构建/编译时[动态创建](https://nx.dev/nx-api/js/generators/typescript-sync)这些引用，或手动运行 `nx sync` 命令。更多信息请参考 <Link path="guides/typescript-project#importing-your-library-code-in-other-projects">TypeScript 指南</Link>。</Aside>

```ts
// packages/game-api/schema/src/procedures/echo.ts
import { z } from 'zod';

export const EchoInputSchema = z.object({
  message: z.string(),
});

export type IEchoInput = z.TypeOf<typeof EchoInputSchema>;

export const EchoOutputSchema = z.object({
  result: z.string(),
});

export type IEchoOutput = z.TypeOf<typeof EchoOutputSchema>;
```

所有 tRPC 模式定义均使用 [Zod](https://zod.dev/) 定义，并通过 `z.TypeOf` 语法导出为 TypeScript 类型。

```ts
// packages/common/constructs/src/app/http-apis/game-api.ts
import { Construct } from 'constructs';
import * as url from 'url';
import { HttpApi } from '../../core/http-api.js';
import { HttpIamAuthorizer } from 'aws-cdk-lib/aws-apigatewayv2-authorizers';
import { Runtime } from 'aws-cdk-lib/aws-lambda';

export class GameApi extends HttpApi {
  constructor(scope: Construct, id: string) {
    super(scope, id, {
      defaultAuthorizer: new HttpIamAuthorizer(),
      apiName: 'GameApi',
      runtime: Runtime.NODEJS_LATEST,
      handler: 'index.handler',
      handlerFilePath: url.fileURLToPath(
        new URL(
          '../../../../../../dist/packages/game-api/backend/bundle',
          import.meta.url,
        ),
      ),
    });
  }
}
```

这是定义 GameAPI 的 CDK 构造。它将处理程序文件路径配置为 tRPC 后端实现的生成包，因此在 `cdk synth` 时不会进行打包操作（与使用 [NodeJsFunction](https://docs.aws.amazon.com/cdk/api/v2/docs/aws-cdk-lib.aws_lambda_nodejs.NodejsFunction.html) 不同），因为打包已在后端项目的构建目标中完成。

</Drawer>

<Aside>
根目录 `package.json` 现在配置了 `"type": "module"`，意味着由 `@aws/nx-plugin` 提供的所有基于 node 的子项目默认使用 ESM 模块类型。更多 TypeScript 项目细节请参考 <Link path="guides/typescript-project">ts#project 指南</Link>。
</Aside>

### 生成故事 API

接下来使用 FastAPI 生成器创建故事 API。该 API 用于通过 Amazon Bedrock 生成流式响应的故事内容。
通过以下步骤创建名为 `StoryApi` 的 Fast API：

<RunGenerator generator="py#fast-api" requiredParameters={{name:"StoryApi"}} noInteractive />

文件树中将出现新文件。

<Drawer title="py#fast-api 更新文件" trigger="点击查看文件详情">
以下是 `py#fast-api` 生成器生成的文件列表。我们将重点查看标亮的关键文件：
<FileTree>
- .venv/ 单一虚拟环境
- packages/
  - common/
    - constructs/
      - src/
        - app/ 应用特定 CDK 构造
          - http-apis/
            - **story-api.ts** 创建 Fast API 的 CDK 构造
            - index.ts 更新以导出新 story-api
      - project.json 添加对 story_api 的构建依赖
    - types/ 共享类型
      - src/
        - **runtime-config.ts** 更新添加 StoryApi
  - story_api/
    - story_api/ Python 模块
      - init.py 配置 Powertools、FastAPI 和中间件
      - **main.py** 包含所有路由的 Lambda 入口点
    - tests/
    - .python-version
    - project.json
    - pyproject.toml
    - project.json
- .python-version 固定的 uv python 版本
- pyproject.toml
- uv.lock
</FileTree>

```ts
// packages/common/constructs/src/app/http-apis/story-api.ts
import { Construct } from 'constructs';
import * as url from 'url';
import { HttpApi } from '../../core/http-api.js';
import { HttpIamAuthorizer } from 'aws-cdk-lib/aws-apigatewayv2-authorizers';
import { Runtime } from 'aws-cdk-lib/aws-lambda';

export class StoryApi extends HttpApi {
  constructor(scope: Construct, id: string) {
    super(scope, id, {
      defaultAuthorizer: new HttpIamAuthorizer(),
      apiName: 'StoryApi',
      runtime: Runtime.PYTHON_3_12,
      handler: 'story_api.main.handler',
      handlerFilePath: url.fileURLToPath(
        new URL(
          '../../../../../../dist/packages/story_api/bundle',
          import.meta.url,
        ),
      ),
    });
  }
}
```

这是定义 StoryApi 的 CDK 构造。它将处理程序文件路径配置为 Fast API 后端实现的生成包，因此在 `cdk synth` 时不会进行打包操作（与 [PythonFunction](https://docs.aws.amazon.com/cdk/api/v2/docs/@aws-cdk_aws-lambda-python-alpha.PythonFunction.html) 不同），因为打包已在后端项目的构建目标中完成。

```diff lang="ts"
// packages/common/types/src/runtime-config.ts
export type ApiUrl = string;
// eslint-disable-next-line @typescript-eslint/no-empty-object-type, @typescript-eslint/no-empty-interface
export interface IRuntimeConfig {
  httpApis: {
    GameApi: ApiUrl;
+    StoryApi: ApiUrl;
  };
}
```

这是生成器执行 AST 转换的示例，保留现有代码并进行更新。可以看到 `StoryApi` 被添加到 `IRuntimeConfig` 定义中，这意味着当该配置被前端使用时，将强制执行类型安全！

```py
// packages/story_api/story_api/main.py
from .init import app, lambda_handler, tracer

handler = lambda_handler

@app.get("/")
@tracer.capture_method
def read_root():
    return {"Hello": "World"}
```

此处定义所有 API 方法。当前已定义映射到 `GET /` 路由的 `read_root` 方法。可以使用 [Pydantic](https://docs.pydantic.dev/latest/) 声明方法输入输出以确保类型安全。

</Drawer>

### 添加游戏 UI 界面

使用 CloudScape 网站生成器创建游戏交互界面。
通过以下步骤创建名为 `GameUI` 的网站：

<RunGenerator generator="ts#cloudscape-website" requiredParameters={{name:"GameUI"}} noInteractive />

文件树中将出现新文件。

<Drawer title="ts#cloudscape-website 更新文件" trigger="点击查看文件详情">
以下是 `ts#cloudscape-website` 生成器生成的文件列表。查看标亮的关键文件：
<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - app/ 应用特定 CDK 构造
          - static-websites/
            - **game-ui.ts** 创建游戏 UI 的 CDK 构造
        - core/
          - static-website.ts 通用静态网站构造
  - game-ui/
    - public/
    - src/
      - components/
        - AppLayout/
          - index.ts 整体页面布局：页头、页脚、侧边栏等
          - navitems.ts 侧边栏导航项
      - hooks/
        - useAppLayout.tsx 动态设置通知、页面样式等
      - routes/ @tanstack/react-router 文件路由
        - index.tsx 根 '/' 页面重定向到 '/welcome'
        - __root.tsx 所有页面的基础组件
        - welcome/
          - **index.tsx**
        - config.ts
        - **main.tsx** React 入口点
        - routeTree.gen.ts 由 @tanstack/react-router 自动更新
        - styles.css
    - index.html
    - project.json
    - vite.config.ts
    - ...
</FileTree>

```ts
// packages/common/constructs/src/app/static-websites/game-ui.ts
import * as url from 'url';
import { Construct } from 'constructs';
import { StaticWebsite } from '../../core/index.js';

export class GameUI extends StaticWebsite {
  constructor(scope: Construct, id: string) {
    super(scope, id, {
      websiteFilePath: url.fileURLToPath(
        new URL(
          '../../../../../../dist/packages/game-ui/bundle',
          import.meta.url,
        ),
      ),
    });
  }
}
```

这是定义 GameUI 的 CDK 构造。已配置文件路径指向基于 Vite 构建的 UI 生成包，这意味着在构建时，打包操作在 game-ui 项目的构建目标中完成，其输出在此处使用。

```tsx
// packages/game-ui/src/main.tsx
import React from 'react';
import { createRoot } from 'react-dom/client';
import { I18nProvider } from '@cloudscape-design/components/i18n';
import messages from '@cloudscape-design/components/i18n/messages/all.en';
import { RouterProvider, createRouter } from '@tanstack/react-router';
import { routeTree } from './routeTree.gen';

import '@cloudscape-design/global-styles/index.css';

const router = createRouter({ routeTree });

// 注册路由实例以实现类型安全
declare module '@tanstack/react-router' {
  interface Register {
    router: typeof router;
  }
}

const root = document.getElementById('root');
root &&
  createRoot(root).render(
    <React.StrictMode>
      <I18nProvider locale="en" messages={[messages]}>
        <RouterProvider router={router} />
      </I18nProvider>
    </React.StrictMode>,
  );
```

这是 React 的挂载入口点。当前配置了 [`@tanstack/react-router` 文件路由](https://tanstack.com/router/v1/docs/framework/react/routing/file-based-routing)，只要开发服务器运行，在 `routes` 文件夹中创建文件时，`@tanstack/react-router` 会自动生成样板文件并更新 `routeTree.gen.ts` 文件。该文件以类型安全方式维护所有路由，因此使用 `<Link>` 时，`to` 选项仅显示有效路由。更多信息请参考 [`@tanstack/react-router` 文档](https://tanstack.com/router/v1/docs/framework/react/quick-start)。

```tsx
// packages/game-ui/src/routes/welcome/index.tsx
import {
  ContentLayout,
  Header,
  SpaceBetween,
  Container,
} from '@cloudscape-design/components';
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/welcome/')({
  component: RouteComponent,
});

function RouteComponent() {
  return (
    <ContentLayout header={<Header>Welcome</Header>}>
      <SpaceBetween size="l">
        <Container>欢迎使用新的 Cloudscape 网站！</Container>
      </SpaceBetween>
    </ContentLayout>
  );
}
```

访问 `/welcome` 路由时将渲染此组件。`@tanstack/react-router` 会在创建/移动文件时自动管理路由（需开发服务器运行）。本教程后续章节将展示此功能。

</Drawer>

### 添加游戏 UI 身份验证

通过 Amazon Cognito 为游戏 UI 配置身份验证访问：

<RunGenerator generator="ts#cloudscape-website#auth" requiredParameters={{cognitoDomain:"game-ui", project:"@dungeon-adventure/game-ui", allowSignup:true}} noInteractive />

文件树中将出现新文件。

<Drawer title="ts#cloudscape-website#auth 更新文件" trigger="点击查看文件详情">
以下是 `ts#cloudscape-website#auth` 生成器生成/更新的文件列表。查看标亮的关键文件：
<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - core/
          - user-identity.ts 创建用户/身份池的 CDK 构造
    - types/
      - src/
        - runtime-config.ts 更新添加 cognitoProps
  - game-ui/
    - src/
      - components/
        - AppLayout/
          - index.tsx 在页头添加登录用户/注销功能
        - CognitoAuth/
          - index.ts 管理 Cognito 登录
        - RuntimeConfig/
          - index.tsx 获取 `runtime-config.json` 并通过上下文传递给子组件
      - hooks/
        - useRuntimeConfig.tsx
      - **main.tsx** 更新添加 Cognito
</FileTree>

```diff lang="tsx"
// packages/game-ui/src/main.tsx
+import CognitoAuth from './components/CognitoAuth';
+import RuntimeConfigProvider from './components/RuntimeConfig';
import React from 'react';
import { createRoot } from 'react-dom/client';
import { I18nProvider } from '@cloudscape-design/components/i18n';
import messages from '@cloudscape-design/components/i18n/messages/all.en';
import { RouterProvider, createRouter } from '@tanstack/react-router';
import { routeTree } from './routeTree.gen';
import '@cloudscape-design/global-styles/index.css';
const router = createRouter({ routeTree });
// 注册路由实例以实现类型安全
declare module '@tanstack/react-router' {
  interface Register {
    router: typeof router;
  }
}
const root = document.getElementById('root');
root &&
  createRoot(root).render(
    <React.StrictMode>
      <I18nProvider locale="en" messages={[messages]}>
+        <RuntimeConfigProvider>
+          <CognitoAuth>
            <RouterProvider router={router} />
+          </CognitoAuth>
+        </RuntimeConfigProvider>
      </I18nProvider>
    </React.StrictMode>,
  );
```

通过 AST 转换在 `main.tsx` 中添加了 `RuntimeConfigProvider` 和 `CognitoAuth` 组件。这使得 `CognitoAuth` 组件可以通过获取包含 Cognito 连接配置的 `runtime-config.json` 进行身份验证，确保后端调用指向正确目标。

</Drawer>

### 配置游戏 UI 连接故事 API

配置游戏 UI 连接之前创建的故事 API：

<RunGenerator generator="api-connection" requiredParameters={{sourceProject:"@dungeon-adventure/game-ui", targetProject:"dungeon_adventure.story_api"}} noInteractive />

文件树中将出现新文件。

<Drawer title="UI -> FastAPI api-connection 更新文件" trigger="点击查看文件详情">
以下是 `api-connection` 生成器生成/更新的文件列表。查看标亮的关键文件：
<FileTree>
- packages/
  - game-ui/
    - src/
      - hooks/
        - useSigV4.tsx StoryApi 用于签名请求
        - useStoryApiClient.tsx 构造 StoryApi 客户端的钩子
        - useStoryApi.tsx 使用 TanStack Query 与 StoryApi 交互的钩子
      - components/
        - QueryClientProvider.tsx TanStack Query 客户端提供者
        - StoryApiProvider.tsx StoryApi TanStack Query 钩子的提供者
      - main.tsx 注入 QueryClientProvider 和 StoryApiProvider
    - .gitignore 忽略生成的客户端文件
    - project.json 更新添加生成 OpenAPI 钩子的目标
    - ...
  - story_api/
    - scripts/
      - generate_open_api.py
    - project.json 更新生成 openapi.json 文件
</FileTree>

```tsx {1,12-15}
// packages/game-ui/src/hooks/useStoryApiClient.tsx
import { StoryApi } from '../generated/story-api/client.gen';
import { useSigV4 } from './useSigV4';
import { useRuntimeConfig } from './useRuntimeConfig';
import { useMemo } from 'react';

export const useStoryApi = (): StoryApi => {
  const runtimeConfig = useRuntimeConfig();
  const apiUrl = runtimeConfig.httpApis.StoryApi;
  const sigv4Client = useSigV4();
  return useMemo(
    () =>
      new StoryApi({
        url: apiUrl,
        fetch: sigv4Client,
      }),
    [apiUrl, sigv4Client],
  );
};
```

使用此钩子可向 `StoryApi` 发起认证 API 请求。它使用构建时生成的 `StoryApi`，在代码构建前 IDE 会显示错误。有关客户端生成及 API 使用的更多信息，请参考 <Link path="guides/api-connection/react-fastapi">React 连接 FastAPI 指南</Link>。

```tsx
// packages/game-ui/src/components/StoryApiProvider.tsx
import { createContext, FC, PropsWithChildren, useMemo } from 'react';
import { useStoryApiClient } from '../hooks/useStoryApiClient';
import { StoryApiOptionsProxy } from '../generated/story-api/options-proxy.gen';

export const StoryApiContext = createContext<StoryApiOptionsProxy | undefined>(
  undefined,
);

export const StoryApiProvider: FC<PropsWithChildren> = ({ children }) => {
  const client = useStoryApiClient();
  const optionsProxy = useMemo(
    () => new StoryApiOptionsProxy({ client }),
    [client],
  );

  return (
    <StoryApiContext.Provider value={optionsProxy}>
      {children}
    </StoryApiContext.Provider>
  );
};

export default StoryApiProvider;
```

上述提供者组件使用 `useStoryApiClient` 钩子实例化 `StoryApiOptionsProxy`，用于构建 TanStack Query 钩子的选项。可使用对应的 `useStoryApi` 钩子访问此选项代理，以与 tRPC API 一致的方式与 FastAPI 交互。

由于 `useStoryApiClient` 为流式 API 提供了异步迭代器，本教程将直接使用原生客户端。

<Aside type="caution">
`src/generated/story-api/*.gen.ts` 文件请勿手动修改，因为它们会在每次 API 构建时重新生成。
</Aside>

</Drawer>

### 配置游戏 UI 连接游戏 API

配置游戏 UI 连接游戏 API：

<RunGenerator generator="api-connection" requiredParameters={{sourceProject:"@dungeon-adventure/game-ui", targetProject:"@dungeon-adventure/game-api-backend"}} noInteractive />

文件树中将出现变更。

<Drawer title="UI -> tRPC api-connection 更新文件" trigger="点击查看文件详情">
以下是 `api-connection` 生成器生成/更新的文件列表。查看标亮的关键文件：
<FileTree>
- packages/
  - game-ui/
    - src/
      - components/
        - TrpcClients/
          - index.tsx
          - TrpcApis.tsx 所有配置的 tRPC API
          - TrpcClientProviders.tsx 为每个 tRPC API 创建客户端提供者
          - TrpcProvider.tsx
      - hooks/
        - **useGameApi.tsx** 调用 GameApi 的钩子
      - **main.tsx** 注入 trpc 客户端提供者
- package.json
</FileTree>

```tsx
// packages/game-ui/src/hooks/useGameApi.tsx
import { TrpcApis } from '../components/TrpcClients';

export const useGameApi = () => TrpcApis.GameApi.useTRPC();
```

此钩子使用 tRPC 最新的 [React Query 集成](https://trpc.io/blog/introducing-tanstack-react-query-client)，允许用户直接与 `@tanstack/react-query` 交互而无需额外抽象层。调用 tRPC API 的示例请参考 <Link path="guides/api-connection/react-trpc#using-the-generated-code">使用 tRPC 钩子指南</Link>。

<Aside>
`useGameApi` 钩子与 `useStoryApi` 不同，由于 tRPC 使用 [Typescript 类型推断](https://trpc.io/docs/concepts)，无需构建即可实时反映后端变更。
</Aside>

```diff lang="tsx"
// packages/game-ui/src/main.tsx
+import TrpcClientProviders from './components/TrpcClients';
+import QueryClientProvider from './components/QueryClientProvider';
import CognitoAuth from './components/CognitoAuth';
import RuntimeConfigProvider from './components/RuntimeConfig';
import React from 'react';
import { createRoot } from 'react-dom/client';
import { I18nProvider } from '@cloudscape-design/components/i18n';
import messages from '@cloudscape-design/components/i18n/messages/all.en';
import { RouterProvider, createRouter } from '@tanstack/react-router';
import { routeTree } from './routeTree.gen';
import '@cloudscape-design/global-styles/index.css';
const router = createRouter({ routeTree });
// 注册路由实例以实现类型安全
declare module '@tanstack/react-router' {
  interface Register {
    router: typeof router;
  }
}
const root = document.getElementById('root');
root &&
  createRoot(root).render(
    <React.StrictMode>
      <I18nProvider locale="en" messages={[messages]}>
        <RuntimeConfigProvider>
          <CognitoAuth>
+            <QueryClientProvider>
+              <TrpcClientProviders>
                <RouterProvider router={router} />
+              </TrpcClientProviders>
+            </QueryClientProvider>
          </CognitoAuth>
        </RuntimeConfigProvider>
      </I18nProvider>
    </React.StrictMode>,
  );
```

通过 AST 转换在 `main.tsx` 中注入了 tRPC 提供者。

</Drawer>

### 创建游戏 UI CDK 基础设施

创建 CDK 基础设施作为最终子项目：

<RunGenerator generator="ts#infra" requiredParameters={{name:"infra"}} noInteractive />

文件树中将出现变更。

<Drawer title="ts#infra 更新文件" trigger="点击查看文件详情">
以下是 `ts#infra` 生成器生成/更新的文件列表。查看标亮的关键文件：
<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - core/
          - cfn-guard-rules/
            - *.guard
          - cfn-guard.ts
          - index.ts
  - infra
    - src/
      - stacks/
        - **application-stack.ts** 定义 CDK 资源
      - index.ts
      - **main.ts** 定义所有堆栈的入口点
    - cdk.json
    - project.json
    - ...
  - package.json
  - tsconfig.json 添加引用
  - tsconfig.base.json 添加别名
</FileTree>

```ts
// packages/infra/src/main.ts
import { ApplicationStack } from './stacks/application-stack.js';
import {
  App,
  CfnGuardValidator,
  RuleSet,
} from ':dungeon-adventure/common-constructs';

const app = new App({
  policyValidationBeta1: [new CfnGuardValidator(RuleSet.AWS_PROTOTYPING)],
});

// 部署自有沙箱环境（需 CLI 凭证）
new ApplicationStack(app, 'dungeon-adventure-infra-sandbox', {
  env: {
    account: process.env.CDK_DEFAULT_ACCOUNT,
    region: process.env.CDK_DEFAULT_REGION,
  },
  crossRegionReferences: true,
});

app.synth();
```

这是 CDK 应用的入口点。配置使用 [`cfn-guard`](https://github.com/cdklabs/cdk-validator-cfnguard) 根据规则集进行基础设施验证，该验证在合成后执行。

<Aside type="tip">
如需在特定构造上禁用规则，可通过以下方式实现：

###### 在构造上禁用规则

```typescript
import { suppressRule } from ':dungeon-adventure/common-constructs';

...
// 在指定构造上禁用 RULE_NAME
suppressRule(construct, 'RULE_NAME');
```

###### 在子构造上禁用规则

```typescript
import { suppressRule } from ':dungeon-adventure/common-constructs';

...
// 如果构造是 Bucket 实例，则禁用 RULE_NAME
suppressRule(construct, 'RULE_NAME', (construct) => construct instanceof Bucket);
```
</Aside>

```ts
// packages/infra/src/stacks/application-stack.ts
import * as cdk from 'aws-cdk-lib';
import { Construct } from 'constructs';

export class ApplicationStack extends cdk.Stack {
  constructor(scope: Construct, id: string, props?: cdk.StackProps) {
    super(scope, id, props);

    // 在此定义堆栈代码
  }
}
```

此处实例化 CDK 构造以构建地下城冒险游戏。

</Drawer>

#### 更新 CDK 基础设施

实例化已生成的构造，更新 `packages/infra/src/stacks/application-stack.ts`：

```diff lang="ts"
+import {
+  GameApi,
+  GameUI,
+  StoryApi,
+  UserIdentity,
+} from ':dungeon-adventure/common-constructs';
import * as cdk from 'aws-cdk-lib';
import { Construct } from 'constructs';

export class ApplicationStack extends cdk.Stack {
  constructor(scope: Construct, id: string, props?: cdk.StackProps) {
    super(scope, id, props);

-    // 在此定义堆栈代码
+    const userIdentity = new UserIdentity(this, 'UserIdentity');
+
+    const gameApi = new GameApi(this, 'GameApi');
+    const storyApi = new StoryApi(this, 'StoryApi');
+
+    // 授予认证角色调用 API 的权限
+    [storyApi, gameApi].forEach((api) =>
+      api.grantInvokeAccess(userIdentity.identityPool.authenticatedRole),
+    );
+
+    // 最后实例化以确保 runtime-config.json 自动配置
+    new GameUI(this, 'GameUI');
  }
}

```

### 构建代码

<Drawer title="Nx 命令" trigger="现在开始首次代码构建">
###### 单目标 vs 多目标

`run-many` 命令会在多个子项目上运行目标（`--all` 选择所有目标），并确保依赖项按正确顺序执行。

也可直接对项目运行目标。例如构建 `@dungeon-adventure/infra` 项目：

<NxCommands commands={['run @dungeon-adventure/infra:build']} />
###### 可视化依赖关系

可通过以下命令可视化依赖：

<NxCommands commands={['graph']} />
<br/>

<Image src={nxGraphPng} alt="nx-graph.png" width="800" height="600" />

###### 缓存

Nx 依赖[缓存](https://nx.dev/concepts/how-caching-works)重用之前构建产物以加速开发。需进行配置才能正常工作。如需跳过缓存构建，可添加 `--skip-nx-cache` 参数：

<NxCommands commands={['run @dungeon-adventure/infra:build --skip-nx-cache']} />
如需清除缓存（存储在 `.nx` 文件夹），运行：

<NxCommands commands={['reset']} />

</Drawer>

<NxCommands commands={['run-many --target build --all']} />

将出现以下提示：

```bash
 NX   The workspace is out of sync

[@nx/js:typescript-sync]: Some TypeScript configuration files are missing project references to the projects they depend on or contain outdated project references.

This will result in an error in CI.

? Would you like to sync the identified changes to get your workspace up to date? …
Yes, sync the changes and run the tasks
No, run the tasks without syncing the changes
```

该提示表明 NX 检测到可自动更新的文件。此处指缺少 Typescript 项目引用的 `tsconfig.json` 文件。选择 **Yes, sync the changes and run the tasks** 继续。同步生成器会自动添加缺失的 Typescript 引用，IDE 导入错误将自动解决！

<Aside type="tip">
如遇 lint 错误，可运行以下命令自动修复：

<NxCommands commands={['run-many --target lint --configuration=fix --all']} />
</Aside>

<Aside type="caution" title="Windows 构建失败">
<Drawer trigger="Windows 用户如遇构建错误，请点击此处。" title="Windows 构建失败">
如果 `@dungeon-adventure/infra` 项目构建/合成失败，这是因为 `cfn-guard` 工具库暂不支持 Windows。可通过修改 `packages/infra/src/main.ts` 文件禁用 `cfn-guard`：

```diff lang="ts"
// packages/infra/src/main.ts
import { ApplicationStack } from './stacks/application-stack.js';
import {
   App,
-  CfnGuardValidator,
-  RuleSet,
} from ':dungeon-adventure/common-constructs';
-
-const app = new App({
-  policyValidationBeta1: [new CfnGuardValidator(RuleSet.AWS_PROTOTYPING)],
-});
+const app = new App();

// 部署自有沙箱环境（需 CLI 凭证）
new ApplicationStack(app, 'dungeon-adventure-infra-sandbox', {
  env: {
    account: process.env.CDK_DEFAULT_ACCOUNT,
    region: process.env.CDK_DEFAULT_REGION,
  },
  crossRegionReferences: true,
});

app.synth();
```
</Drawer>
</Aside>

所有构建产物现在位于 monorepo 根目录的 `dist/` 文件夹中。使用 `@aws/nx-plugin` 生成的项目遵循此标准实践，避免生成文件散落各处。清理时可直接删除 `dist/` 文件夹。

恭喜！您已完成地下城冒险游戏核心功能所需的所有子项目创建。🎉🎉🎉