---
title: Graph Builder
description: 可视化设计您的工作空间，然后复制生成它的命令。
# The builder needs the full content width, which the table of contents column
# would otherwise take.
tableOfContents: false
---
import Link from '@components/link.astro';
import GraphBuilder from '@components/graph-builder.astro';

勾画出您的工作空间所需的项目以及它们之间的连接，构建器会生成精确搭建您所绘制内容的命令。复制它们，运行它们，您就拥有了一个可工作的工作空间。

从调色板拖动组件到画布上，从一个组件的右边缘拖动到另一个组件来连接它们，并在右侧编辑每个组件的属性。底部生成的命令会随着每次更改而更新——点击 **Copy** 来获取整个脚本。

<GraphBuilder />

## 工作原理

画布上的每个组件都映射到一个生成器，每个连接都映射到<Link path="guides/connection">连接生成器</Link>。命令按依赖顺序发出：首先是工作空间，然后是项目，然后是它们托管的组件，最后是将它们连接在一起的连接。

最后会添加一个 `infra` 项目——<Link path="guides/typescript-infrastructure">`ts#infra`</Link> 或 <Link path="guides/terraform-project">`terraform#project`</Link>，与您的 IaC 选择相匹配。每个生成的项目都会为它提供要实例化的构造，因此这就是您要部署的内容。因为它拥有该名称，所以画布上的任何组件都不能被称为 `infra`。

拖动画布背景来平移，拖动组件来重新定位它。按住 Shift 键单击可选择多个组件并将它们作为一组移动。单击连接以选中它，然后按 <kbd>Delete</kbd> 键将其删除（或使用连接本身上的 ✕）。

调色板列出了可以参与连接的每个项目和组件类型，您只能绘制插件支持的连接——因此验证通过的图就是可以搭建的图。

有两条规则值得了解：

- **组件共享宿主项目。** 代理和 MCP 服务器是添加_到_项目中的，而不是它们自己的项目。给它们相同的宿主项目名称以将它们放在一个项目中，或者使用不同的名称为每个创建一个项目。
- **某些连接会约束其端点。** 例如，将一个代理连接到另一个代理需要目标使用 `a2a` 协议。当属性设置会破坏您绘制的连接时，构建器会在组件和连接旁边提示。

:::tip[更喜欢用 AI 构建？]
该插件附带了一个 MCP 服务器，因此您的编码代理可以为您搭建和连接项目。请参阅<Link path="get_started/building-with-ai">使用 AI 构建</Link>。
:::
