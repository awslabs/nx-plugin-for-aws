---
title: FastAPI a Base de Datos Relacional
description: Conectar una FastAPI a una Base de Datos Relacional de Python
when:
  sourceType: py#fast-api
  targetType: py#rdb
---
import { FileTree } from '@astrojs/starlight/components';
import Link from '@components/link.astro';
import RunGenerator from '@components/run-generator.astro';
import GeneratorParameters from '@components/generator-parameters.astro';
import NxCommands from '@components/nx-commands.astro';
import Infrastructure from '@components/infrastructure.astro';

El generador `connection` conecta una <Link path="guides/fastapi">FastAPI</Link> a un proyecto de <Link path="guides/py-rdb">Base de Datos Relacional de Python</Link>, inyectando una sesión SQLModel tipada en tus manejadores de rutas a través de una dependencia de FastAPI.

## Requisitos previos

Antes de usar este generador, asegúrate de tener:

1. Un proyecto <Link path="guides/fastapi">`py#fast-api`</Link>
2. Un proyecto <Link path="guides/py-rdb">`py#rdb`</Link>

## Uso

### Ejecutar el Generador

<RunGenerator generator="connection" />

Selecciona tu proyecto FastAPI como origen y tu proyecto de base de datos relacional como destino.

### Opciones

<GeneratorParameters generator="connection" />

## Salida del Generador

El generador modifica tu proyecto FastAPI:

<FileTree>

- packages/my_api/my_api
  - dependencies
    - my-db.py Dependencia `SessionDep` de FastAPI para la sesión de base de datos

</FileTree>

Además, el objetivo `dev` de FastAPI se actualiza para depender del objetivo `dev` de la base de datos, de modo que ambos se inicien juntos durante el desarrollo local.

## Cómo Funciona

El archivo de dependencia generado exporta un alias de tipo `SessionDep` que inyecta una `Session` de SQLModel a través del `Depends` de FastAPI:

```python title="packages/my_api/my_api/dependencies/my-db.py"
from collections.abc import Generator
from typing import Annotated

from fastapi import Depends
from sqlmodel import Session

from my_scope.my_db.connection import get_session

def get_session_dep() -> Generator[Session]:
    yield from get_session()


SessionDep = Annotated[Session, Depends(get_session_dep)]
```

## Usar la Base de Datos en Manejadores de Rutas

Importa `SessionDep` y decláralo como parámetro en tus manejadores de rutas:

```python title="packages/my_api/my_api/api.py"
from sqlmodel import select
from my_api.dependencies.my_db import SessionDep
from my_scope.my_db.models.example import ExampleModel

@app.get("/examples")
def list_examples(session: SessionDep):
    return session.exec(select(ExampleModel)).all()

@app.post("/examples")
def create_example(name: str, session: SessionDep):
    item = ExampleModel(name=name)
    session.add(item)
    session.commit()
    session.refresh(item)
    return item
```

FastAPI abre automáticamente una nueva sesión por solicitud y la cierra cuando el manejador retorna.

## Infraestructura

Para permitir que la función Lambda de FastAPI se conecte a la base de datos en tiempo de ejecución, debe desplegarse en la misma VPC que la base de datos y se le debe otorgar acceso de red e IAM.

<Infrastructure>
<Fragment slot="cdk">

En tu stack de aplicación, despliega la API en la misma VPC que la base de datos, luego llama a `allowDefaultPortFrom` y `grantConnect` para abrir la ruta de red y otorgar el permiso IAM `rds-db:connect` al manejador Lambda:

```ts title="packages/infra/src/stacks/application-stack.ts"
import { MyDatabase } from ':my-scope/common-constructs';

const db = new MyDatabase(this, 'Db', { vpc, ... });

const api = new MyApi(this, 'Api', {
  integrations: MyApi.defaultIntegrations(this)
    .withDefaultOptions({
      vpc,
      vpcSubnets: { subnetType: SubnetType.PRIVATE_WITH_EGRESS },
    })
    .build(),
});

Object.entries(api.integrations).forEach(([operation, integration]) => {
  db.allowDefaultPortFrom(integration.handler, `Allow ${operation} to connect to the database`);
  db.grantConnect(integration.handler);
});
```

Despliega las funciones Lambda de la API en una **subred privada con salida**, no en una subred privada aislada. En tiempo de ejecución, `get_session()` recupera la configuración de la base de datos desde AWS AppConfig, que es un endpoint de servicio público de AWS que requiere acceso a internet saliente.

:::note
Este ejemplo otorga acceso a todos los manejadores de tu API, pero si solo algunos manejadores necesitan acceso es mejor configurarlos individualmente.
:::

</Fragment>
<Fragment slot="terraform">

Pasa las salidas del módulo de base de datos a tu módulo de API para que pueda alcanzar la base de datos y leer su configuración en tiempo de ejecución:

```hcl title="packages/infra/src/main.tf"
module "my_database" {
  source              = "../../common/terraform/src/app/dbs/my-database"
  vpc_id              = module.vpc.vpc_id
  database_subnet_ids = module.vpc.private_isolated_subnet_ids
  lambda_subnet_ids   = module.vpc.private_subnet_ids
}

module "api" {
  source             = "..."
  vpc_id             = module.vpc.vpc_id
  private_subnet_ids = module.vpc.private_subnet_ids

  appconfig_application_id     = module.my_database.appconfig_application_id
  database_cluster_resource_id = module.my_database.cluster_resource_id
  database_runtime_user        = module.my_database.database_runtime_user
  database_security_group_id   = module.my_database.security_group_id
  database_port                = module.my_database.cluster_port

  environment_variables = {
    RUNTIME_CONFIG_APP_ID = module.my_database.appconfig_application_id
  }
}
```

Despliega las funciones Lambda de la API en **subredes privadas con salida**, no en subredes privadas aisladas. Asegúrate de que el rol Lambda tenga el permiso `rds-db:connect` y que su grupo de seguridad pueda alcanzar el grupo de seguridad de la base de datos en el puerto de la base de datos.

</Fragment>
</Infrastructure>

### Requisitos SSL al Conectar sin RDS Proxy

Cuando las funciones Lambda de FastAPI se conectan directamente al clúster Aurora (sin RDS Proxy), adjunta la capa Lambda `RdsCaLayer` (CDK) o el módulo Terraform `ca-layer`. La capa CDK usa empaquetado Docker para agregar el paquete CA de Amazon RDS, y la capa desplegada lo proporciona en `/opt/global-bundle.pem`.

Adjunta la capa del paquete CA a las integraciones Lambda de FastAPI generadas:

<Infrastructure>
<Fragment slot="cdk">

```ts title="packages/infra/src/stacks/application-stack.ts"
import { RdsCaLayer } from ':my-scope/common-constructs';

const rdsCaLayer = new RdsCaLayer(this, 'RdsCaLayer');

const api = new MyApi(this, 'Api', {
  integrations: MyApi.defaultIntegrations(this)
    .withDefaultOptions({
      layers: [rdsCaLayer.layer],
    })
    .build(),
});
```

</Fragment>
<Fragment slot="terraform">

```hcl title="packages/infra/src/main.tf"
module "rds_ca_layer" {
  source = "../../common/terraform/src/core/rdb/ca-layer"
}

module "api" {
  source = "..."
  ...

  layers = [module.rds_ca_layer.layer_arn]
}
```

</Fragment>
</Infrastructure>

Luego pasa la ruta de la capa a `get_session` en la dependencia generada:

```python title="packages/my_api/my_api/dependencies/my-db.py"
from collections.abc import Generator
from typing import Annotated

from fastapi import Depends
from sqlmodel import Session

from my_scope.my_db.connection import get_session

def get_session_dep() -> Generator[Session]:
    yield from get_session(rds_ca='/opt/global-bundle.pem')


SessionDep = Annotated[Session, Depends(get_session_dep)]
```

Cuando uses RDS Proxy, no necesitas configurar el paquete CA de RDS en el tiempo de ejecución Lambda de FastAPI.

## Desarrollo Local

<NxCommands commands={["dev <project-name>"]} />

Esto inicia la FastAPI y todas las bases de datos conectadas. La variable de entorno `LOCAL_DEV=true` hace que el cliente de base de datos se conecte a su base de datos Docker local en lugar de Aurora.
