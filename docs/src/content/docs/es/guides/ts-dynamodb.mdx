---
title: DynamoDB
description: Crear un proyecto DynamoDB
generator: ts#dynamodb
---

import { FileTree } from '@astrojs/starlight/components';
import Link from '@components/link.astro';
import RunGenerator from '@components/run-generator.astro';
import GeneratorParameters from '@components/generator-parameters.astro';
import Infrastructure from '@components/infrastructure.astro';
import NxCommands from '@components/nx-commands.astro';
import Snippet from '@components/snippet.astro';
import OptionFilter from '@components/option-filter.astro';

Este generador crea un nuevo proyecto TypeScript respaldado por [Amazon DynamoDB](https://aws.amazon.com/dynamodb/), utilizando [ElectroDB](https://electrodb.dev/) para el modelado de entidades con seguridad de tipos. Genera el código de aplicación y la infraestructura necesaria para aprovisionar y gestionar una tabla DynamoDB usando AWS CDK o Terraform, con soporte para diseño de tabla única y desarrollo local integrado mediante DynamoDB Local.

## Uso

### Generar un Proyecto DynamoDB

<RunGenerator generator="ts#dynamodb" />

### Opciones

<GeneratorParameters generator="ts#dynamodb" />

## Salida del Generador

El generador crea la siguiente estructura de proyecto en el directorio `<directory>/<name>`:

<FileTree>
  - scripts
    - create-local-table.ts Crea la tabla DynamoDB en la instancia local de DynamoDB Local
    - docker-pull.ts Descarga la imagen Docker de DynamoDB Local
    - docker-start.ts Inicia el contenedor Docker de DynamoDB Local
  - src
    - index.ts Punto de entrada del proyecto y exportaciones
    - constants.ts Constantes de desarrollo local y clave de configuración en tiempo de ejecución
    - client.ts Singleton del cliente DynamoDB y resolución del nombre de tabla
    - entities
      - example.ts Definición de entidad ElectroDB de ejemplo
      - index.ts Exportaciones de entidades
  - project.json Configuración del proyecto y objetivos de compilación
</FileTree>

### Infraestructura

<Snippet name="shared-constructs" />

<Infrastructure>
  <Fragment slot="cdk">
    <FileTree>
      - packages/common/constructs/src
        - app
          - dynamodb
            - \<name>.ts Infraestructura específica para tu tabla
        - core
          - dynamodb.ts Construcción genérica de tabla DynamoDB
    </FileTree>
  </Fragment>
  <Fragment slot="terraform">
    <FileTree>
      - packages/common/terraform/src
        - app
          - dynamodb
            - \<name>.tf Módulo específico para tu tabla
        - core
          - dynamodb.tf Módulo genérico de DynamoDB
    </FileTree>
  </Fragment>
</Infrastructure>

## Desarrollo Local

### Iniciar DynamoDB Local

El generador configura un objetivo `serve-local` que inicia una instancia de [DynamoDB Local](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.html) mediante Docker y crea la tabla:

<NxCommands commands={['run <project-name>:serve-local']} />

Esto automáticamente:
1. Descarga la imagen Docker de DynamoDB Local (objetivo `docker-pull`)
2. Inicia un contenedor Docker
3. Crea una tabla local con índices predefinidos

### Modelado de Datos

El proyecto generado utiliza [ElectroDB](https://electrodb.dev/) para el modelado de entidades con seguridad de tipos en una única tabla DynamoDB. Agrega o actualiza archivos de entidad en `src/entities/`, utilizando la entidad de ejemplo generada como punto de partida.

Ejemplo de definición de entidad:

```ts title="packages/my-table/src/entities/user.ts"
import { Entity } from 'electrodb';
import { getDynamoDBClient, resolveTableName } from '../client.js';

export const createUserEntity = async () =>
  new Entity(
    {
      model: {
        entity: 'user',
        version: '1',
        service: 'MyTable',
      },
      attributes: {
        id: { type: 'string', required: true },
        email: { type: 'string', required: true },
        createdAt: {
          type: 'string',
          default: () => new Date().toISOString(),
          readOnly: true,
        },
        updatedAt: {
          type: 'string',
          default: () => new Date().toISOString(),
          watch: '*',
          set: () => new Date().toISOString(),
        },
      },
      indexes: {
        primary: {
          pk: { field: 'pk', composite: ['id'] },
          sk: { field: 'sk', composite: [] },
        },
      },
    },
    { client: getDynamoDBClient(), table: await resolveTableName() },
  );
```

Para más detalles, consulta la [documentación de entidades de ElectroDB](https://electrodb.dev/en/modeling/entities/).

### Usar el Cliente DynamoDB

El archivo generado `src/client.ts` exporta dos utilidades clave:

- `getDynamoDBClient()` — devuelve un singleton en caché de `DynamoDBClient`. Cuando `SERVE_LOCAL=true`, se conecta a la instancia local de DynamoDB Local; de lo contrario, crea un cliente AWS usando la cadena de credenciales predeterminada.
- `resolveTableName()` — devuelve el nombre de la tabla DynamoDB. Cuando `SERVE_LOCAL=true`, devuelve la constante del nombre de tabla local; de lo contrario, obtiene el nombre de AWS AppConfig usando la variable de entorno `RUNTIME_CONFIG_APP_ID` y lo almacena en caché para llamadas posteriores.

## Agregar/Eliminar Índices Secundarios Globales

Comienza actualizando `GLOBAL_SECONDARY_INDEXES` en `src/gsi.ts` — esta es la fuente de verdad para tus definiciones de GSI, y en la próxima ejecución de `serve-local` el script agregará o eliminará automáticamente índices en la tabla local para reflejar la nueva lista. Al usar CDK, estos cambios también se reflejan automáticamente en tu tabla de AWS en el próximo despliegue.

<OptionFilter when={{ iacProvider: 'Terraform' }}>
  A diferencia de CDK, los cambios en `src/gsi.ts` no se reflejan automáticamente en tu módulo Terraform. También debes actualizar manualmente tu módulo Terraform para agregar o eliminar las definiciones de GSI correspondientes antes de desplegar.
</OptionFilter>

## Conectarse a la Tabla

En cualquier proyecto TypeScript, importa las fábricas de entidades de tu paquete DynamoDB y úsalas directamente:

```ts
import { createExampleEntity } from ':my-scope/my-table';

const entity = await createExampleEntity();
const result = await entity.query.primary({ id: '123' }).go();
```

### Generadores de Conexión

Para tipos de proyecto específicos, usa el generador `connection` para conectar automáticamente las dependencias de desarrollo local para que DynamoDB Local se inicie automáticamente junto con tu proyecto:

- <Link path="guides/connection/trpc-dynamodb">API tRPC → DynamoDB</Link>
- <Link path="guides/connection/smithy-dynamodb">API Smithy → DynamoDB</Link>
- <Link path="guides/connection/ts-agent-dynamodb">Agente TypeScript → DynamoDB</Link>
- <Link path="guides/connection/ts-mcp-server-dynamodb">Servidor MCP → DynamoDB</Link>

## Desplegar tu Tabla

El generador DynamoDB crea infraestructura CDK o Terraform según tu `iacProvider` seleccionado.

<Infrastructure>
<Fragment slot="cdk">
La construcción CDK se crea en `common/constructs`. Ejemplo de uso:

```ts title="packages/infra/src/stacks/application-stack.ts"
import { MyTable } from ':my-scope/common-constructs';

export class ApplicationStack extends Stack {
  constructor(scope: Construct, id: string, props?: StackProps) {
    super(scope, id, props);

    const table = new MyTable(this, 'Table');
  }
}
```

Esto aprovisiona una tabla DynamoDB con:
- `pk` (clave de partición) y `sk` (clave de ordenamiento), ambas de tipo `String`
- Facturación bajo demanda (`PAY_PER_REQUEST`)
- Cifrado KMS gestionado por el cliente con rotación automática de claves
- Recuperación point-in-time habilitada
- Protección contra eliminación habilitada
- Nombre de tabla registrado en Runtime Config bajo el espacio de nombres `dynamodb` en AWS AppConfig
</Fragment>
<Fragment slot="terraform">
El módulo Terraform se crea en `common/terraform`. Ejemplo de uso:

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source = "../../common/terraform/src/app/dynamodb/my-table"
}
```

Esto aprovisiona una tabla DynamoDB con:
- `pk` (clave de partición) y `sk` (clave de ordenamiento), ambas de tipo `String`
- Facturación bajo demanda (`PAY_PER_REQUEST`)
- Cifrado KMS gestionado por el cliente con rotación automática de claves
- Recuperación point-in-time habilitada
- Protección contra eliminación habilitada
- Nombre de tabla registrado en Runtime Config
</Fragment>
</Infrastructure>

### Conceder Acceso

<Snippet name="connection/lambda-dynamodb-access" />

Asegúrate de que tus funciones Lambda tengan la variable de entorno RUNTIME_CONFIG_APP_ID configurada para que resolveTableName() pueda buscar el nombre de la tabla en tiempo de ejecución.

### Protección contra Eliminación

La protección contra eliminación está habilitada por defecto para prevenir la eliminación accidental de la tabla.

#### Deshabilitar la Protección contra Eliminación

Deshabilítala para entornos donde se espera la eliminación de tablas, como stacks de desarrollo o vista previa de corta duración.

<Infrastructure>
<Fragment slot="cdk">

```ts title="packages/infra/src/stacks/application-stack.ts"
import { MyTable } from ':my-scope/common-constructs';

const table = new MyTable(this, 'Table', {
  deletionProtection: false,
});
```
</Fragment>
<Fragment slot="terraform">

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source                      = "../../common/terraform/src/app/dynamodb/my-table"
  deletion_protection_enabled = false
}
```
</Fragment>
</Infrastructure>

### Modo de Facturación

La tabla usa por defecto facturación bajo demanda (`PAY_PER_REQUEST`). Cambia a capacidad aprovisionada para cargas de trabajo predecibles de alto rendimiento.

<Infrastructure>
<Fragment slot="cdk">

```ts title="packages/infra/src/stacks/application-stack.ts"
import { BillingMode } from 'aws-cdk-lib/aws-dynamodb';
import { MyTable } from ':my-scope/common-constructs';

const table = new MyTable(this, 'Table', {
  billingMode: BillingMode.PROVISIONED,
  readCapacity: 5,
  writeCapacity: 5,
});
```
</Fragment>
<Fragment slot="terraform">

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source       = "../../common/terraform/src/app/dynamodb/my-table"
  billing_mode = "PROVISIONED"
}
```
</Fragment>
</Infrastructure>

### Recuperación Point-in-time

La recuperación point-in-time está habilitada por defecto, permitiéndote restaurar la tabla a cualquier punto en los últimos 35 días.

#### Deshabilitar la Recuperación Point-in-time

<Infrastructure>
<Fragment slot="cdk">

```ts title="packages/infra/src/stacks/application-stack.ts"
import { MyTable } from ':my-scope/common-constructs';

const table = new MyTable(this, 'Table', {
  pointInTimeRecoverySpecification: { pointInTimeRecoveryEnabled: false },
});
```
</Fragment>
<Fragment slot="terraform">

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source                         = "../../common/terraform/src/app/dynamodb/my-table"
  point_in_time_recovery_enabled = false
}
```
</Fragment>
</Infrastructure>

### Rotación de Clave de Cifrado

La clave KMS utilizada para cifrar la tabla tiene la rotación automática de claves habilitada por defecto. Deshabilítala si tu política de seguridad gestiona la rotación externamente.

#### Deshabilitar la Rotación de Clave de Cifrado

<Infrastructure>
<Fragment slot="cdk">

```ts title="packages/infra/src/stacks/application-stack.ts"
import { MyTable } from ':my-scope/common-constructs';

const table = new MyTable(this, 'Table', {
  enableKeyRotation: false,
});
```
</Fragment>
<Fragment slot="terraform">

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source              = "../../common/terraform/src/app/dynamodb/my-table"
  enable_key_rotation = false
}
```
</Fragment>
</Infrastructure>
