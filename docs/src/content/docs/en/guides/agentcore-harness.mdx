---
title: AgentCore Harness
description: Create an Amazon Bedrock AgentCore Harness project
generator: agentcore-harness
---

import { FileTree } from '@astrojs/starlight/components';
import GeneratorParameters from '@components/generator-parameters.astro';
import Infrastructure from '@components/infrastructure.astro';
import Link from '@components/link.astro';
import NxCommands from '@components/nx-commands.astro';
import RunGenerator from '@components/run-generator.astro';
import Snippet from '@components/snippet.astro';

Generate an [Amazon Bedrock AgentCore Harness](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness.html) project. A Harness is a managed agent loop powered by Strands Agents: it owns deployment defaults for the model, system prompt, tools, memory, skills, environments, truncation, authorization, and execution limits, while the service accepts per-invocation overrides for supported fields. Reusing the same Runtime Session ID continues the same Harness session.

The generator only writes files: a standalone invocation project, plus optional CDK or Terraform infrastructure. The Harness exists in AWS after you deploy that infrastructure through your normal workflow; nothing runs, validates, or invokes a live Harness during generation.

## Usage

### Generate an AgentCore Harness

<RunGenerator generator="agentcore-harness" />

### Options

<GeneratorParameters generator="agentcore-harness" />

Everything except `name` is optional, with these defaults:

| Option | Default |
| --- | --- |
| `directory` / `subDirectory` | `packages` / the kebab-case harness name (project root `packages/<name>`) |
| `modelId` | `global.anthropic.claude-sonnet-4-6` |
| `systemPrompt` | `You are a helpful AI assistant.` |
| `allowedTools` | `['@builtin']` (accepts 1–64 non-empty patterns) |
| `maxIterations`, `maxTokens`, `timeoutSeconds` | Unset — the AgentCore service defaults apply (positive integers when supplied) |
| `infra` | `agentcore` (`none` skips infrastructure) |
| `iac` | `inherit` (uses your workspace's configured provider) |

Set `allowedTools` to narrower patterns such as `@builtin/file_operations`, or add further tool definitions through the native extension points described in <a href="#customizing-your-harness">Customizing your Harness</a>.

## Generator Output

The generator creates a standalone project at `packages/<name>/`:

<FileTree>
  - packages/\<name>/
    - invoke.ts Command-line entry point for the `invoke` target
    - invoke-harness.ts `InvokeHarness` discovery, session, and streaming implementation
    - tsconfig.json TypeScript configuration for the `build` type-check target
    - README.md Invocation and customization instructions
    - project.json Adds the `invoke` and `build` targets
</FileTree>

### Infrastructure

Infrastructure is generated when `infra` is `agentcore` (the default). With `infra: none` no infrastructure is generated — set `HARNESS_ARN` to invoke a Harness managed elsewhere, and re-run the generator with `infra: agentcore` later to add infrastructure; existing project files (including your edits) are preserved.

When `iac` is `inherit` (the default), the provider configured for your workspace is used. An explicit `--iac=cdk` or `--iac=terraform` that differs from the provider of your existing shared infrastructure project fails with a diagnostic naming both providers, before any files are written.

<Snippet name="shared-constructs" />

<Infrastructure>
  <Fragment slot="cdk">
    <FileTree>
      - packages/common/constructs/src/app/harnesses/\<name>/
        - \<name>.ts CDK construct containing the Harness and execution role
    </FileTree>
  </Fragment>
  <Fragment slot="terraform">
    <FileTree>
      - packages/common/terraform/src/app/harnesses/\<name>/
        - \<name>.tf Terraform module containing the Harness and execution role
    </FileTree>
  </Fragment>
</Infrastructure>

The generated infrastructure manages the Harness through the native resource (CDK `aws_bedrockagentcore.CfnHarness`, Terraform `aws_bedrockagentcore_harness`) with your generated defaults, creates an IAM execution role with the baseline permissions described below, and uses IAM inbound authorization (no custom JWT authorizer is configured by default).

The Harness ARN is registered at `agentcore.harnesses.<ClassName>` in <Link path="guides/runtime-config">Runtime Configuration</Link>, preserving any existing entries.

## Deploying your AgentCore Harness

The generator creates CDK or Terraform infrastructure as code based on your selected `iac` provider. You can use this to deploy your Harness through your usual infrastructure workflow.

<Infrastructure>
  <Fragment slot="cdk">
The CDK construct for deploying your Harness lives in the `common/constructs` folder. Instantiate it from a CDK application:

```ts title="packages/infra/src/stacks/application-stack.ts"
import { MyHarness } from ':my-scope/common-constructs';
import { Stack, type StackProps } from 'aws-cdk-lib';
import type { Construct } from 'constructs';

export class ApplicationStack extends Stack {
  constructor(scope: Construct, id: string, props?: StackProps) {
    super(scope, id, props);

    new MyHarness(this, 'MyHarness');
  }
}
```

The construct's props interface (`MyHarnessProps`) extends `Partial<Omit<CfnHarnessProps, 'executionRoleArn'>>`, so any native Harness property can be supplied and takes precedence over the generated defaults:

```ts
const harness = new MyHarness(this, 'MyHarness', {
  allowedTools: ['@builtin/file_operations'],
  maxIterations: 20,
  timeoutSeconds: 600,
});
```

The construct also accepts:

- `executionRole` — an existing IAM role to use instead of the generated role. A supplied role is used as-is: the baseline permissions are not added to it, and its ARN always feeds the Harness (the raw `executionRoleArn` string cannot be overridden).
- `modelResourceArns` — the Bedrock model and inference-profile ARNs the generated execution role may invoke, replacing the default list.

Its public members are `harness` (the `CfnHarness`), `executionRole`, `grantPrincipal`, the `harnessArn` getter, `addToRolePolicy(statement)` for execution role extensions, and `grantInvokeAccess(grantee)` for authorizing callers.

Deploy the stack with your infrastructure project as usual — see the <Link path="guides/typescript-infrastructure">CDK infrastructure guide</Link>.
  </Fragment>
  <Fragment slot="terraform">
The Terraform module for deploying your Harness is in the `common/terraform` folder. Reference it from a Terraform configuration:

```hcl title="packages/infra/src/main.tf"
module "my_harness" {
  source = "../../common/terraform/src/app/harnesses/my-harness"

  # Module arguments override the generated variable defaults
  allowed_tools   = ["@builtin/file_operations"]
  max_iterations  = 20
  timeout_seconds = 600
}
```

The module exposes variables for `model_id`, `system_prompt` (marked `sensitive`), `allowed_tools`, and the nullable execution limits `max_iterations`, `max_tokens`, and `timeout_seconds` (null means the AgentCore service default). It also accepts:

- `model_resource_arns` — the Bedrock model and inference-profile ARNs the execution role may invoke, replacing the default list.
- `additional_execution_role_policy_statements` — a list of IAM statement objects (`Effect`, `Action`, `Resource`, optional `Sid` and `Condition`) appended to the execution role policy.

The module outputs `harness_id`, `harness_arn`, and `execution_role_arn`.

Deploy with your Terraform project's plan/apply workflow as usual — see the <Link path="guides/terraform-project">Terraform project guide</Link>.
  </Fragment>
</Infrastructure>

:::note[System prompt visibility]
The system prompt is deployment configuration, not secret storage: it can appear in synthesized CloudFormation templates or Terraform state. Terraform marks `system_prompt` as sensitive to reduce routine CLI display, but the value remains in state.
:::

## Execution Role Permissions

When you do not supply your own role, the generated execution role trusts `bedrock-agentcore.amazonaws.com` (scoped by `aws:SourceAccount` and `aws:SourceArn` conditions) and grants the baseline permissions from the [Harness security guidance](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/harness-security.html):

- Bedrock model invocation (`bedrock:InvokeModel`, `bedrock:InvokeModelWithResponseStream`) — defaults to all foundation models plus your account's Bedrock resources in the deployment Region; narrow it with `modelResourceArns` (CDK) or `model_resource_arns` (Terraform)
- Amazon ECR Public authorization token access for the environment image
- X-Ray tracing, CloudWatch Logs scoped to AgentCore runtime log groups, and CloudWatch metrics in the `bedrock-agentcore` namespace
- AgentCore workload identity token access
- The AWS-owned default Browser and Code Interpreter

The defaults deliberately exclude `bedrock-agentcore:InvokeAgentRuntimeCommand` and all access to customer-owned resources: Gateways, memory, custom browsers, custom code interpreters, skills, secrets, and VPC resources. When you configure one of these capabilities, grant it least-privilege access explicitly:

<Infrastructure>
  <Fragment slot="cdk">
```ts
harness.addToRolePolicy(
  new iam.PolicyStatement({
    actions: ['secretsmanager:GetSecretValue'],
    resources: [toolSecret.secretArn],
  }),
);
```
  </Fragment>
  <Fragment slot="terraform">
```hcl
module "my_harness" {
  source = "../../common/terraform/src/app/harnesses/my-harness"

  additional_execution_role_policy_statements = [
    {
      Sid      = "ReadToolSecret"
      Effect   = "Allow"
      Action   = ["secretsmanager:GetSecretValue"]
      Resource = [aws_secretsmanager_secret.tool.arn]
    }
  ]
}
```
  </Fragment>
</Infrastructure>

### Invocation permissions

A caller needs exactly `bedrock-agentcore:InvokeHarness` and `bedrock-agentcore:InvokeAgentRuntime` on the Harness ARN. The CDK construct's `grantInvokeAccess(grantee)` grants exactly these two actions on the base Harness ARN; for Terraform, add an equivalent statement to the calling principal's policy using the `harness_arn` output.

## Invoking your Harness

The generated `invoke` target calls `InvokeHarness` once with your prompt and streams the response. All prompt arguments are joined with single spaces and trimmed into one user message; an empty or whitespace-only prompt exits with usage guidance before any AWS call. Credentials come from the standard AWS SDK credential provider chain, and the AWS Region is derived from the Harness ARN in any partition.

The Harness ARN is discovered in this order:

1. `HARNESS_ARN` (non-empty): used directly, without reading Runtime Configuration:

   <NxCommands commands={['run <project>:invoke -- "Explain this repository"']} env={{ HARNESS_ARN: '<harness-arn>' }} />

2. `RUNTIME_CONFIG_APP_ID`: resolves the ARN from the `agentcore.harnesses.<ClassName>` entry published by the deployed infrastructure:

   <NxCommands commands={['run <project>:invoke -- "Explain this repository"']} env={{ RUNTIME_CONFIG_APP_ID: '<application-id>' }} />

If neither is set, the command fails with an error naming both options.

### Sessions

Without `HARNESS_SESSION_ID`, each command generates a fresh UUID as the `runtimeSessionId` and prints it to stderr. A supplied `HARNESS_SESSION_ID` must contain between 33 and 100 characters, inclusive — out-of-range values are rejected before any AWS call. Reuse the printed session ID to continue the same session:

<NxCommands commands={['run <project>:invoke -- "Continue with the next step"']} env={{ HARNESS_SESSION_ID: '<session-id>', HARNESS_ARN: '<harness-arn>' }} />

### Output and exit codes

- stdout carries only the streamed response text, in delta order.
- stderr carries diagnostics: the session ID, metadata events from the stream, and errors.
- A successful invocation requires the stream's `messageStop` event and exits zero. The command exits non-zero for a missing ARN source, an invalid session ID length, a malformed or Region-less ARN, a missing event stream, service error events, a stream that ends without `messageStop`, or SDK request failures — text already streamed stays on stdout either way.

## Customizing your Harness

Generator options seed the deployment defaults at creation time; after that, the generated files are yours. Re-running the generator never overwrites existing files (it only adds missing files and merges project metadata), so changed generator options do not rewrite existing infrastructure — edit the generated construct or module directly instead.

<Infrastructure>
  <Fragment slot="cdk">
Every native Harness property of the pinned `aws-cdk-lib/aws-bedrockagentcore` module is available through the construct's props — alternate model providers, tool definitions, memory, skills, environment configuration, truncation, custom JWT authorization, and execution limits — and explicit props take precedence over the generated defaults. Alternatively, edit the generated construct in `packages/common/constructs/src/app/harnesses/<name>/<name>.ts`.
  </Fragment>
  <Fragment slot="terraform">
The generated module keeps the native `aws_bedrockagentcore_harness` resource directly editable and documents an extension region for provider-native fields: alternate model providers (`gemini_model_config`, `openai_model_config`), `tool` blocks, `memory`, `skill` blocks, environments (`environment`, `environment_variables`, `environment_artifact`), `truncation`, and `authorizer_configuration` with a `custom_jwt_authorizer`. Edit `packages/common/terraform/src/app/harnesses/<name>/<name>.tf` directly.
  </Fragment>
</Infrastructure>

Omitting authorizer configuration (the default) means IAM inbound authorization; configure a custom JWT authorizer through the native field to change that.

### Per-invocation overrides

The values configured in infrastructure are deployment defaults. The service also accepts per-invocation overrides for supported Harness fields (such as models, tools, and skills) in the `InvokeHarness` request; deployment defaults apply wherever a field is not overridden.

:::caution[Validate invocation overrides]
Per-invocation overrides are trusted inputs at the service boundary: any authorized caller can override supported fields. If untrusted users can reach an application that forwards overrides to `InvokeHarness`, validate and authorize them at the application layer (for example, allowlist models and tools), keep the execution role scoped to required resources, and do not grant `bedrock-agentcore:InvokeAgentRuntimeCommand` unless you specifically need it.
:::

## Connecting to Gateways and MCP servers

Harness-to-Gateway and Harness-to-MCP connection generators are follow-up work and are not part of this initial release. To use an AgentCore Gateway or MCP server with your Harness today, configure the corresponding native tool fields in the generated infrastructure and grant the execution role least-privilege access to those resources explicitly.
