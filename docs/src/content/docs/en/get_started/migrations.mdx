---
title: Migrations Reference
description: Every migration @aws/nx-plugin ships, in the order nx migrate runs them.
---
import Link from '@components/link.astro';
import MigrationList from '@components/migration-list.astro';

Every migration `@aws/nx-plugin` ships, grouped by the release that shipped it and listed in the order [`nx migrate`](https://nx.dev/docs/features/automate-updating-dependencies) runs them. See <Link path="get_started/upgrading">Upgrading Your Workspace</Link> for how to run them.

`nx migrate` applies only the migrations released after the version you are on, so the release you are upgrading from is where to start reading.

## Releases

<MigrationList />
