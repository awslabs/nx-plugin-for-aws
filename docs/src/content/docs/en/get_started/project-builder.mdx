---
title: Project Builder
description: Visually design your project architecture and generate CLI commands to scaffold it.
tableOfContents: false
---

import ProjectBuilder from '@components/project-builder/ProjectBuilder.tsx';

<style>{`
  /* ── Full-width layout overrides ── */
  /* Remove the max-width content constraint */
  .sl-container {
    max-width: none !important;
  }
  /* Remove content panel padding */
  .content-panel {
    padding: 0 !important;
  }
  /* Hide the right sidebar entirely */
  .right-sidebar-container {
    display: none !important;
  }
  /* Let the main frame expand */
  .main-frame {
    max-width: none !important;
  }
  /* Remove main padding */
  main {
    padding: 0 !important;
  }
  /* Remove markdown content constraints */
  .sl-markdown-content {
    max-width: none !important;
    padding: 0 !important;
  }
  .sl-markdown-content > :first-child {
    margin-top: 0 !important;
  }

  /* ── ReactFlow controls dark mode ── */
  .react-flow__controls {
    box-shadow: 0 1px 4px rgba(0,0,0,0.2) !important;
    border-radius: 6px !important;
    overflow: hidden !important;
    border: 1px solid var(--sl-color-gray-5) !important;
  }
  .react-flow__controls-button {
    background: var(--sl-color-bg-nav) !important;
    color: var(--sl-color-white) !important;
    border-bottom: 1px solid var(--sl-color-gray-5) !important;
    fill: var(--sl-color-white) !important;
    width: 28px !important;
    height: 28px !important;
  }
  .react-flow__controls-button:hover {
    background: var(--sl-color-gray-5) !important;
  }
  .react-flow__controls-button svg {
    fill: currentColor !important;
  }

  /* ── Ensure edges render above background ── */
  .react-flow__edges {
    z-index: 1 !important;
  }
  .react-flow__edge-path {
    stroke-width: 2 !important;
  }
`}</style>

<ProjectBuilder client:only="react" />
