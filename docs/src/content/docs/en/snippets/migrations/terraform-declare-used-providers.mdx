---
title: terraform-declare-used-providers
---
import { Aside } from '@astrojs/starlight/components';

<Aside type="note">
If you have added your own Terraform providers to a generated module, declare them in that module's `required_providers` block before running this migration — the codemod only reconciles the providers the generators vend.
</Aside>
