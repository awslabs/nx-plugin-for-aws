---
title: Cấu hình WAF Terraform
---

#### WAF

Đối với REST API, module Terraform được tạo ra sẽ liên kết AWS WAFv2 Web ACL với API Gateway stage theo mặc định. Web ACL sử dụng bộ quy tắc mặc định được quản lý bởi AWS (`AWSManagedRulesCommonRuleSet` và `AWSManagedRulesKnownBadInputsRuleSet`), cung cấp bảo vệ chống lại các lỗ hổng web phổ biến bao gồm OWASP Top 10.

Để tắt tính năng này (ví dụ: để gắn Web ACL của riêng bạn), hãy đặt `enable_waf` thành `false`:

```hcl {4}
module "my_api" {
  source = "../../common/terraform/src/app/apis/my-api"

  enable_waf = false
}
```
