---
title: "TypeScript API"
description: "Tài liệu tham khảo cho trình tạo API TypeScript"
generator: ts#api
---
import Astro from '@astrojs/react';
import { CardGrid } from '@astrojs/starlight/components';
import RunGenerator from '@components/run-generator.astro';
import GeneratorParameters from '@components/generator-parameters.astro';
import FrameworkCard from '@components/framework-card.astro';

Trình tạo TypeScript API tạo dự án API mới với framework bạn chọn. Nó hỗ trợ [tRPC](https://trpc.io/) cho API an toàn kiểu từ đầu đến cuối và [Smithy](https://smithy.io/) cho phát triển API hướng mô hình.

## Cách sử dụng

### Tạo TypeScript API

Bạn có thể tạo TypeScript API mới bằng hai cách:

<RunGenerator generator="ts#api" />

### Tùy chọn

<GeneratorParameters generator="ts#api" />

## Framework

<CardGrid>
  <FrameworkCard
    title="tRPC"
    description="API an toàn kiểu từ đầu đến cuối với TypeScript. Lý tưởng khi frontend và backend chia sẻ codebase TypeScript."
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/trpc`}
    logo="trpc"
  />
  <FrameworkCard
    title="Smithy"
    description="Phát triển API hướng mô hình sử dụng ngôn ngữ định nghĩa giao diện. Lý tưởng cho API được sử dụng bởi nhiều ngôn ngữ."
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/ts-smithy-api`}
    logo="smithy"
  />
</CardGrid>
