---
title: "Kết nối"
description: "Tài liệu tham khảo cho các kết nối"
---
import Astro from '@astrojs/react';
import { CardGrid, LinkButton } from '@astrojs/starlight/components';
import Link from '@components/link.astro';
import ConnectionCard from '@components/connection-card.astro';

Generator này được sử dụng để kết nối các dự án với nhau, chẳng hạn như các trang web gọi API. Chỉ cần chọn dự án nguồn (ví dụ: dự án sẽ gọi API của bạn) và dự án đích (ví dụ: dự án API của bạn), và generator này sẽ xử lý việc tích hợp hai dự án đó.

### Các Kết Nối Được Hỗ Trợ

Generator Connection hỗ trợ các kết nối sau:

<CardGrid>
  <ConnectionCard
    title="React to tRPC"
    description="Gọi API tRPC từ một trang web React"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/connection/react-trpc`}
    source="react"
    target="trpc"
  />
  <ConnectionCard
    title="React to FastAPI"
    description="Gọi Python FastAPI từ một trang web React"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/connection/react-fastapi`}
    source="react"
    target="fastapi"
  />
  <ConnectionCard
    title="React to Smithy API"
    description="Gọi Smithy API từ một trang web React"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/connection/react-smithy`}
    source="react"
    target="smithy"
  />
  <ConnectionCard
    title="React to Python Strands Agent"
    description="Gọi Python Strands Agent từ một trang web React"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/connection/react-py-strands-agent`}
    source="react"
    target="strands"
    targetBadge="python"
  />
  <ConnectionCard
    title="React to TypeScript Strands Agent"
    description="Gọi TypeScript Strands Agent từ một trang web React"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/connection/react-ts-strands-agent`}
    source="react"
    target="strands"
    targetBadge="typescript"
  />
  <ConnectionCard
    title="React to AG-UI Agent"
    description="Gọi Strands Agent hỗ trợ giao thức AG-UI từ một trang web React thông qua CopilotKit"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/connection/react-agui`}
    source="react"
    target="copilotkit"
  />
  <ConnectionCard
    title="Python Strands Agent to MCP"
    description="Kết nối Python Strands Agent với MCP server"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/connection/py-strands-agent-mcp`}
    source="strands"
    sourceBadge="python"
    target="mcp"
  />
  <ConnectionCard
    title="TypeScript Strands Agent to MCP"
    description="Kết nối TypeScript Strands Agent với MCP server"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/connection/ts-strands-agent-mcp`}
    source="strands"
    sourceBadge="typescript"
    target="mcp"
  />
  <ConnectionCard
    title="Python Strands Agent to A2A Agent"
    description="Kết nối Python Strands Agent với A2A agent từ xa"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/connection/py-strands-agent-a2a`}
    source="strands"
    sourceBadge="python"
    target="a2a"
  />
  <ConnectionCard
    title="TypeScript Strands Agent to A2A Agent"
    description="Kết nối TypeScript Strands Agent với A2A agent từ xa"
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/connection/ts-strands-agent-a2a`}
    source="strands"
    sourceBadge="typescript"
    target="a2a"
  />
</CardGrid>

:::note[Cấu Hình Runtime]
Generator connection sử dụng <Link path="guides/runtime-config">Runtime Configuration</Link> để truyền các giá trị tại thời điểm triển khai (chẳng hạn như URL API, cài đặt Cognito và ARN runtime của agent) giữa các dự án và component được tạo ra tại runtime để chúng có thể khám phá và kết nối với nhau.
:::