---
title: DCR Proxy
description: Tạo một proxy OAuth Dynamic Client Registration cho các máy chủ MCP được xác thực bằng Cognito
generator: ts#dcr-proxy
---

import { FileTree } from '@astrojs/starlight/components';
import Link from '@components/link.astro';
import RunGenerator from '@components/run-generator.astro';
import GeneratorParameters from '@components/generator-parameters.astro';
import NxCommands from '@components/nx-commands.astro';
import Infrastructure from '@components/infrastructure.astro';
import Snippet from '@components/snippet.astro';

Trình tạo DCR Proxy tạo ra một proxy OAuth [Dynamic Client Registration (DCR)](https://datatracker.ietf.org/doc/html/rfc7591) đặt trước Amazon Cognito User Pool.

Các client MCP (như Claude Code, Kiro CLI hoặc MCP Inspector) mong đợi xác thực với một máy chủ ủy quyền OAuth hỗ trợ Dynamic Client Registration và khám phá metadata. Amazon Cognito không hỗ trợ DCR một cách tự nhiên, và App Client secret của nó không bao giờ được phép lộ ra cho một public client. Proxy này bắc cầu khoảng cách đó: nó giữ nguyên luồng Cognito Hosted UI, triển khai DCR, chèn App Client secret ở phía máy chủ trong quá trình trao đổi token, và chuyển tiếp lưu lượng MCP đến máy chủ MCP upstream của bạn.

## Cách sử dụng

### Tạo một DCR proxy

<RunGenerator generator="ts#dcr-proxy" />

### Tùy chọn

<GeneratorParameters generator="ts#dcr-proxy" />

## Đầu ra của Trình tạo

Trình tạo tạo ra một <Link path="/guides/typescript-project">dự án TypeScript</Link> độc lập chứa các Lambda handler, và cơ sở hạ tầng để triển khai chúng dựa trên `iacProvider` bạn đã chọn.

<FileTree>

- \<dcr-proxy-name>
  - src/
    - handlers/
      - authorization-server-metadata.ts Phục vụ `/.well-known/oauth-authorization-server` và `/.well-known/openid-configuration`
      - protected-resource-metadata.ts Phục vụ `/.well-known/oauth-protected-resource`
      - register.ts RFC 7591 Dynamic Client Registration
      - authorize.ts Chuyển hướng đến Cognito Hosted UI
      - token.ts Chèn App Client secret và trao đổi token
      - mcp-proxy.ts Proxy các yêu cầu `/mcp` đến máy chủ MCP upstream

</FileTree>

Các handler được đóng gói độc lập với [Rolldown](https://rolldown.rs/), và cả hai nhà cung cấp IaC đều tham chiếu đến đầu ra bundle kết quả.

### Cơ sở hạ tầng

<Snippet name="shared-constructs" />

Để triển khai proxy, các tệp sau được tạo ra:

<Infrastructure>
<Fragment slot="cdk">
<FileTree>
  - packages/common/constructs/src
    - app
      - dcr-proxies
        - \<dcr-proxy-name>
          - \<dcr-proxy-name>.ts CDK construct triển khai proxy
</FileTree>
</Fragment>
<Fragment slot="terraform">
<FileTree>
  - packages/common/terraform/src
    - app
      - dcr-proxies
        - \<dcr-proxy-name>
          - \<dcr-proxy-name>.tf Terraform module triển khai proxy
</FileTree>
</Fragment>
</Infrastructure>

Cơ sở hạ tầng cung cấp một API Gateway HTTP API với sáu hàm Lambda được kết nối với các route sau:

| Route | Mô tả |
| --- | --- |
| `GET /.well-known/oauth-protected-resource` | Protected resource metadata |
| `GET /.well-known/oauth-authorization-server` | Authorization server metadata |
| `GET /.well-known/openid-configuration` | OpenID configuration (được phục vụ bởi authorization server metadata handler) |
| `POST /register` | Dynamic Client Registration |
| `GET /authorize` | Authorization (chuyển hướng đến Cognito Hosted UI) |
| `POST /oauth/token` | Token exchange (chèn App Client secret) |
| `ANY /mcp` | Proxy đến máy chủ MCP upstream |

Chỉ có token handler được cấp quyền truy cập đọc vào Cognito App Client secret trong Secrets Manager.

## Triển khai DCR Proxy

Proxy không tạo các tài nguyên Cognito hoặc máy chủ MCP của bạn. Thay vào đó, bạn chèn các định danh của các tài nguyên được quản lý ở nơi khác (dù được tạo bởi plugin này hay được cung cấp riêng biệt), giữ cho proxy tách rời khỏi cách các tài nguyên đó được cung cấp.

Bạn cung cấp:

- Cognito User Pool id và App Client id
- ARN của một Secrets Manager secret chứa App Client secret. Token handler đọc điều này tại runtime; giá trị không bao giờ được lộ ra cho client.
- URL cơ sở của Cognito Hosted UI domain
- URL đầy đủ của máy chủ MCP upstream của bạn

<Infrastructure>
<Fragment slot="cdk">
Khởi tạo construct được tạo ra trong stack của bạn, truyền các thuộc tính bắt buộc:

```typescript
import { DcrProxy } from ':my-scope/common-constructs';

new DcrProxy(this, 'DcrProxy', {
  userPoolId: userPool.userPoolId,
  userPoolClientId: userPoolClient.userPoolClientId,
  cognitoClientSecretArn: clientSecret.secretArn,
  cognitoHostedUiBase: userPoolDomain.baseUrl(),
  upstreamUrl: 'https://my-agentcore-runtime-url/mcp',
});
```

Construct này hiển thị các proxy endpoint (`proxyUrl`, `mcpUrl`, `metadataUrl`, `tokenEndpoint`, `registrationEndpoint`) dưới dạng các thuộc tính readonly.
</Fragment>
<Fragment slot="terraform">
Tham chiếu module được tạo ra từ cấu hình Terraform của bạn, truyền các biến bắt buộc:

```hcl
module "dcr_proxy" {
  source = "../../common/terraform/src/app/dcr-proxies/dcr-proxy"

  user_pool_id              = aws_cognito_user_pool.main.id
  user_pool_client_id       = aws_cognito_user_pool_client.main.id
  cognito_client_secret_arn = aws_secretsmanager_secret.client_secret.arn
  cognito_hosted_ui_base    = "https://${aws_cognito_user_pool_domain.main.domain}.auth.${data.aws_region.current.region}.amazoncognito.com"
  upstream_url              = "https://my-agentcore-runtime-url/mcp"
  asset_bucket_name         = module.asset_bucket.bucket_name
}
```

Module này hiển thị các proxy endpoint (`proxy_url`, `mcp_url`, `metadata_url`, `token_endpoint`, `registration_endpoint`) dưới dạng các output.
</Fragment>
</Infrastructure>

:::note
Proxy `/mcp` sử dụng API Gateway HTTP API integrations, có thời gian chờ cố định là 29 giây. Các yêu cầu upstream chạy lâu được đệm bởi proxy thay vì được stream.
:::

### Đặt trước một AgentCore Gateway

Một upstream phổ biến là một [AgentCore Gateway](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/gateway.html) được xác thực bằng Cognito. Tạo một User Pool và App Client duy nhất, ủy quyền App Client đó trên gateway, sau đó truyền các định danh **giống nhau** cho DCR proxy. `upstreamUrl` của proxy là URL của gateway.

Gateway bên dưới là một construct `aws-cdk-lib/aws-bedrockagentcore` thuần túy được cấu hình cho xác thực inbound Cognito — không phải gateway được xác thực bằng IAM được phát ra bởi trình tạo `agentcore-gateway`, không tương thích với luồng OAuth của proxy.

<Infrastructure>
<Fragment slot="cdk">
```typescript
import { DcrProxy } from ':my-scope/common-constructs';
import { UserPool } from 'aws-cdk-lib/aws-cognito';
import * as secretsmanager from 'aws-cdk-lib/aws-secretsmanager';
import {
  Gateway,
  GatewayAuthorizer,
  McpProtocolConfiguration,
  MCPProtocolVersion,
} from 'aws-cdk-lib/aws-bedrockagentcore';

// User Pool và App Client duy nhất được chia sẻ bởi gateway và proxy.
// Đăng ký các callback URL mà client của bạn sử dụng (xem bên dưới).
const userPool = new UserPool(this, 'UserPool');
const userPoolClient = userPool.addClient('DcrProxyClient', {
  generateSecret: true,
  oAuth: {
    flows: { authorizationCodeGrant: true },
    callbackUrls: [
      'http://localhost:41100/callback',
      // Callback được sử dụng bởi Claude Desktop
      'https://claude.ai/api/mcp/auth_callback',
    ],
  },
});
// Cognito Hosted UI yêu cầu một domain — proxy chuyển hướng các luồng OAuth đến đây.
// Sử dụng lại domain hiện có của bạn nếu User Pool đã có một domain.
const userPoolDomain = userPool.addDomain('Domain', {
  cognitoDomain: { domainPrefix: 'my-dcr-proxy' },
});

// Lưu trữ App Client secret trong Secrets Manager để token handler đọc
const clientSecret = new secretsmanager.Secret(this, 'ClientSecret', {
  secretStringValue: userPoolClient.userPoolClientSecret,
});

// Một AgentCore Gateway ủy quyền các JWT inbound được phát hành cho App Client
const gateway = new Gateway(this, 'Gateway', {
  protocolConfiguration: new McpProtocolConfiguration({
    supportedVersions: [MCPProtocolVersion.MCP_2025_06_18],
  }),
  authorizerConfiguration: GatewayAuthorizer.usingCognito({
    userPool,
    allowedClients: [userPoolClient],
  }),
});

new DcrProxy(this, 'DcrProxy', {
  userPoolId: userPool.userPoolId,
  userPoolClientId: userPoolClient.userPoolClientId,
  cognitoClientSecretArn: clientSecret.secretArn,
  cognitoHostedUiBase: userPoolDomain.baseUrl(),
  upstreamUrl: gateway.gatewayUrl ?? '',
});
```
</Fragment>
<Fragment slot="terraform">
```hcl
# User Pool và App Client duy nhất được chia sẻ bởi gateway và proxy.
# Đăng ký các callback URL mà client của bạn sử dụng (xem bên dưới).
resource "aws_cognito_user_pool" "main" {
  name = "my-dcr-proxy"
}

resource "aws_cognito_user_pool_client" "main" {
  name                                 = "dcr-proxy-client"
  user_pool_id                         = aws_cognito_user_pool.main.id
  generate_secret                      = true
  allowed_oauth_flows                  = ["code"]
  allowed_oauth_flows_user_pool_client = true
  callback_urls = [
    "http://localhost:41100/callback",
    # Callback được sử dụng bởi Claude Desktop
    "https://claude.ai/api/mcp/auth_callback",
  ]
}

# Cognito Hosted UI yêu cầu một domain — proxy chuyển hướng các luồng OAuth đến đây.
# Sử dụng lại domain hiện có của bạn nếu User Pool đã có một domain.
resource "aws_cognito_user_pool_domain" "main" {
  domain       = "my-dcr-proxy"
  user_pool_id = aws_cognito_user_pool.main.id
}

# Lưu trữ App Client secret trong Secrets Manager để token handler đọc
resource "aws_secretsmanager_secret" "client_secret" {
  name = "my-dcr-proxy-client-secret"
}

resource "aws_secretsmanager_secret_version" "client_secret" {
  secret_id     = aws_secretsmanager_secret.client_secret.id
  secret_string = aws_cognito_user_pool_client.main.client_secret
}

# Execution role được gateway đảm nhận để truy cập các dịch vụ AWS.
resource "aws_iam_role" "gateway" {
  name = "my-dcr-proxy-gateway-role"

  assume_role_policy = jsonencode({
    Version = "2012-10-17"
    Statement = [{
      Effect    = "Allow"
      Principal = { Service = "bedrock-agentcore.amazonaws.com" }
      Action    = "sts:AssumeRole"
    }]
  })
}

# Một AgentCore Gateway ủy quyền các JWT inbound được phát hành cho App Client.
# Đây là một gateway resource thuần túy được cấu hình cho xác thực inbound Cognito — không phải
# gateway được xác thực bằng IAM được phát ra bởi trình tạo `agentcore-gateway`.
resource "aws_bedrockagentcore_gateway" "main" {
  name          = "my-dcr-proxy-gateway"
  role_arn      = aws_iam_role.gateway.arn
  protocol_type = "MCP"

  authorizer_type = "CUSTOM_JWT"
  authorizer_configuration {
    custom_jwt_authorizer {
      discovery_url   = "https://cognito-idp.${data.aws_region.current.region}.amazonaws.com/${aws_cognito_user_pool.main.id}/.well-known/openid-configuration"
      allowed_clients = [aws_cognito_user_pool_client.main.id]
    }
  }
}

module "dcr_proxy" {
  source = "../../common/terraform/src/app/dcr-proxies/dcr-proxy"

  user_pool_id              = aws_cognito_user_pool.main.id
  user_pool_client_id       = aws_cognito_user_pool_client.main.id
  cognito_client_secret_arn = aws_secretsmanager_secret.client_secret.arn
  cognito_hosted_ui_base    = "https://${aws_cognito_user_pool_domain.main.domain}.auth.${data.aws_region.current.region}.amazoncognito.com"
  upstream_url              = aws_bedrockagentcore_gateway.main.gateway_url
  asset_bucket_name         = module.asset_bucket.bucket_name
}
```
</Fragment>
</Infrastructure>

### Cho phép Client Redirect URI

Vì proxy triển khai Dynamic Client Registration một cách ảo — không có Cognito App Client riêng cho từng client — mọi client MCP đều xác thực thông qua **App Client duy nhất** mà bạn truyền cho proxy. Trong quá trình luồng OAuth, proxy chuyển tiếp `redirect_uri` của client đến Cognito Hosted UI không thay đổi, vì vậy Cognito thực hiện kiểm tra có thẩm quyền: callback phải được đăng ký là một **callback URL trên App Client đó**, nếu không Cognito sẽ từ chối đăng nhập.

Đây là một tác dụng phụ của thiết kế DCR ảo. Một client có thể đăng ký bất kỳ `redirect_uri` nào với proxy, nhưng đăng nhập chỉ thành công nếu URL chính xác đó là một trong các callback URL của App Client. Cognito khớp các callback URL chính xác, bao gồm cả cổng, vì vậy các client lắng nghe trên một cổng tạm thời ngẫu nhiên không thể được bao phủ bởi một wildcard — bạn phải ghim mỗi client vào một callback URL cố định và đăng ký URL chính xác đó trên App Client.

Thêm các callback URL mà client của bạn sử dụng khi bạn tạo App Client:

<Infrastructure>
<Fragment slot="cdk">
```typescript
const userPoolClient = userPool.addClient('DcrProxyClient', {
  generateSecret: true,
  oAuth: {
    flows: { authorizationCodeGrant: true },
    callbackUrls: [
      // Client cục bộ: ghim vào một cổng cố định, không phổ biến thay vì một cổng mặc định
      'http://localhost:41100/callback',
      // Callback được sử dụng bởi Claude Desktop
      'https://claude.ai/api/mcp/auth_callback',
    ],
  },
});
```
</Fragment>
<Fragment slot="terraform">
```hcl
resource "aws_cognito_user_pool_client" "dcr_proxy" {
  # ...
  generate_secret                     = true
  allowed_oauth_flows                 = ["code"]
  allowed_oauth_flows_user_pool_client = true
  callback_urls = [
    # Client cục bộ: ghim vào một cổng cố định, không phổ biến thay vì một cổng mặc định
    "http://localhost:41100/callback",
    # Callback được sử dụng bởi Claude Desktop
    "https://claude.ai/api/mcp/auth_callback",
  ]
}
```
</Fragment>
</Infrastructure>

:::caution
Chỉ đăng ký các callback URL mà bạn tin tưởng — bất kỳ client nào có thể điều khiển trình duyệt đến một trong các URL này đều có thể hoàn thành đăng nhập vào User Pool của bạn.
:::

## Sử dụng Máy chủ MCP được Proxy

Proxy cho phép các client MCP xác thực với Cognito User Pool của bạn mà không cần bất kỳ cấu hình cụ thể nào cho client ngoài URL proxy. Khi một client kết nối với endpoint `/mcp`, nó khám phá metadata OAuth (thông qua `/.well-known/oauth-protected-resource` và `/.well-known/oauth-authorization-server`), tự động đăng ký chính nó, và dẫn người dùng qua đăng nhập Cognito Hosted UI. Proxy chèn App Client secret trong quá trình trao đổi token, vì vậy client không bao giờ cần nó.

Để kết nối một client, hướng nó đến `mcpUrl` của proxy (tức là `<proxyUrl>/mcp`). Các ví dụ bên dưới giả định proxy của bạn được triển khai tại `https://my-proxy.example.com`.

:::note
Mỗi client sử dụng một callback URL trong quá trình luồng OAuth. URL chính xác đó — bao gồm cả cổng của nó — phải được đăng ký là một [callback URL trên Cognito App Client của bạn](#cho-phép-client-redirect-uri), nếu không đăng nhập sẽ thất bại. Khi một client sẽ chọn một cổng ngẫu nhiên, hãy ghim nó vào một giá trị cố định và đăng ký giá trị đó.
:::

### Claude Code

Thêm máy chủ được proxy bằng lệnh [`claude mcp add`](https://code.claude.com/docs/en/mcp), sử dụng HTTP transport. Theo mặc định Claude Code lắng nghe trên một cổng callback ngẫu nhiên; truyền `--callback-port` để ghim nó vào cổng được đăng ký trên App Client của bạn (`41100` trong các ví dụ trên). Claude Code luôn sử dụng đường dẫn `/callback`, vì vậy redirect URI kết quả là `http://localhost:41100/callback`:

```bash
claude mcp add --transport http --callback-port 41100 my-proxied-server https://my-proxy.example.com/mcp
```

Khi bạn lần đầu tiên gọi một công cụ từ máy chủ, Claude Code mở Cognito Hosted UI để xác thực trước khi yêu cầu được proxy upstream.

### Kiro CLI

Thêm máy chủ vào cấu hình MCP [Kiro CLI](https://kiro.dev/docs/cli/mcp/) của bạn, sử dụng HTTP transport. Không có `oauth.redirectUri` rõ ràng, Kiro chọn một cổng callback ngẫu nhiên; đặt nó thành URL được đăng ký trên App Client của bạn để cổng và đường dẫn khớp chính xác:

```json
{
  "mcpServers": {
    "my-proxied-server": {
      "type": "http",
      "url": "https://my-proxy.example.com/mcp",
      "oauth": {
        "redirectUri": "http://localhost:41100/callback"
      }
    }
  }
}
```

Kiro CLI kích hoạt đăng nhập Cognito Hosted UI khi sử dụng lần đầu và quản lý các token kết quả cho các yêu cầu tiếp theo.

:::tip
Bất kỳ client MCP nào hỗ trợ Streamable HTTP transport cùng với ủy quyền OAuth và Dynamic Client Registration đều có thể kết nối với proxy theo cùng một cách — cung cấp URL `/mcp` và client xử lý khám phá, đăng ký và đăng nhập cho bạn.
:::
