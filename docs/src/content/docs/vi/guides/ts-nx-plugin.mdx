---
title: TypeScript Nx Plugin
description: Tạo một Nx Plugin của riêng bạn! Xây dựng các generator tùy chỉnh được tự động cung cấp cho AI vibe-coding thông qua MCP
generator: ts#nx-plugin
---

import { FileTree } from '@astrojs/starlight/components';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';
import GeneratorParameters from '@components/generator-parameters.astro';
import Link from '@components/link.astro';
import Snippet from '@components/snippet.astro';
import InstallCommand from '@components/install-command.astro';

Tạo một [Nx Plugin](https://nx.dev/extending-nx/intro/getting-started) TypeScript hoàn chỉnh với máy chủ [Model Context Protocol (MCP)](https://modelcontextprotocol.io/) tích hợp. Generator này tạo nền tảng để xây dựng các generator tùy chỉnh có thể được sử dụng liền mạch bởi các trợ lý AI để tự động tạo mã và scaffolding dự án.

## Cách sử dụng

### Tạo một Nx Plugin

Bạn có thể tạo một TypeScript Nx Plugin theo hai cách:

<RunGenerator generator="ts#nx-plugin" />

### Tùy chọn

<GeneratorParameters generator="ts#nx-plugin" />

## Kết quả của Generator

Generator sẽ tạo cấu trúc dự án sau:

<FileTree>
- your-plugin/
  - src/
    - index.ts Empty entry point for your plugin
    - mcp-server/
      - index.ts Exports your server
      - server.ts Main MCP server configuration
      - stdio.ts Entry point for your MCP server with STDIO transport
      - http.ts Entry point for your MCP server with Streamable HTTP transport
      - tools/
        - create-workspace-command.ts Tool for workspace creation guidance
        - general-guidance.ts Tool for general Nx and plugin guidance
        - list-generators.ts Tool to list available generators
        - generator-guide.ts Tool for detailed generator information
      - utils.ts Utility functions for the MCP server
  - generators.json Nx generator configuration (initially empty)
  - package.json Plugin package configuration
  - tsconfig.json TypeScript configuration (CommonJS for Nx compatibility)
  - project.json Nx project configuration with build and package targets
</FileTree>

## Làm việc với Nx Plugin của bạn

### Thêm Generators

Sau khi có dự án plugin, bạn có thể thêm các generator bằng cách sử dụng generator <Link path="/vi/guides/nx-generator">`ts#nx-generator`</Link>:

<RunGenerator generator="ts#nx-generator" requiredParameters={{ pluginProject: 'your-plugin' }} />

Điều này sẽ thêm một generator mới vào plugin của bạn.

:::tip[Tài liệu Generator]
Đọc <Link path="/vi/guides/nx-generator">hướng dẫn `ts#nx-generator`</Link> để biết chi tiết về cách triển khai các generator.
:::

Hãy đảm bảo viết một `README.md` chi tiết cho generator của bạn, vì nó được sử dụng bởi công cụ `generator-guide` của MCP Server.

### Các Target của Dự án

Plugin được tạo bao gồm một số target quan trọng:

#### Build Target
Biên dịch mã TypeScript của bạn và chuẩn bị plugin để sử dụng:

<NxCommands commands={['build your-plugin']} />

#### Package Target
Tạo một package có thể phân phối sẵn sàng để xuất bản lên NPM:

<NxCommands commands={['package your-plugin']} />

Package target bao gồm tất cả các tài nguyên cần thiết:
- Các file JavaScript đã biên dịch
- Các file khai báo TypeScript
- Các file cấu hình generator và executor
- Các file tài liệu và giấy phép

## Tích hợp MCP Server

Plugin cấu hình một MCP server bằng cách sử dụng generator <Link path="/vi/guides/ts-mcp-server">`ts#mcp-server`</Link>.

### Công cụ

Plugin bao gồm một MCP server hoàn chỉnh cung cấp cho các trợ lý AI các công cụ sau:

- `general-guidance`: Nhận các phương pháp hay nhất để làm việc với Nx và plugin của bạn
- `create-workspace-command`: Tìm hiểu cách tạo workspace mới có thể sử dụng plugin của bạn
- `list-generators`: Liệt kê tất cả các generator có sẵn trong plugin của bạn
- `generator-guide`: Nhận thông tin chi tiết về các generator cụ thể bao gồm schema và cách sử dụng

### Cấu hình với các Trợ lý AI

<Snippet name="mcp/configuration-ts" parentHeading="Cấu hình với các Trợ lý AI" />

### Tùy chỉnh MCP Server

Bạn có thể mở rộng MCP server bằng cách sửa đổi `server.ts` để thêm các công cụ hoặc tài nguyên bổ sung cụ thể cho lĩnh vực của plugin của bạn.

Để biết thêm chi tiết về việc viết MCP Servers, hãy tham khảo <Link path="/vi/guides/ts-mcp-server">hướng dẫn `ts#mcp-server`</Link>.

## Xuất bản Plugin của bạn

### Chuẩn bị để Xuất bản

1. **Cập nhật Thông tin Package**: Chỉnh sửa [`package.json`](https://docs.npmjs.com/cli/v10/configuring-npm/package-json) của plugin với tên, phiên bản, mô tả và thông tin repository phù hợp.

2. **Build Package**:
   <NxCommands commands={['package your-plugin']} />

3. **Kiểm tra Cục bộ**: Bạn có thể kiểm tra plugin của mình cục bộ bằng cách cài đặt nó trong một workspace khác:
   ```bash
   npm install /path/to/your/workspace/dist/your-plugin/package
   ```

### Xuất bản lên NPM

Khi plugin của bạn đã sẵn sàng:

```bash
cd dist/your-plugin/package
npm publish
```

### Sử dụng Plugins đã Xuất bản

Sau khi xuất bản, người khác có thể cài đặt và sử dụng plugin của bạn:

<InstallCommand pkg="your-plugin-name" />

Sau đó sử dụng các generator của bạn:

<RunGenerator namespace="your-plugin-name" generator="your-generator-name" />

#### Cấu hình các Trợ lý AI với Plugin đã Xuất bản của bạn

MCP server của plugin đã xuất bản của bạn có thể được cấu hình như sau:

```json {3-6}
{
  "mcpServers": {
    "aws-nx-mcp": {
      "command": "npx",
      "args": ["-y", "-p", "your-plugin-name", "your-plugin-name-mcp-server"]
    }
  }
}
```
