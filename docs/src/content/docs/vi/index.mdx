---
title: "@aws/nx-plugin"
description: Nhanh chóng tạo và xây dựng các ứng dụng trên AWS, đưa bạn đến gần hơn với production.
template: splash
hero:
  # `set:html`, so the second line sits under the first inside the hero's copy.
  tagline: <span class="hero-lead">Xây dựng ứng dụng AWS full-stack trong vài phút, không phải vài ngày.</span><span class="hero-subline">Một bộ sưu tập các Nx generator để nhanh chóng xây dựng các ứng dụng cloud-native trên AWS — type-safe, có thể chạy cục bộ và sẵn sàng triển khai.</span>
  image:
    light: ../assets/bulb-black.svg
    dark: ../assets/bulb-white.svg
  actions:
    - text: Bắt đầu
      link: get_started/quick-start
      icon: right-arrow
      variant: primary
    - text: Xây dựng với AI
      link: get_started/building-with-ai
      icon: rocket
      variant: secondary
---

import { CardGrid, Card } from "@astrojs/starlight/components";
import { ContributorList } from "astro-contributors";
import PresetShowcase from "@components/preset-showcase.astro";
import Link from "@components/link.astro";

<div class="landing-section landing-showcase">
  <h2 class="landing-heading">Tạo workspace. Sau đó chỉ cần hỏi.</h2>
  <p class="landing-lede">Một workspace mới đi kèm với MCP server của plugin đã được cấu hình sẵn, vì vậy trợ lý của bạn có thể tìm đúng generator, chạy chúng và để lại cho bạn mã theo best-practice <em>và</em> cơ sở hạ tầng để triển khai nó.</p>

  <PresetShowcase />

  <p class="landing-note">Thích tự chạy các generator? Sao chép các lệnh từ bất kỳ ví dụ nào. Hoặc thiết kế của riêng bạn trong <Link path="get_started/graph-builder">graph builder</Link>: kéo các dự án ra, vẽ các kết nối giữa chúng, và nó sẽ đưa cho bạn các lệnh để tạo ứng dụng của bạn.</p>
</div>

<div class="landing-section landing-why">
  <h2 class="landing-heading">Tại sao chọn @aws/nx-plugin?</h2>
  <CardGrid>
    <Card title="Full-stack trong vài phút" icon="puzzle">
      Website, API, xác thực, agent và cơ sở hạ tầng — tất cả được tạo và kết nối với nhau.
    </Card>
    <Card title="Xây dựng với AI" icon="rocket">
      Một MCP server đi kèm với plugin để trợ lý AI của bạn có thể tạo và kết nối các dự án cho bạn.
    </Card>
    <Card title="Type-safe, từ đầu đến cuối" icon="seti:json">
      Các kiểu dữ liệu được chia sẻ từ API của bạn đến frontend. Refactor với sự tự tin.
    </Card>
    <Card title="Mã của bạn, theo cách của bạn" icon="setting">
      Một khởi đầu nhanh, không phải một framework — mã nguồn là của bạn. Và{" "}
      <Link path="get_started/upgrading">migrations</Link> tiếp tục mang các cải tiến từ upstream đến nó: một template bạn sở hữu, được cập nhật như một thư viện.
    </Card>
  </CardGrid>
</div>

<div class="landing-section">
  <h2 class="landing-heading">Cảm ơn những người đóng góp tuyệt vời của chúng tôi!</h2>
  <ContributorList githubRepo="awslabs/nx-plugin-for-aws" />
</div>
