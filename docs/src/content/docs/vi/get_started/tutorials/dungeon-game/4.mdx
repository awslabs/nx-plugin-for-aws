---
title: "Xây dựng giao diện người dùng"
description: "Hướng dẫn chi tiết cách xây dựng trò chơi phiêu lưu hầm ngục được hỗ trợ bởi AI tác nhân sử dụng @aws/nx-plugin."
---

import { Aside, Code, FileTree, Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';
import Link from '@components/link.astro';
import Drawer from '@components/drawer.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';
import PackageManagerShortCommand from '@components/package-manager-short-command.astro';
import InstallCommand from '@components/install-command.astro';
import E2ECode from '@components/e2e-code.astro';
import E2EDiff from '@components/e2e-diff.astro';

import dungeonAdventureArchitecturePng from '@assets/dungeon-game-architecture.png'
import baselineWebsitePng from '@assets/baseline-website.png'
import baselineGamePng from '@assets/baseline-game.png'
import nxGraphPng from '@assets/nx-graph.png'
import gameSelectPng from '@assets/game-select.png'
import gameConversationPng from '@assets/game-conversation.png'

## Nhiệm vụ 1: Cấu hình máy chủ phát triển cục bộ

Tải file `runtime-config.json` đã triển khai về cục bộ để máy chủ phát triển có thể giao tiếp với backend thực:

<NxCommands commands={["run game-ui:load:runtime-config"]} />

Sau đó khởi động máy chủ phát triển:

<NxCommands commands={["serve game-ui"]} />

Mở trang web cục bộ của bạn trong trình duyệt, đăng nhập và làm theo các hướng dẫn để tạo người dùng mới. Bạn sẽ thấy trang web Shadcn cơ bản:

<Image src={baselineWebsitePng} alt="baseline-website.png" width="800" height="600" />

<Aside type="caution" title="Giữ server phát triển chạy">
Để máy chủ phát triển chạy trong suốt phần còn lại của module này — nó sẽ tự động tải lại mọi thay đổi bên dưới.
</Aside>

## Nhiệm vụ 2: Nơi CopilotKit đã được kết nối

Khi bạn chạy generator `connection` cho `game-ui → story` trong <Link path="get_started/tutorials/dungeon-game/1">Module 1</Link>, tích hợp AG-UI của trang web Shadcn đã được tạo cho bạn. Đáng để xem qua nhanh:

<FileTree>
- packages/game-ui/src/
  - components/
    - AguiProvider.tsx Single `CopilotKitProvider` registered with every AG-UI agent.
    - copilot/
      - index.tsx Re-exports themed `CopilotChat` / `CopilotSidebar` / `CopilotPopup`.
      - ShadcnAssistantMessage.tsx, ShadcnUserMessage.tsx, ShadcnChatInput.tsx, ShadcnCursor.tsx, copilot.css
  - hooks/
    - useAguiStoryAgent.tsx Instantiates an `@ag-ui/client` `HttpAgent` pointing at the deployed Story Agent and pads `threadId` to AgentCore's 33-character minimum session id.
  - main.tsx Wraps `<App />` in `<AguiProvider>`
</FileTree>

Tất cả những gì chúng ta cần làm là thêm `<CopilotChat agentId="agent" threadId={...} />` vào một route. Để biết thêm chi tiết về cách tích hợp được kết hợp, hãy xem <Link path="guides/connection/react-agui">hướng dẫn kết nối React → AG-UI</Link>.

## Nhiệm vụ 3: Thay đổi giao diện cho hầm ngục

Thay thế `packages/game-ui/src/styles.css` — đây là file *duy nhất* chúng ta thay đổi cho việc tạo kiểu. Nó import các global Shadcn được chia sẻ, ghi đè bảng màu thành theme hầm ngục được thắp sáng bởi ngọn đuốc, và làm cho CopilotKit kế thừa các màu đó (nếu không CopilotKit sẽ đặt lại `--background` và các biến khác bên trong bề mặt chat của nó):

<E2ECode lang="css" path="dungeon-adventure/4/styles.css.template" />

## Nhiệm vụ 4: Tạo các route của trò chơi

Chúng ta cần hai route — một để chọn nhân vật, một để chơi. Cả hai đều sử dụng các component shadcn và chat CopilotKit; không có giao diện chat tự viết.

<Tabs>
<TabItem label="routes/index.tsx">
<E2EDiff before="dungeon-adventure/4/routes/index.tsx.old.template" after="dungeon-adventure/4/routes/index.tsx.template" lang="tsx" />

Đây là bộ chọn trò chơi: form trò chơi mới (shadcn `Input` + `Button` + `Card`) cộng với danh sách "Tiếp tục" được cung cấp bởi `useGameApi().games.query`. Bắt đầu một trò chơi sẽ `saveGame` cặp `(playerName, genre)` (để nó hiển thị lần sau) và điều hướng đến route chơi.
</TabItem>
<TabItem label="routes/game/$playerName.tsx">
<E2ECode path="dungeon-adventure/4/routes/game/$playerName.tsx.template" lang="tsx" />

Đây là route chơi. Nó xây dựng `threadId` xác định (``{player}-{genre}`` được đệm đến 33 ký tự — hook AG-UI sẽ gửi nó nguyên văn làm session id của AgentCore), render `<CopilotChat agentId="agent" threadId={threadId} />`, và phủ lên trên inventory từ `useGameApi().inventory.query`. Trên một thread mới (chưa có tin nhắn được lưu trữ), route sử dụng `useAgent` để gửi một tin nhắn khởi tạo với tên người chơi và thể loại; system prompt của agent yêu cầu nó sử dụng điều đó để thiết lập bối cảnh. Các lần truy cập tiếp theo sẽ truy cập `FileSessionManager` của agent, phát lại lịch sử nên `useEffect` khởi tạo không thực hiện gì.

<Aside type="tip" title="threadId xác định làm cho /game/$playerName?genre=... có thể chia sẻ">
Truy cập lại cùng một URL — dù sau khi tải lại, triển khai lại, hoặc trên thiết bị khác — sẽ phân giải thành cùng một `threadId`, vì vậy agent tiếp tục kể cùng một câu chuyện.
</Aside>

<Aside type="tip" title="Tham số đường dẫn">
Cú pháp `$playerName` yêu cầu `@tanstack/react-router` xử lý `playerName` như một [path param](https://tanstack.com/router/v1/docs/framework/react/guide/path-params). `validateSearch` làm cho `genre` được định kiểu mạnh theo enum của chúng ta.
</Aside>
</TabItem>
</Tabs>

Bạn có thể xóa thư mục `packages/game-ui/src/routes/welcome` được tạo sẵn — chúng ta đang sử dụng `/` làm bộ chọn trò chơi.

Sau khi lưu, máy chủ phát triển tại `http://localhost:4200/` giờ đây sẽ cho phép bạn bắt đầu một cuộc phiêu lưu và trò chuyện với Story Agent.

<Drawer title="Build và Deploy" trigger="Bạn cũng có thể build & deploy code của mình lên CloudFront nếu muốn.">

## Nhiệm vụ 5: Build và deploy

### Build code của bạn
<PackageManagerShortCommand commands={["build"]} />

### Deploy ứng dụng của bạn
<NxCommands commands={['deploy infra dungeon-adventure-infra-sandbox/*']} />

Sau khi deploy, hãy điều hướng đến URL CloudFront của bạn (`GameUIDistributionDomainName` từ các đầu ra CDK).

</Drawer>

<Image src={gameSelectPng} alt="game-select.png" width="500" height="400" />
<div style="margin-top: -100px; margin-left: 100px;">
<Image src={gameConversationPng} alt="game-conversation.png" width="500" height="400" />
</div>

Chúc mừng. Bạn đã xây dựng và triển khai Trò chơi Phiêu lưu Hầm ngục với Agent của mình!  🎉🎉🎉