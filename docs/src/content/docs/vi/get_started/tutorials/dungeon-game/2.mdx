---
title: "Triển khai Game API và Inventory MCP server"
description: "Hướng dẫn chi tiết cách xây dựng trò chơi phiêu lưu hầm ngục được hỗ trợ bởi AI agentic sử dụng @aws/nx-plugin."
---

import { Aside, Code, FileTree, Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';
import Link from '@components/link.astro';
import Drawer from '@components/drawer.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';
import PackageManagerShortCommand from '@components/package-manager-short-command.astro';
import InstallCommand from '@components/install-command.astro';
import E2EDiff from '@components/e2e-diff.astro';
import E2ECode from '@components/e2e-code.astro';
import Snippet from '@components/snippet.astro';

import dungeonAdventureArchitecturePng from '@assets/dungeon-game-architecture.png'
import baselineWebsitePng from '@assets/baseline-website.png'
import baselineGamePng from '@assets/baseline-game.png'
import nxGraphPng from '@assets/nx-graph.png'
import gameSelectPng from '@assets/game-select.png'
import gameConversationPng from '@assets/game-conversation.png'

import { getDungeonAdventureElectroDbDependencies } from '../../../../../../../../e2e/src/utils';

## Nhiệm vụ 1: Triển khai Game API

Chúng ta sẽ triển khai các API sau trong phần này:

1. `saveGame` - tạo hoặc cập nhật một trò chơi.
2. `queryGames` - trả về danh sách phân trang các trò chơi đã lưu trước đó.
3. `queryInventory` - trả về danh sách phân trang các vật phẩm trong kho của người chơi.
4. `queryActions` - trả về lịch sử hội thoại cho một trò chơi nhất định.

:::note[Nơi lưu trữ từng phần trạng thái]
- `games` và `inventory` được lưu trong **DynamoDB** (thông qua ElectroDB) — các lambda của Game API và Inventory MCP server đọc/ghi chúng.
- `actions` (bản ghi trò chuyện) được lưu trong **S3** — `S3SessionManager` của Story Agent ghi một blob JSON cho mỗi lượt chơi, và `queryActions` trong Game API đọc chúng lại để UI có thể phát lại lịch sử khi tải lại trang. Không có thủ tục `saveAction`; agent là người ghi duy nhất.
:::

### Schema API

Để định nghĩa đầu vào và đầu ra của API, hãy tạo schema của chúng ta bằng [Zod](https://zod.dev/) trong file `packages/game-api/src/schema/index.ts` như sau:

<E2EDiff lang="typescript" before="dungeon-adventure/2/schema/index.ts.old.template" after="dungeon-adventure/2/schema/index.ts.template" />

Xóa file `packages/game-api/src/schema/echo.ts` vì chúng ta sẽ không sử dụng nó trong dự án này.

<Aside type="tip" title="Suy luận kiểu tự động">
Đối với mỗi schema chúng ta định nghĩa trong Zod, chúng ta cũng xuất một interface bằng cách sử dụng cú pháp `z.TypeOf`. Điều này chuyển đổi định nghĩa Zod của chúng ta thành một interface Typescript mà không cần phải trùng lặp công sức.
</Aside>

### Mô hình hóa thực thể

Đây là sơ đồ ER cho ứng dụng của chúng ta.

```d2
direction: right

game: Game {
  shape: sql_table
  playerName: "string  PK"
  genre: string
  lastUpdated: string
}

item: Item {
  shape: sql_table
  playerName: "string  PK"
  itemName: "string  SK"
  emoji: "string  (optional)"
  lastUpdated: string
  quantity: number
}

game -> item: 1..*
```

Generator `ts#dynamodb` đã thiết lập [ElectroDB](https://electrodb.dev/en/core-concepts/introduction/), mà chúng ta sẽ sử dụng để mô hình hóa dữ liệu. Chúng ta sẽ lưu trữ lịch sử hội thoại trong S3, vì vậy chúng ta thêm dependency vào S3 client:

<InstallCommand pkg={getDungeonAdventureElectroDbDependencies()} project="@dungeon-adventure/game-api" />

<Aside title="Phụ thuộc dự án">
S3 client được thêm vào `package.json` riêng của dự án `game-api`, vì mã nguồn của dự án đó import nó. Các phiên bản vẫn được căn chỉnh trên toàn workspace thông qua dependency catalog của package manager ([single version policy](https://nx.dev/concepts/decisions/dependency-management#single-version-policy)). Để biết thêm thông tin, tham khảo <Link path="guides/typescript-project#dependencies">hướng dẫn ts#project</Link>.
</Aside>

Thay thế entity mẫu được tạo trong `packages/dungeon-db/src/entities/index.ts` bằng các entities `Game` và `Inventory` của chúng ta, và xóa `packages/dungeon-db/src/entities/example.ts`:

<E2ECode lang="typescript" path="dungeon-adventure/2/dungeon-db/entities/index.ts.template" />

ElectroDB cho phép chúng ta không chỉ định nghĩa các kiểu của mình mà còn có thể cung cấp giá trị mặc định cho các giá trị nhất định như timestamps. Ngoài ra, ElectroDB tuân theo [single-table design](https://electrodb.dev/en/core-concepts/single-table-relationships/) là best practice khi sử dụng DynamoDB.

<Aside title="Bộ sưu tập ElectroDB">
Mặc dù ElectroDB hỗ trợ [collections](https://electrodb.dev/en/modeling/collections/), chúng ta đã chọn không sử dụng chúng trong hướng dẫn này để đơn giản.
</Aside>

### Định nghĩa các procedures

Để triển khai các phương thức API, thực hiện các thay đổi sau trong `packages/game-api/src/procedures`:

<Tabs>
  <TabItem label="games.ts">
<E2ECode lang="typescript" path="dungeon-adventure/2/procedures/games.ts.template" />
  </TabItem>
  <TabItem label="inventory.ts">
<E2ECode lang="typescript" path="dungeon-adventure/2/procedures/inventory.ts.template" />
  </TabItem>
  <TabItem label="actions.ts">
<E2ECode lang="typescript" path="dungeon-adventure/2/procedures/actions.ts.template" />
  </TabItem>
</Tabs>

Xóa file `echo.ts` (từ `packages/game-api/src/procedures`) vì chúng ta sẽ không sử dụng nó trong dự án này.

### Thiết lập Router

Sau khi chúng ta định nghĩa các procedures, để kết nối chúng vào API của chúng ta, cập nhật file sau:

<Tabs>
  <TabItem label="packages/game-api/src/router.ts">
<E2EDiff lang="typescript" before="dungeon-adventure/2/router.ts.old.template" after="dungeon-adventure/2/router.ts.template" />
</TabItem>
</Tabs>

## Nhiệm vụ 2: Tạo Inventory MCP server

Hãy tạo một MCP server cho phép agent của chúng ta quản lý các vật phẩm trong kho của người chơi.

Chúng ta sẽ định nghĩa các công cụ sau cho agent của chúng ta:

- `list-inventory-items` để lấy các vật phẩm kho hiện tại của người chơi
- `add-to-inventory` để thêm vật phẩm vào kho của người chơi
- `remove-from-inventory` để xóa vật phẩm khỏi kho của người chơi

Để tiết kiệm thời gian, chúng ta sẽ định nghĩa tất cả các công cụ inline:

<Tabs>
  <TabItem label="packages/inventory/src/mcp-server/server.ts">
<E2EDiff lang="typescript" before="dungeon-adventure/2/mcp/server.ts.old.template" after="dungeon-adventure/2/mcp/server.ts.template" />
</TabItem>
</Tabs>

Khi số lượng công cụ tăng lên, bạn có thể tái cấu trúc chúng thành các file riêng biệt nếu muốn.

Xóa các thư mục `tools` và `resources` trong `packages/inventory/src/mcp-server` vì chúng sẽ không được sử dụng.

## Nhiệm vụ 3: Cập nhật cơ sở hạ tầng

Construct `DungeonDb` được tạo bởi `ts#dynamodb` đã cung cấp bảng của chúng ta, vì vậy chúng ta chỉ cần khởi tạo nó trong stack và cấp cho Game API và Inventory MCP server các quyền mà chúng cần. Cập nhật `packages/infra/src/stacks/application-stack.ts` như sau:

<E2EDiff lang="typescript" before="dungeon-adventure/1/application-stack.ts.template" after="dungeon-adventure/2/stacks/application-stack.ts.template" />

:::note[Thủ tục cô lập]
Lưu ý ở đây rằng vì mỗi procedure được phục vụ bởi một lambda function riêng lẻ, chúng ta có thể tuân theo nguyên tắc least privilege và chỉ gán các quyền đọc/ghi cần thiết dựa trên triển khai của procedure.
:::

:::tip[Cấu hình runtime]
Construct `DungeonDb` đăng ký tên bảng đã triển khai trong <Link path="guides/runtime-config">Runtime Configuration</Link> dưới namespace `dynamodb`, được lưu trữ trong AWS AppConfig. Game API và Inventory MCP server lấy nó tại runtime bằng Lambda Powertools, thay vì cần truyền nó như một biến môi trường. Lợi ích của cách tiếp cận này là khi số lượng bảng hoặc các định danh cơ sở hạ tầng khác tăng lên, bạn chỉ cần một biến môi trường `RUNTIME_CONFIG_APP_ID` duy nhất khi chạy với cơ sở hạ tầng đã triển khai.
:::

## Nhiệm vụ 4: Kiểm thử Game API locally

Không cần triển khai lên AWS để thử API của chúng ta — target `serve-local` chạy Game API với [DynamoDB Local](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.html). Vì chúng ta đã kết nối Game API với dự án `DungeonDb` trong Module 1, target này cũng tự động khởi động DynamoDB Local.

Đầu tiên, sửa mọi vấn đề lint:

<PackageManagerShortCommand commands={["lint"]} />

Sau đó build codebase:

<PackageManagerShortCommand commands={["build"]} />

### Khởi động local server

Khởi động Game API locally với target `dev`, cũng khởi động DynamoDB Local:

<NxCommands commands={["dev game-api"]} />

<Aside type="tip" title="Lần chạy đầu tiên">
Lần chạy đầu tiên sẽ pull container image DynamoDB Local, có thể mất một chút thời gian. Để server chạy trong terminal này và mở terminal thứ hai cho các lệnh `curl` bên dưới.
</Aside>

### Kiểm thử API

Khi server của bạn đã chạy, truy vấn danh sách trò chơi (rỗng):

```bash
curl -X GET 'http://localhost:2022/games.query?input=%7B%7D'
```

:::note[Mã hóa URL]
`%7B%7D` mà chúng ta truyền để kiểm thử API là một đối tượng JSON rỗng được mã hóa URI (`{}`).
:::

Bạn sẽ thấy một danh sách rỗng:

```json
{"result":{"data":{"items":[],"cursor":null}}}
```

Bây giờ lưu một trò chơi:

```bash
curl -X POST 'http://localhost:2022/games.save' \
  -H 'Content-Type: application/json' \
  -d '{"playerName":"Alice","genre":"zombie"}'
```

Lệnh save trả về trò chơi đã được lưu (với timestamp `lastUpdated` mà entity đặt cho bạn):

```json
{"result":{"data":{"playerName":"Alice","genre":"zombie","lastUpdated":"..."}}}
```

Truy vấn lại để xác nhận nó đã được lưu trong DynamoDB Local:

```bash
curl -X GET 'http://localhost:2022/games.query?input=%7B%7D'
```

Phản hồi này bây giờ bao gồm trò chơi đã lưu:

```json
{"result":{"data":{"items":[{"playerName":"Alice","genre":"zombie","lastUpdated":"..."}],"cursor":null}}}
```

Bạn có thể dừng local server (`Ctrl+C`) khi đã xong.

## Nhiệm vụ 5: Kiểm thử Inventory MCP server locally

Chúng ta có thể thử các công cụ của MCP server với [MCP Inspector](https://github.com/modelcontextprotocol/inspector) bằng cách sử dụng target `mcp-server-inspect` được tạo:

<NxCommands commands={["mcp-server-inspect inventory"]} />

Điều này phục vụ MCP server locally (cũng khởi động DynamoDB Local) và khởi chạy MCP Inspector tại `http://localhost:6274` được cấu hình sẵn để kết nối với nó. Nhấp **Connect**, chuyển sang tab **Tools**, nhấp **List Tools**, và thử `add-to-inventory` (ví dụ: `playerName: Alice`, `itemName: Rusty Sword`, `emoji: ⚔️`) theo sau là `list-inventory-items` để xem nó được lưu vào DynamoDB Local. Dừng server (`Ctrl+C`) khi đã xong.

Chúc mừng, bạn đã xây dựng và kiểm thử API tRPC đầu tiên và MCP server với bảng DynamoDB local!  🎉🎉🎉