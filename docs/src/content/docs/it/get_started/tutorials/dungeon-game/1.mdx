---
title: "Gioco di Dungeon con IA"
description: "Una guida dettagliata su come costruire un gioco di avventura dungeon alimentato da IA utilizzando il plugin @aws/nx-plugin."
---



import { Aside, Code, FileTree, Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';
import Link from '@components/link.astro';
import Drawer from '@components/drawer.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';
import InstallCommand from '@components/install-command.astro';
import CreateNxWorkspaceCommand from '@components/create-nx-workspace-command.astro';
import E2EDiff from '@components/e2e-diff.astro';

import dungeonAdventureArchitecturePng from '@assets/dungeon-game-architecture.png'
import dungeonAdventureErPng from '@assets/dungeon-adventure-er.png'
import baselineWebsitePng from '@assets/baseline-website.png'
import baselineGamePng from '@assets/baseline-game.png'
import nxGraphPng from '@assets/nx-graph.png'
import gameSelectPng from '@assets/game-select.png'
import gameConversationPng from '@assets/game-conversation.png'

## Modulo 1: Configurazione del monorepo

Iniziamo creando un nuovo monorepo. Dal percorso desiderato, esegui il seguente comando:

<CreateNxWorkspaceCommand workspace="dungeon-adventure" />

Questo configurerà un monorepo NX all'interno della directory `dungeon-adventure` che potrai aprire in VSCode. Dovrebbe apparire come segue:

<FileTree>
- .nx/
- .vscode/
- node_modules/
- packages/ qui risiederanno i tuoi sotto-progetti
- .gitignore
- .npmrc
- .prettierignore
- .prettierrc
- nx.json configura la CLI NX e le impostazioni predefinite del monorepo
- package.json tutte le dipendenze Node sono definite qui
- pnpm-lock.yaml o bun.lock, yarn.lock, package-lock.json in base al package manager
- pnpm-workspace.yaml se si utilizza pnpm
- README.md
- tsconfig.base.json tutti i sotto-progetti Node estendono questo file
- tsconfig.json
</FileTree>

Per iniziare ad aggiungere componenti dal plugin `@aws/nx-plugin` nel monorepo, dobbiamo installarlo come dipendenza di sviluppo eseguendo questo comando dalla root del monorepo `dungeon-adventure`:

<InstallCommand dev pkg="@aws/nx-plugin" />

Ora siamo pronti per creare i nostri sotto-progetti utilizzando il plugin `@aws/nx-plugin`.

<Aside type="tip">È una buona pratica assicurarsi che tutti i file non inseriti nello staging siano commitati in Git prima di eseguire qualsiasi generatore. Questo permette di visualizzare le modifiche dopo l'esecuzione del generatore tramite `git diff`</Aside>

### API del Gioco

Iniziamo creando la nostra Game API. Creiamo un'API tRPC chiamata `GameApi` seguendo questi passaggi:

<RunGenerator generator="ts#trpc-api" requiredParameters={{apiName:"GameApi"}} noInteractive />

<br />

Dovresti vedere nuovi file apparire nel tuo albero delle directory.

<Aside>
Il `package.json` principale è ora configurato con `type` impostato a `module`, il che significa che ESM è il tipo di modulo predefinito per tutti i sotto-progetti Node gestiti dal plugin `@aws/nx-plugin`. Per dettagli sui progetti TypeScript, consulta la <Link path="guides/typescript-project">guida ts#project</Link>.
</Aside>

<Drawer title="File aggiornati da ts#trpc-api" trigger="Clicca qui per esaminare questi file nel dettaglio.">
Ecco l'elenco dei file generati dal generatore `ts#trpc-api`. Esamineremo alcuni file chiave evidenziati:

<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - app/ componenti CDK specifici per l'applicazione
          - apis/
            - **game-api.ts** costrutto CDK per creare l'API tRPC
            - index.ts
            - ...
          - index.ts
        - core/ componenti CDK generici
          - api/
            - rest-api.ts costrutto base per API Gateway Rest API
            - trpc-utils.ts utility per costrutti CDK tRPC
            - utils.ts utility per i costrutti API
          - index.ts
          - runtime-config.ts
        - index.ts
      - project.json
      - ...
    - types/ tipi condivisi
      - src/
        - index.ts
        - runtime-config.ts definizione dell'interfaccia usata da CDK e sito web
      - project.json
      - ...
  - game-api/
    - backend/ codice di implementazione tRPC
      - src/
        - client/ client vanilla per chiamate machine-to-machine
          - index.ts
          - sigv4.ts
        - middleware/ strumentazione Powertools
          - error.ts
          - index.ts
          - logger.ts
          - metrics.ts
          - tracer.ts
        - procedures/ implementazioni specifiche delle procedure/route API
          - **echo.ts**
        - index.ts
        - init.ts configura contesto e middleware
        - local-server.ts utilizzato per eseguire il server tRPC localmente
        - **router.ts** punto di ingresso per il lambda handler che definisce tutte le procedure
      - project.json
      - ...
    - schema/
      - src/
        - procedures/
          - **echo.ts**
        - index.ts
      - project.json
      - ...
- eslint.config.mjs
- vitest.workspace.ts
</FileTree>

Esaminiamo alcuni file chiave:

```ts {5,12}
// packages/game-api/backend/src/router.ts
import {
  awsLambdaRequestHandler,
  CreateAWSLambdaContextOptions,
} from '@trpc/server/adapters/aws-lambda';
import { echo } from './procedures/echo.js';
import { t } from './init.js';
import { APIGatewayProxyEvent } from 'aws-lambda';

export const router = t.router;

export const appRouter = router({
  echo,
});

export const handler = awsLambdaRequestHandler({
  router: appRouter,
  createContext: (
    ctx: CreateAWSLambdaContextOptions<APIGatewayProxyEvent>,
  ) => ctx,
  responseMeta: () => ({
    headers: {
      'Access-Control-Allow-Origin': '*',
      'Access-Control-Allow-Methods': '*',
    },
  }),
});

export type AppRouter = typeof appRouter;
```
Il router definisce il punto di ingresso per la tua API tRPC, dove dichiarerai tutti i metodi. Nell'esempio, vediamo un metodo `echo` la cui implementazione risiede in `./procedures/echo.ts`.

```ts {2-5}
// packages/game-api/backend/src/procedures/echo.ts
import { publicProcedure } from '../init.js';
import {
  EchoInputSchema,
  EchoOutputSchema,
} from ':dungeon-adventure/game-api-schema';

export const echo = publicProcedure
  .input(EchoInputSchema)
  .output(EchoOutputSchema)
  .query((opts) => ({ result: opts.input.message }));
```

Questo file implementa il metodo `echo`, con input e output tipizzati importati dal progetto `:dungeon-adventure/game-api-schema` tramite [alias TypeScript](https://www.typescriptlang.org/tsconfig/paths.html).

<Aside type="tip">Se vedi errori di importazione nell'IDE, è perché il backend non ha ancora riferimenti TypeScript configurati nel suo tsconfig.json. Nx crea questi riferimenti [dinamicamente](https://nx.dev/nx-api/js/generators/typescript-sync) durante le operazioni di build. Per maggiori dettagli, consulta la <Link path="guides/typescript-project#importing-your-library-code-inother-projects">guida TypeScript</Link>.</Aside>

```ts
// packages/game-api/schema/src/procedures/echo.ts
import { z } from 'zod';

export const EchoInputSchema = z.object({
  message: z.string(),
});

export type IEchoInput = z.TypeOf<typeof EchoInputSchema>;

export const EchoOutputSchema = z.object({
  result: z.string(),
});

export type IEchoOutput = z.TypeOf<typeof EchoOutputSchema>;
```

Tutte le definizioni degli schemi tRPC utilizzano [Zod](https://zod.dev/), con tipi TypeScript generati tramite `z.TypeOf`.

```ts
// packages/common/constructs/src/app/apis/game-api.ts
import { Construct } from 'constructs';
import * as url from 'url';
import {
  Code,
  Runtime,
  Function,
  FunctionProps,
  Tracing,
} from 'aws-cdk-lib/aws-lambda';
import {
  AuthorizationType,
  Cors,
  LambdaIntegration,
} from 'aws-cdk-lib/aws-apigateway';
import { Duration, Stack } from 'aws-cdk-lib';
import {
  PolicyDocument,
  PolicyStatement,
  Effect,
  AccountPrincipal,
  AnyPrincipal,
} from 'aws-cdk-lib/aws-iam';
import {
  IntegrationBuilder,
  RestApiIntegration,
} from '../../core/api/utils.js';
import { RestApi } from '../../core/api/rest-api.js';
import { Procedures, routerToOperations } from '../../core/api/trpc-utils.js';
import { AppRouter, appRouter } from ':dungeon-adventure/game-api-backend';

type Operations = Procedures<AppRouter>;

export interface GameApiProps<
  TIntegrations extends Record<Operations, RestApiIntegration>,
> {
  integrations: TIntegrations;
}

export class GameApi<
  TIntegrations extends Record<Operations, RestApiIntegration>,
> extends RestApi<Operations, TIntegrations> {
  public static defaultIntegrations = (scope: Construct) => {
    return IntegrationBuilder.rest({
      operations: routerToOperations(appRouter),
      defaultIntegrationOptions: {
        runtime: Runtime.NODEJS_LATEST,
        handler: 'index.handler',
        code: Code.fromAsset(
          url.fileURLToPath(
            new URL(
              '../../../../../../dist/packages/game-api/backend/bundle',
              import.meta.url,
            ),
          ),
        ),
        timeout: Duration.seconds(30),
        tracing: Tracing.ACTIVE,
        environment: {
          AWS_CONNECTION_REUSE_ENABLED: '1',
        },
      } satisfies FunctionProps,
      buildDefaultIntegration: (op, props: FunctionProps) => {
        const handler = new Function(scope, `GameApi${op}Handler`, props);
        return { handler, integration: new LambdaIntegration(handler) };
      },
    });
  };

  constructor(
    scope: Construct,
    id: string,
    props: GameApiProps<TIntegrations>,
  ) {
    super(scope, id, {
      apiName: 'GameApi',
      defaultMethodOptions: {
        authorizationType: AuthorizationType.IAM,
      },
      defaultCorsPreflightOptions: {
        allowOrigins: Cors.ALL_ORIGINS,
        allowMethods: Cors.ALL_METHODS,
      },
      policy: new PolicyDocument({
        statements: [
          new PolicyStatement({
            effect: Effect.ALLOW,
            principals: [new AccountPrincipal(Stack.of(scope).account)],
            actions: ['execute-api:Invoke'],
            resources: ['execute-api:/*'],
          }),
          new PolicyStatement({
            effect: Effect.ALLOW,
            principals: [new AnyPrincipal()],
            actions: ['execute-api:Invoke'],
            resources: ['execute-api:/*/OPTIONS/*'],
          }),
        ],
      }),
      operations: routerToOperations(appRouter),
      ...props,
    });
  }
}
```

Questo costrutto CDK definisce la GameApi. Il metodo `defaultIntegrations` crea automaticamente una funzione Lambda per ogni procedura tRPC, puntando al bundle già generato durante la build del progetto.

</Drawer>

### API della Storia

Ora creiamo la Story API. Segui questi passaggi per creare un'API Fast chiamata `StoryApi`:

<RunGenerator generator="py#fast-api" requiredParameters={{name:"StoryApi"}} noInteractive />

Nuovi file dovrebbero apparire nel tuo albero delle directory.
<Drawer title="File aggiornati da py#fast-api" trigger="Clicca qui per esaminare questi file nel dettaglio.">
Ecco i file chiave generati dal generatore `py#fast-api`:

<FileTree>
- .venv/ ambiente virtuale unico per il monorepo
- packages/
  - common/
    - constructs/
      - src/
        - app/
          - apis/
            - **story-api.ts** costrutto CDK per l'API Fast
            - index.ts aggiornato per esportare la nuova API
      - project.json aggiornato con dipendenza da story_api
    - types/
      - src/
        - **runtime-config.ts** aggiornato per aggiungere StoryApi
  - story_api/
    - story_api/ modulo Python
      - init.py configura Powertools, FastAPI e middleware
      - **main.py** entrypoint Lambda con tutte le route
    - tests/
    - .python-version
    - project.json
    - pyproject.toml
- .python-version versione Python bloccata
- pyproject.toml
- uv.lock
</FileTree>

```ts
// packages/common/constructs/src/app/apis/story-api.ts
// Implementazione CDK della StoryApi simile alla GameApi
// (codice omesso per brevità, mantiene stessa struttura con adattamenti Python)
```

Questo costrutto CDK definisce la StoryApi con integrazioni Lambda predefinite.

```diff lang="ts"
// packages/common/types/src/runtime-config.ts
export type ApiUrl = string;
export interface IRuntimeConfig {
  apis: {
    GameApi: ApiUrl;
+    StoryApi: ApiUrl;
  };
}
```

Il generatore ha aggiornato l'interfaccia `IRuntimeConfig` aggiungendo `StoryApi`, garantendo type safety nel frontend.

```py
// packages/story_api/story_api/main.py
from .init import app, lambda_handler, tracer

handler = lambda_handler

@app.get("/")
@tracer.capture_method
def read_root():
    return {"Hello": "World"}
```

Qui definisci le route API usando [Pydantic](https://docs.pydantic.dev/latest/) per la type safety.

</Drawer>

### Interfaccia Utente del Gioco: Sito Web

Creiamo l'interfaccia utente per interagire con il gioco. Generiamo un sito web chiamato `GameUI`:

<RunGenerator generator="ts#cloudscape-website" requiredParameters={{name:"GameUI"}} noInteractive />

Nuovi file dovrebbero apparire nel tuo albero.
<Drawer title="File aggiornati da ts#cloudscape-website" trigger="Clicca qui per esaminare nel dettaglio.">
Struttura chiave generata:

<FileTree>
- packages/
  - game-ui/
    - public/
    - src/
      - routes/ routing basato su file con @tanstack/react-router
        - welcome/
          - **index.tsx**
        - ...
    - ...
</FileTree>

```ts
// packages/common/constructs/src/app/static-websites/game-ui.ts
// Costrutto CDK per il sito statico che punta al bundle Vite
```

```tsx
// packages/game-ui/src/main.tsx
// Entrypoint React con configurazione router
```

```tsx
// packages/game-ui/src/routes/welcome/index.tsx
// Componente per la route /welcome
```

</Drawer>

### Interfaccia Utente del Gioco: Autenticazione

Aggiungiamo l'autenticazione via Amazon Cognito:

<RunGenerator generator="ts#cloudscape-website#auth" requiredParameters={{cognitoDomain:"game-ui", project:"@dungeon-adventure/game-ui", allowSignup:true}} noInteractive />

<Drawer title="File aggiornati per l'autenticazione" trigger="Dettagli delle modifiche">
```diff lang="tsx"
// packages/game-ui/src/main.tsx
+ Componenti aggiunti per Cognito e RuntimeConfig
```

</Drawer>

### Collegamento UI a Story API

Colleghiamo la UI alla Story API:

<RunGenerator generator="api-connection" requiredParameters={{sourceProject:"@dungeon-adventure/game-ui", targetProject:"dungeon_adventure.story_api"}} noInteractive />

<Drawer title="Collegamento UI-FastAPI" trigger="Dettagli implementazione">
```tsx
// packages/game-ui/src/hooks/useStoryApiClient.tsx
// Hook per chiamate autenticate alla StoryApi
```

</Drawer>

### Collegamento UI a Game API

Colleghiamo la UI alla Game API:

<RunGenerator generator="api-connection" requiredParameters={{sourceProject:"@dungeon-adventure/game-ui", targetProject:"@dungeon-adventure/game-api-backend"}} noInteractive />

<Drawer title="Collegamento UI-tRPC" trigger="Dettagli implementazione">
```tsx
// packages/game-ui/src/hooks/useGameApi.tsx
// Hook tRPC con integrazione React Query
```

</Drawer>

### Infrastruttura del Gioco

Creiamo l'infrastruttura CDK finale:

<RunGenerator generator="ts#infra" requiredParameters={{name:"infra"}} noInteractive />

<Drawer title="Infrastruttura CDK" trigger="Dettagli implementazione">
```ts
// packages/infra/src/main.ts
// Configurazione CDK con validazione cfn-guard
```

```ts
// packages/infra/src/stacks/application-stack.ts
// Stack CDK per le risorse dell'applicazione
```

</Drawer>

#### Aggiornare l'infrastruttura

Aggiorniamo `application-stack.ts` per istanziare i costrutti:

<E2EDiff before="dungeon-adventure/1/application-stack.ts.original.template" after="dungeon-adventure/1/application-stack.ts.template" lang="ts" />

### Compilazione del codice

<Drawer title="Comandi NX" trigger="Guida ai comandi di build">
```bash
# Comandi per build singola/multipla e gestione cache
<NxCommands commands={['run @dungeon-adventure/infra:build']} />
<NxCommands commands={['graph']} />
```

</Drawer>

<NxCommands commands={['run-many --target build --all']} />

Durante la build, Nx richiederà di sincronizzare i riferimenti TypeScript. Seleziona **Sì** per risolvere gli errori di importazione.

<Aside type="tip">Per correggere errori lint:
<NxCommands commands={['run-many --target lint --configuration=fix --all']} />
</Aside>

<Aside type="caution" title="Errore build Windows">
<Drawer trigger="Soluzione per errori Windows" title="Disabilita cfn-guard">
```diff lang="ts"
// packages/infra/src/main.ts
- Configurazione cfn-guard rimossa per compatibilità Windows
```
</Drawer>
</Aside>

I file generati sono disponibili in `dist/`. Complimenti! Hai completato la configurazione iniziale. 🎉🎉🎉