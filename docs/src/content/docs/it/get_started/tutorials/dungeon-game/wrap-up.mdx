---
title: "Gioco di Dungeon con IA Agentiva"
description: "Una guida dettagliata su come costruire un gioco di avventura in dungeon basato su IA agentiva utilizzando @aws/nx-plugin."
---

import { Aside, Code, FileTree, Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';
import Drawer from '@components/drawer.astro';
import Link from '@components/link.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';
import InstallCommand from '@components/install-command.astro';
import dungeonAdventureArchitecturePng from '@assets/dungeon-game-architecture.png'
import dungeonAdventureErPng from '@assets/dungeon-adventure-er.png'
import baselineWebsitePng from '@assets/baseline-website.png'
import baselineGamePng from '@assets/baseline-game.png'
import nxGraphPng from '@assets/nx-graph.png'
import gameSelectPng from '@assets/game-select.png'
import gameConversationPng from '@assets/game-conversation.png'

## Conclusione

Complimenti! Hai creato un gioco di avventura Agentic AI Dungeon che utilizza molti dei generatori disponibili nel plugin `@aws/nx-plugin`. 🎉🎉🎉

## E ora?

Ti consigliamo di provare ad estendere il codice con le seguenti funzionalità in autonomia:

1. Aggiungi il <Link path="guides/license">generatore di licenze</Link> al tuo progetto per gestire automaticamente i file LICENSE e gli header nel codice sorgente.
2. <Link path="get_started/tutorials/contribute-generator">Contribuisci con il tuo generatore</Link> per semplificare l'aggiunta di nuove API tRPC.
3. Aggiungi una nuova API `resetToAction` (usando il generatore sopra) in GameAPI che permetta di tornare indietro nel tempo cancellando tutte le azioni da un punto specifico. Aggiorna l'interfaccia per aggiungere un pulsante sui bubble di conversazione che resetti a questo punto (richiamando la nuova API).
4. Esplora le varie guide sui componenti utilizzando la navigazione a sinistra.

## Eliminazione delle risorse

1. Per distruggere le risorse AWS create, esegui questo comando:

<NxCommands commands={['run @dungeon-adventure/infra:destroy dungeon-adventure-infra-sandbox/*']} />

    Ti verrà chiesto di selezionare gli stack da eliminare, che dovrebbero includere `dungeon-adventure-infra-sandbox/Application/GameUI/waf` e `dungeon-adventure-infra-sandbox/Application`. 

2. Digita `Y` per continuare. CloudFormation provvederà a eliminare gli stack.