---
title: "Configurazione del Server MCP"
---



### File di configurazione

La maggior parte degli assistenti AI che supportano MCP utilizza un approccio di configurazione simile. Dovrai creare o aggiornare un file di configurazione con i dettagli del tuo server MCP:

```json
{
  "mcpServers": {
    "your-mcp-server": {
      "command": "uv",
      "args": [
        "run",
        "python",
        "-m",
        "my_module.mcp_server.stdio"
      ],
      "env": {
        "VIRTUAL_ENV": "/path/to/your/project/.venv"
      }
    }
  }
}
```

:::caution
Se ricevi un errore come `ENOENT uv` durante la connessione al server, potrebbe essere necessario specificare il percorso completo di `uv`, che puoi ottenere eseguendo `which uv` nel terminale.
:::

### Configurazione specifica dell'assistente

Consulta la seguente documentazione per configurare MCP con assistenti AI specifici:

import Snippet from '@components/snippet.astro';

<Snippet name="mcp/assistant-docs" />

:::tip
Alcuni assistenti AI, come Amazon Q Developer, consentono di specificare una configurazione del server MCP a livello di workspace, particolarmente utile per definire i server MCP rilevanti per un progetto specifico.
:::