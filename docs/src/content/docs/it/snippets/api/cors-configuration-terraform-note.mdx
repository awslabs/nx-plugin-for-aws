---
title: Configurazione CORS Terraform
---
import Link from '@components/link.astro';

:::note[Configurazione CORS Terraform]
Se la tua soluzione include un modulo Terraform per siti web, puoi utilizzare il suo nome di dominio CloudFront per limitare il CORS.
Dato un nome di dominio CloudFront `<domain_name>`, all'interno del modulo Terraform per il deployment della tua API aggiungi
- una proprietà `cors_allow_origins`, impostata su `["http://localhost:4200", "http://localhost:4300", "https://<domain name>"]`, per le API HTTP. Questo limita il CORS dell'API gateway a questa distribuzione e al localhost.
- una variabile d'ambiente `ALLOWED_ORIGINS`, impostata su `"https://<domain_name>"`, per le API REST. Questo imposta la distribuzione CloudFront come unica origine CORS consentita (oltre al localhost) nelle integrazioni AWS Lambda. Nota che questa restrizione non viene applicata alle richieste preflight OPTIONS - per favore metti un +1 su [questa issue GitHub](https://github.com/awslabs/nx-plugin-for-aws/issues/377) per aiutare a dare priorità alla risoluzione di questo problema.

```hcl {6,9}
module "my_api" {
  source = "../../common/terraform/src/app/apis/my-api"

  asset_bucket_name = module.asset_bucket.bucket_name

  cors_allow_origins = ["http://localhost:4200", "http://localhost:4300", "https://<domain name>"] // Only required for HTTP API

  env = {
    ALLOWED_ORIGINS = "https://<domain name>" // Only required for REST API
    ENVIRONMENT = var.environment
    LOG_LEVEL   = "INFO"
  }
}
```
Il costrutto `MyWebsite` può essere generato utilizzando il <Link path="/guides/react-website">generatore `ts#website`</Link>. Questo esempio presuppone che le porte locali del sito web siano `4200`/`4300` - controlla il suo `project.json` (o `vite.config.mts`) per le porte effettivamente assegnate, poiché ogni sito web generato in questo modo ottiene le proprie.
:::