---
title: Avvio di DynamoDB Locale
---
import NxCommands from '@components/nx-commands.astro';

Il generatore configura un target `serve-local` che avvia un'istanza di [DynamoDB Local](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.html) e crea la tabella. Usa il target `dev` del progetto (un alias per `serve-local`):

<NxCommands commands={['run <project-name>:dev']} />

Questo automaticamente:
1. Scarica l'immagine DynamoDB Local (target `pull-image`)
2. Avvia un container
3. Crea una tabella locale con gli indici definiti in `config.json`
