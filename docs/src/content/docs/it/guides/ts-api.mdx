---
title: API TypeScript
description: Documentazione di riferimento per il generatore di API TypeScript
generator: ts#api
---
import Astro from '@astrojs/react';
import { CardGrid } from '@astrojs/starlight/components';
import GeneratorParameters from '@components/generator-parameters.astro';
import FrameworkCard from '@components/framework-card.astro';

Il generatore di API TypeScript crea un nuovo progetto API con il framework di tua scelta. Supporta [tRPC](https://trpc.io/) per API type-safe end-to-end e [Smithy](https://smithy.io/) per lo sviluppo di API basato su modelli.

## Framework

<CardGrid>
  <FrameworkCard
    title="tRPC"
    description="API type-safe end-to-end con TypeScript. Ideale quando il frontend e il backend condividono una codebase TypeScript."
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/trpc`}
    logo="trpc"
  />
  <FrameworkCard
    title="Smithy"
    description="Sviluppo di API basato su modelli utilizzando un linguaggio di definizione delle interfacce. Ideale per API utilizzate da più linguaggi."
    href={`/nx-plugin-for-aws/${Astro.currentLocale || 'en'}/guides/ts-smithy-api`}
    logo="smithy"
  />
</CardGrid>
