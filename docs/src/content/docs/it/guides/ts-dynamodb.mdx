---
title: DynamoDB
description: Crea un progetto DynamoDB
generator: ts#dynamodb
---

import { FileTree } from '@astrojs/starlight/components';
import Link from '@components/link.astro';
import RunGenerator from '@components/run-generator.astro';
import GeneratorParameters from '@components/generator-parameters.astro';
import Infrastructure from '@components/infrastructure.astro';
import NxCommands from '@components/nx-commands.astro';
import Snippet from '@components/snippet.astro';
import OptionFilter from '@components/option-filter.astro';

Questo generatore crea un nuovo progetto TypeScript basato su [Amazon DynamoDB](https://aws.amazon.com/dynamodb/), utilizzando [ElectroDB](https://electrodb.dev/) per la modellazione di entità type-safe. Genera il codice dell'applicazione e l'infrastruttura necessaria per il provisioning e la gestione di una tabella DynamoDB utilizzando AWS CDK o Terraform, con supporto per il design single-table e sviluppo locale integrato tramite DynamoDB Local.

## Utilizzo

### Genera un Progetto DynamoDB

<RunGenerator generator="ts#dynamodb" />

### Opzioni

<GeneratorParameters generator="ts#dynamodb" />

## Output del Generatore

Il generatore crea la seguente struttura di progetto nella directory `<directory>/<name>`:

<FileTree>
  - scripts
    - create-local-table.ts Crea la tabella DynamoDB nell'istanza locale di DynamoDB Local
    - pull-image.ts Scarica l'immagine di DynamoDB Local
    - start-container.ts Avvia il container di DynamoDB Local
  - src
    - index.ts Punto di ingresso del progetto ed esportazioni
    - constants.ts Costanti per lo sviluppo locale e chiave di configurazione runtime
    - client.ts Singleton del client DynamoDB e risoluzione del nome della tabella
    - entities
      - example.ts Esempio di definizione di entità ElectroDB
      - index.ts Esportazioni delle entità
  - project.json Configurazione del progetto e target di build
</FileTree>

### Infrastruttura

<Snippet name="shared-constructs" />

<Infrastructure>
  <Fragment slot="cdk">
    <FileTree>
      - packages/common/constructs/src
        - app
          - dynamodb
            - \<name>.ts Infrastruttura specifica per la tua tabella
        - core
          - dynamodb.ts Costrutto generico per tabelle DynamoDB
    </FileTree>
  </Fragment>
  <Fragment slot="terraform">
    <FileTree>
      - packages/common/terraform/src
        - app
          - dynamodb
            - \<name>.tf Modulo specifico per la tua tabella
        - core
          - dynamodb.tf Modulo generico DynamoDB
    </FileTree>
  </Fragment>
</Infrastructure>

## Sviluppo Locale

### Avvio di DynamoDB Locale

Il generatore configura un target `serve-local` che avvia un'istanza di [DynamoDB Local](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/DynamoDBLocal.html) e crea la tabella:

<NxCommands commands={['run <project-name>:serve-local']} />

Questo automaticamente:
1. Scarica l'immagine di DynamoDB Local (target `pull-image`)
2. Avvia un container
3. Crea una tabella locale con indici predefiniti

### Modellazione dei Dati

Il progetto generato utilizza [ElectroDB](https://electrodb.dev/) per la modellazione di entità type-safe su una singola tabella DynamoDB. Aggiungi o aggiorna i file delle entità in `src/entities/`, utilizzando l'entità di esempio generata come punto di partenza.

Esempio di definizione di entità:

```ts title="packages/my-table/src/entities/example.ts"
import { Entity } from 'electrodb';
import { getDynamoDBClient, resolveTableName } from '../client.js';

export const createExampleEntity = async () =>
  new Entity(
    {
      model: {
        entity: 'example',
        version: '1',
        service: 'MyTable',
      },
      attributes: {
        id: {
          type: 'string',
          required: true,
        },
        createdAt: {
          type: 'string',
          required: true,
          default: () => new Date().toISOString(),
          readOnly: true,
        },
        updatedAt: {
          type: 'string',
          required: true,
          default: () => new Date().toISOString(),
          watch: '*',
          set: () => new Date().toISOString(),
        },
      },
      indexes: {
        primary: {
          pk: {
            field: 'pk',
            composite: ['id'],
          },
          sk: {
            field: 'sk',
            composite: [],
          },
        },
      },
    },
    { client: getDynamoDBClient(), table: await resolveTableName() },
  );
```

Per maggiori dettagli, consulta la [documentazione delle entità ElectroDB](https://electrodb.dev/en/modeling/entities/).

### Utilizzo del Client DynamoDB

Il file generato `src/client.ts` esporta due utility chiave:

- `getDynamoDBClient()` — restituisce un singleton `DynamoDBClient` in cache. Quando `SERVE_LOCAL=true`, si connette all'istanza locale di DynamoDB Local; altrimenti crea un client AWS utilizzando la catena di credenziali predefinita.
- `resolveTableName()` — restituisce il nome della tabella DynamoDB. Quando `SERVE_LOCAL=true`, restituisce la costante del nome della tabella locale; altrimenti recupera il nome da AWS AppConfig utilizzando la variabile d'ambiente `RUNTIME_CONFIG_APP_ID` e lo memorizza in cache per le chiamate successive.

## Aggiunta/Rimozione di Global Secondary Index

Inizia aggiornando `GLOBAL_SECONDARY_INDEXES` in `src/gsi.ts` — questa è la fonte di verità per le tue definizioni GSI, e alla prossima esecuzione di `serve-local` lo script aggiungerà o rimuoverà automaticamente gli indici sulla tabella locale per riflettere la nuova lista. Quando si utilizza CDK, queste modifiche vengono automaticamente riflesse anche nella tua tabella AWS al prossimo deployment.

<OptionFilter when={{ iacProvider: 'Terraform' }}>
  A differenza di CDK, le modifiche a `src/gsi.ts` non vengono automaticamente riflesse nel tuo modulo Terraform. Devi anche aggiornare manualmente il tuo modulo Terraform per aggiungere o rimuovere le corrispondenti definizioni GSI prima del deployment.
</OptionFilter>

## Connessione alla Tabella

In qualsiasi progetto TypeScript, importa le factory delle entità dal tuo package DynamoDB e utilizzale direttamente:

```ts
import { createExampleEntity } from ':my-scope/my-table';

const entity = await createExampleEntity();
const result = await entity.query.primary({ id: '123' }).go();
```

### Generatori di Connessione

Per tipi di progetto specifici, utilizza il generatore `connection` per configurare automaticamente le dipendenze di sviluppo locale in modo che DynamoDB Local si avvii automaticamente insieme al tuo progetto:

- <Link path="guides/connection/trpc-dynamodb">tRPC API → DynamoDB</Link>
- <Link path="guides/connection/smithy-dynamodb">Smithy API → DynamoDB</Link>
- <Link path="guides/connection/ts-agent-dynamodb">TypeScript Agent → DynamoDB</Link>
- <Link path="guides/connection/ts-mcp-server-dynamodb">MCP Server → DynamoDB</Link>

## Deployment della Tabella

Il generatore DynamoDB crea l'infrastruttura CDK o Terraform in base al tuo `iacProvider` selezionato.

<Infrastructure>
<Fragment slot="cdk">
Il costrutto CDK viene creato in `common/constructs`. Esempio di utilizzo:

```ts title="packages/infra/src/stacks/application-stack.ts"
import { MyTable } from ':my-scope/common-constructs';

export class ApplicationStack extends Stack {
  constructor(scope: Construct, id: string, props?: StackProps) {
    super(scope, id, props);

    const table = new MyTable(this, 'Table');
  }
}
```

Questo effettua il provisioning di una tabella DynamoDB con:
- `pk` (partition key) e `sk` (sort key), entrambe di tipo `String`
- 2 Global Secondary Index per impostazione predefinita, come definito in `src/gsi.ts`
- Fatturazione on-demand (`PAY_PER_REQUEST`)
- Crittografia KMS gestita dal cliente con rotazione automatica delle chiavi
- Point-in-time recovery abilitato
- Protezione dalla cancellazione abilitata
- Nome della tabella registrato in Runtime Config sotto il namespace `dynamodb` in AWS AppConfig
</Fragment>
<Fragment slot="terraform">
Il modulo Terraform viene creato in `common/terraform`. Esempio di utilizzo:

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source = "../../common/terraform/src/app/dynamodb/my-table"
}
```

Questo effettua il provisioning di una tabella DynamoDB con:
- `pk` (partition key) e `sk` (sort key), entrambe di tipo `String`
- 2 Global Secondary Index per impostazione predefinita, come definito in `src/gsi.ts`
- Fatturazione on-demand (`PAY_PER_REQUEST`)
- Crittografia KMS gestita dal cliente con rotazione automatica delle chiavi
- Point-in-time recovery abilitato
- Protezione dalla cancellazione abilitata
- Nome della tabella registrato in Runtime Config
</Fragment>
</Infrastructure>

### Concessione dell'Accesso

<Snippet name="connection/lambda-dynamodb-access" />

Assicurati che le tue funzioni Lambda abbiano la variabile d'ambiente RUNTIME_CONFIG_APP_ID impostata in modo che resolveTableName() possa cercare il nome della tabella a runtime.

### Protezione dalla Cancellazione

La protezione dalla cancellazione è abilitata per impostazione predefinita per prevenire la cancellazione accidentale della tabella.

#### Disabilitare la Protezione dalla Cancellazione

Disabilitala per ambienti in cui la cancellazione della tabella è prevista, come stack di sviluppo o preview di breve durata.

<Infrastructure>
<Fragment slot="cdk">

```ts title="packages/infra/src/stacks/application-stack.ts"
import { MyTable } from ':my-scope/common-constructs';

const table = new MyTable(this, 'Table', {
  deletionProtection: false,
});
```
</Fragment>
<Fragment slot="terraform">

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source                      = "../../common/terraform/src/app/dynamodb/my-table"
  deletion_protection_enabled = false
}
```
</Fragment>
</Infrastructure>

### Modalità di Fatturazione

La tabella utilizza per impostazione predefinita la fatturazione on-demand (`PAY_PER_REQUEST`). Passa alla capacità provisionata per carichi di lavoro prevedibili ad alto throughput.

<Infrastructure>
<Fragment slot="cdk">

```ts title="packages/infra/src/stacks/application-stack.ts"
import { BillingMode } from 'aws-cdk-lib/aws-dynamodb';
import { MyTable } from ':my-scope/common-constructs';

const table = new MyTable(this, 'Table', {
  billingMode: BillingMode.PROVISIONED,
  readCapacity: 5,
  writeCapacity: 5,
});
```
</Fragment>
<Fragment slot="terraform">

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source       = "../../common/terraform/src/app/dynamodb/my-table"
  billing_mode = "PROVISIONED"
}
```
</Fragment>
</Infrastructure>

### Point-in-time Recovery

Il point-in-time recovery è abilitato per impostazione predefinita, consentendoti di ripristinare la tabella a qualsiasi punto negli ultimi 35 giorni.

#### Disabilitare il Point-in-time Recovery

<Infrastructure>
<Fragment slot="cdk">

```ts title="packages/infra/src/stacks/application-stack.ts"
import { MyTable } from ':my-scope/common-constructs';

const table = new MyTable(this, 'Table', {
  pointInTimeRecoverySpecification: { pointInTimeRecoveryEnabled: false },
});
```
</Fragment>
<Fragment slot="terraform">

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source                         = "../../common/terraform/src/app/dynamodb/my-table"
  point_in_time_recovery_enabled = false
}
```
</Fragment>
</Infrastructure>

### Rotazione della Chiave di Crittografia

La chiave KMS utilizzata per crittografare la tabella ha la rotazione automatica delle chiavi abilitata per impostazione predefinita. Disabilitala se la tua policy di sicurezza gestisce la rotazione esternamente.

#### Disabilitare la Rotazione della Chiave di Crittografia

<Infrastructure>
<Fragment slot="cdk">

```ts title="packages/infra/src/stacks/application-stack.ts"
import { MyTable } from ':my-scope/common-constructs';

const table = new MyTable(this, 'Table', {
  enableKeyRotation: false,
});
```
</Fragment>
<Fragment slot="terraform">

```hcl title="packages/infra/src/main.tf"
module "my_table" {
  source              = "../../common/terraform/src/app/dynamodb/my-table"
  enable_key_rotation = false
}
```
</Fragment>
</Infrastructure>
