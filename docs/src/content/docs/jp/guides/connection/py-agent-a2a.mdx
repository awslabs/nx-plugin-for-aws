---
title: "Python Agent から A2A Agent への接続"
description: "Python Agent をリモート A2A エージェントに接続する"
when:
  sourceType: py#agent
  targetType:
    - ts#agent
    - py#agent
---
import { FileTree, Tabs, TabItem } from '@astrojs/starlight/components';
import Link from '@components/link.astro';
import RunGenerator from '@components/run-generator.astro';
import GeneratorParameters from '@components/generator-parameters.astro';
import NxCommands from '@components/nx-commands.astro';
import Snippet from '@components/snippet.astro';

`connection` ジェネレーターは、<Link path="guides/py-agent">Python Agent</Link> をリモートの [A2A](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/runtime-a2a.html) エージェント（<Link path="guides/ts-agent">TypeScript</Link> または <Link path="guides/py-agent">Python</Link>）に接続し、エージェントが別のエージェントにツールとして委任できるようにします。

このジェネレーターは、AWS へのデプロイ時（Bedrock AgentCore 経由）とローカル実行時の両方で、エージェントがリモート A2A エージェントを検出して呼び出すために必要なすべての配線を設定します。

## 前提条件

このジェネレーターを使用する前に、以下を確認してください:

1. <Link path="guides/py-agent">Python Agent</Link> コンポーネント（Strands または LangChain）を持つ Python プロジェクト
2. `--protocol=A2A` と `--auth=IAM` で生成された Agent コンポーネントを持つプロジェクト（<Link path="guides/ts-agent">`ts#agent`</Link> または <Link path="guides/py-agent">`py#agent`</Link> のいずれか）
3. 両方のコンポーネントが `infra: agentcore` で作成されていること

## 使用方法

### ジェネレーターの実行

<RunGenerator generator="connection" />

ソースとしてホストエージェントプロジェクトを、ターゲットとして A2A エージェントプロジェクトを選択します。プロジェクトに複数のコンポーネントが含まれている場合は、`sourceComponent` と `targetComponent` オプションを指定して明確にしてください。

### オプション

<GeneratorParameters generator="connection" />

## ジェネレーターの出力

ジェネレーターは、`packages/common/agent_connection/` に共有の `agent_connection` Python プロジェクトを作成します（まだ存在しない場合）。接続ごとのクライアントモジュールがこの共有プロジェクトに生成されます:

<FileTree>

- packages/common/agent\_connection
  - \<scope>\_agent\_connection
    - \_\_init\_\_.py 接続ごとのクライアントを再エクスポート
    - core
      - agentcore\_endpoints.py フレームワークに依存しない ARN/URL 解決
      - agentcore\_a2a\_client\_config.py フレームワークに依存しない A2A クライアント設定（署名済み `ClientConfig`）
      - agentcore\_a2a\_client\_strands.py 設定をラップする A2A トランスポートクライアント
      - auth/ フレームワークに依存しない SigV4 / セッション転送 `httpx.Auth`
    - app
      - \<target\_agent\_name>\_client\_strands.py 各 A2A エージェント用の接続ごとの A2A トランスポートクライアント

</FileTree>

A2A トランスポート（Strands の `A2AAgent`）はモデルを持たないため、Strands と LangChain の両方のエージェントがこれを再利用します — フレームワークによって異なるのは `agent.py` のツール配線のみです。

さらに、ジェネレーターは以下を行います:
- エージェントの `agent.py` を変換して、リモート A2A エージェントを `@tool` を使用してツールとして登録
- エージェントプロジェクトのワークスペース依存関係として `agent_connection` プロジェクトを追加
- エージェントの `serve-local` ターゲットを更新して、ターゲットエージェントの `serve-local` ターゲットに依存するようにする

## 接続された A2A エージェントの使用

ジェネレーターは、エージェントの `agent.py` を変換して、リモート A2A エージェントをツールとしてラップします。リモートエージェントは `@tool` でデコレートされたデリゲートとして登録されます — デコレーターのインポートとエージェントのコンストラクターはフレームワークによって異なります:

<Tabs syncKey="framework">
<TabItem label="Strands">

```python title="packages/my-project/my_module/agent/agent.py" {4,9-15,21}
from contextlib import contextmanager
from strands import Agent, tool

from my_scope_agent_connection import RemoteAgentClientStrands

@contextmanager
def get_agent(session_id: str):
    remote_agent = RemoteAgentClientStrands.create(session_id=session_id)

    @tool
    def ask_remote_agent(prompt: str) -> str:
        """Delegate a question to the remote RemoteAgent A2A agent and return its reply."""
        return str(remote_agent(prompt))

    yield Agent(
        system_prompt="...",
        tools=[ask_remote_agent],
    )
```
</TabItem>
<TabItem label="LangChain">
```python title="packages/my-project/my_module/agent/agent.py" {4,8-13,17}
from langchain.agents import create_agent
from langchain_aws import ChatBedrockConverse
from langchain_core.tools import tool

from my_scope_agent_connection import RemoteAgentClientStrands

def get_agent():
    remote_agent = RemoteAgentClientStrands.create()

    @tool
    def ask_remote_agent(prompt: str) -> str:
        """Delegate a question to the remote RemoteAgent A2A agent and return its reply."""
        return str(remote_agent(prompt))

    return create_agent(
        model=ChatBedrockConverse(model=MODEL_ID, region_name=REGION),
        system_prompt="...",
        tools=[ask_remote_agent],
    )
```
</TabItem>
</Tabs>

内部的には、`RemoteAgentClientStrands.create()` は Strands の `A2AAgent`（純粋に A2A トランスポートとして使用され、モデルを持たないため LangChain エージェントも再利用します）を返します。これは、AWS にデプロイされた際に SigV4 でリクエストに署名する `httpx.AsyncClient` で構成され、`SERVE_LOCAL=true` の場合はプレーンな `http://localhost:<port>/` エンドポイントを使用します。Strands エージェントの場合、`session_id` は呼び出し元から渡され、[Bedrock AgentCore Observability](https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/observability.html) の一貫性を確保します。

## インフラストラクチャ

<Snippet name="connection/a2a-infrastructure" parentHeading="Infrastructure" />

## ローカル開発

ジェネレーターは、ホストエージェントの `serve-local` ターゲットを以下のように構成します:
1. 接続された A2A エージェントを自動的に起動
2. `SERVE_LOCAL=true` を設定して、生成されたクライアントが AgentCore の代わりに `http://localhost:<port>/` に直接接続するようにする

エージェントをローカルで実行するには:

<NxCommands commands={["<agent-name>-serve-local <project-name>"]} />

これにより、ホストエージェントとすべての接続された A2A エージェントが起動し、ホストエージェントは割り当てられたローカルポートでプレーン HTTP を介してリモートエージェントを呼び出します。
