---
title: "クイックスタートガイド"
description: "@aws/nx-pluginの使い方に関するクイックスタート。"
---



import { Steps } from '@astrojs/starlight/components';
import Link from '@components/link.astro';
import Snippet from '@components/snippet.astro';
import CreateNxWorkspaceCommand from '@components/create-nx-workspace-command.astro';
import InstallCommand from '@components/install-command.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';

このガイドでは、`@aws/nx-plugin`をインストールしAWS上でプロジェクトを迅速に構築する基本手順を説明します。

:::tip
フルスタックアプリケーション構築の詳細なチュートリアルについては、<Link path="get_started/tutorials/dungeon-game/overview">ダンジョンアドベンチャーチュートリアル</Link>を参照してください。
:::

## 前提条件

始める前に以下のグローバル依存関係をインストールしてください:

<Snippet name="prerequisites" />

## ステップ 1: 新しいNxワークスペースの初期化

任意のパッケージマネージャーを使用してNxワークスペースを作成するには、次のコマンドを実行します:

<CreateNxWorkspaceCommand workspace="my-project" />

プロジェクトディレクトリに移動します:

```sh
cd my-project
```

## ステップ 2: AWS用Nxプラグインの追加

パッケージマネージャーを使用してプラグインをインストールします:

<InstallCommand dev pkg="@aws/nx-plugin" />

## ステップ 3: ジェネレーターを使用したプロジェクトのスキャフォールディング

このクイックスタートガイドでは、tRPC API・Reactウェブサイト・Cognito認証・CDKインフラをプロジェクトに追加します。構築するプロジェクトタイプに応じて、任意のジェネレーターの組み合わせを選択できます。全てのオプションを確認するには、左側のナビゲーションバーにある__ガイド__セクションを参照してください。

### tRPC APIの追加

<RunGenerator generator="ts#trpc-api" requiredParameters={{ apiName: 'demo-api' }} />

これにより`packages/demo-api`フォルダ内にAPIが作成されます。

### Reactウェブサイトの追加

<RunGenerator generator="ts#cloudscape-website" requiredParameters={{ name: 'demo-website' }} />

`packages/demo-website`に新しいReactウェブサイトがスキャフォールドされます。

### Cognito認証の追加

<RunGenerator generator="ts#cloudscape-website#auth" requiredParameters={{ project: '@my-project/demo-website', cognitoDomain: 'my-demo' }} />

ウェブサイトにCognito認証を追加するための必要なインフラとReactコードをセットアップします。

### フロントエンドとバックエンドの接続

<RunGenerator generator="api-connection" requiredParameters={{ sourceProject: '@my-project/demo-website', targetProject: '@my-project/demo-api-backend' }} />

ウェブサイトがtRPC APIを呼び出せるように必要なプロバイダーを設定します。

### CDKインフラの追加

<RunGenerator generator="ts#infra" requiredParameters={{ name: 'infra' }} />

AWSにインフラをデプロイするためのCDKアプリを設定します。

## ステップ 4: クラウドリソースの定義とAWSへのデプロイ

`packages/infra/src/stacks/application-stack.ts`を開き、以下のコードを追加します:

```typescript
import * as cdk from 'aws-cdk-lib';
import { DemoApi, DemoWebsite, UserIdentity } from ':my-project/common-constructs';
import { Construct } from 'constructs';

export class ApplicationStack extends cdk.Stack {
  constructor(scope: Construct, id: string, props?: cdk.StackProps) {
    super(scope, id, props);

    const identity = new UserIdentity(this, 'identity');
    const api = new DemoApi(this, 'api');
    api.grantInvokeAccess(identity.identityPool.authenticatedRole);

    new DemoWebsite(this, 'website');
  }
}
```
__注記__: フルスタックアプリケーションをデプロイするために必要なCDKコードはこれですべてです。

### インフラのビルドとデプロイ

プロジェクトをビルドするには次のコマンドを実行します:

<NxCommands commands={['run-many --target build --all']} />

:::tip
リンターエラーが発生した場合、以下のコマンドで自動修正できます:

<NxCommands commands={['run-many --target lint --configuration=fix --all']} />
:::

## ステップ 5: フロントエンドのローカル実行

<Steps>
1. `runtime-config.json`ファイルの取得

    <NxCommands commands={['run @demo/demo-website:load:runtime-config']} />

2. ローカルWebサーバーの起動

    <NxCommands commands={['run @demo/demo-website:serve']} />
</Steps>

ブラウザで`http://localhost:4200`にアクセスしてウェブサイトをプレビューできます。

---

おめでとうございます！🎉 `@aws/nx-plugin`を使用したフルスタックアプリケーションの構築とデプロイに成功しました！