---
title: "UIの構築"
description: "@aws/nx-pluginを使用したエージェント型AI搭載のダンジョン冒険ゲームの作り方チュートリアル"
---

import { Aside, Code, FileTree, Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';
import Link from '@components/link.astro';
import Drawer from '@components/drawer.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';
import PackageManagerShortCommand from '@components/package-manager-short-command.astro';
import InstallCommand from '@components/install-command.astro';
import E2ECode from '@components/e2e-code.astro';
import E2EDiff from '@components/e2e-diff.astro';

import dungeonAdventureArchitecturePng from '@assets/dungeon-game-architecture.png'
import baselineWebsitePng from '@assets/baseline-website.png'
import baselineGamePng from '@assets/baseline-game.png'
import nxGraphPng from '@assets/nx-graph.png'
import gameSelectPng from '@assets/game-select.png'
import gameConversationPng from '@assets/game-conversation.png'

## タスク1: すべてをローカルで実行

フルローカルスタック — game-ui開発サーバー、ローカルGame API、ローカルAG-UI Story Agent（これがInventory MCPサーバーを起動します）— を1つのコマンドで起動します:

<NxCommands commands={["serve-local game-ui"]} />

`game-ui:serve-local`は`game-api:serve-local`と`dungeon_adventure.story:agent-serve-local`に`dependsOn`しているため、Nxは各プロジェクトのローカルサーバーを並行して起動します。ブラウザで開発サーバーを開き、ログインして、新規ユーザー作成のプロンプトに従います。ベースラインのShadcnウェブサイトが表示されます:

<Image src={baselineWebsitePng} alt="baseline-website.png" width="800" height="600" />

<Aside type="caution" title="開発サーバーを起動したまま">
このモジュールの残りの作業中は開発サーバーを起動したままにしてください。以下のすべての変更がホットリロードされます。
</Aside>

<Aside type="tip" title="代わりにデプロイ済みバックエンドを指す">
ローカル開発サーバーを既にデプロイされたAPI + エージェントと通信させたい場合は、デプロイされたランタイム設定を一度取得してから、通常の`serve`ターゲットを使用します:

<NxCommands commands={["run game-ui:load:runtime-config"]} />
<NxCommands commands={["serve game-ui"]} />

これは、UIコードのみを反復処理していて、実際のバックエンドに対してテストしたい場合に便利です。
</Aside>

## タスク2: CopilotKitが既に配線されている場所

<Link path="get_started/tutorials/dungeon-game/1">モジュール1</Link>で`game-ui → story`の`connection`ジェネレータを実行したとき、ShadcnウェブサイトのAG-UI統合が自動生成されました。簡単に確認してみましょう:

<FileTree>
- packages/game-ui/src/
  - components/
    - AguiProvider.tsx すべてのAG-UIエージェントに登録された単一の`CopilotKitProvider`。
    - copilot/
      - index.tsx テーマ化された`CopilotChat` / `CopilotSidebar` / `CopilotPopup`を再エクスポート。
      - ShadcnAssistantMessage.tsx, ShadcnUserMessage.tsx, ShadcnChatInput.tsx, ShadcnCursor.tsx, copilot.css
  - hooks/
    - useAguiStoryAgent.tsx デプロイされたStory Agentを指す`@ag-ui/client`の`HttpAgent`をインスタンス化し、`threadId`をAgentCoreの最小セッションID長である33文字にパディング。
  - main.tsx `<App />`を`<AguiProvider>`でラップ
</FileTree>

必要なのは、ルートに`<CopilotChat agentId="agent" threadId={...} />`を配置するだけです。統合の詳細については、<Link path="guides/connection/react-agui">React → AG-UI接続ガイド</Link>を参照してください。

## タスク3: ダンジョン用のスタイル変更

`packages/game-ui/src/styles.css`を置き換えます。これはスタイリングのために変更する*唯一*のファイルです。共有のShadcnグローバルをインポートし、パレットを松明で照らされたダンジョンテーマに上書きし、CopilotKitがそれらの色を継承するようにします:

<E2ECode lang="css" path="dungeon-adventure/4/styles.css.template" />

## タスク4: ゲームルートの作成

2つのルートが必要です。1つはヒーローを選ぶため、もう1つはプレイするためです。どちらもshadcnコンポーネントとCopilotKitチャットを使用します。手作りのチャットUIはありません。

<Tabs>
<TabItem label="routes/index.tsx">
<E2EDiff before="dungeon-adventure/4/routes/index.tsx.old.template" after="dungeon-adventure/4/routes/index.tsx.template" lang="tsx" />

これはゲームピッカーです: 新規ゲームフォーム（shadcnの`Input` + `Button` + `Card`）と、`useInfiniteQuery`を使用した`useGameApi().games.query`によって供給される「続きから」リスト — 下部のセンチネル`<div>`が`IntersectionObserver`によって監視され、ビューにスクロールされると自動的に`fetchNextPage()`を呼び出し、見出しの横とリストの下のスピナーがローディング状態を表示します。ゲームを開始すると`(playerName, genre)`ペアを`saveGame`し（次回表示されるように）、プレイルートに移動します。
</TabItem>
<TabItem label="routes/game/$playerName.tsx">
<E2ECode path="dungeon-adventure/4/routes/game/$playerName.tsx.template" lang="tsx" />

これはプレイルートです。決定論的な`threadId`（``{player}-{genre}``を33文字にパディング — AG-UIフックはこれをそのままAgentCoreセッションIDとして送信）を構築し、`<CopilotChat agentId="agent" threadId={threadId} />`をレンダリングし、`useGameApi().inventory.query`からのインベントリを上にオーバーレイします。マウント時に、`useGameApi().actions.query({ sessionId: threadId })`がエージェントがS3に保存した会話履歴を読み取り — 履歴がある場合は — `agent.setMessages(...)`を呼び出してチャットを再ハイドレートします。履歴がない場合は、ストーリーを開始するための1つのプライミングユーザーメッセージを送信します。`agent.messages`は`useAgent({ updates: [OnMessagesChanged] })`を介してサブスクライブされているため、新しいターンごとにインベントリクエリも再フェッチされます（MCPツール呼び出しはDynamoDBを直接変更します）。

<Aside type="tip" title="`isLoading`ではなく`isFetching`を待つ">
React Queryのデフォルトキャッシュは最初の結果を5分間保持するため、再訪問時には最初のロードからの古い空のリストが最初に表示されます。`staleTime: 0` + `refetchOnMount: 'always'`を設定し、ハイドレーション`useEffect`を`!isFetching && isSuccess`でガードすることで、再プライムパスが本当に空のスレッドでのみ実行されるようにします。
</Aside>

<Aside type="tip" title="決定論的なthreadIdにより/game/$playerName?genre=...が共有可能に">
同じURLを再訪問すると — リロード後、再デプロイ後、または別のデバイスでも — 同じ`threadId`に解決されるため、エージェントは同じストーリーを語り続けます。
</Aside>

<Aside type="tip" title="パスパラメータ">
`$playerName`構文は`@tanstack/react-router`に`playerName`を[パスパラメータ](https://tanstack.com/router/v1/docs/framework/react/guide/path-params)として扱うよう指示します。`validateSearch`は`genre`を列挙型に対して厳密に型付けします。
</Aside>
</TabItem>
</Tabs>

保存すると、`http://localhost:4200/`の開発サーバーで冒険を開始し、Story Agentとチャットできるようになります。

<Drawer title="ビルドとデプロイ" trigger="CloudFrontへのコードのビルド＆デプロイも可能です">

## タスク5: ビルドとデプロイ

### コードのビルド
<PackageManagerShortCommand commands={["build"]} />

### アプリケーションのデプロイ
<NxCommands commands={['deploy infra dungeon-adventure-infra-sandbox/*']} />

デプロイ後、CloudFront URL（CDK出力の`GameUIDistributionDomainName`）に移動します。

</Drawer>

<Image src={gameSelectPng} alt="game-select.png" width="500" height="400" />
<div style="margin-top: -100px; margin-left: 100px;">
<Image src={gameConversationPng} alt="game-conversation.png" width="500" height="400" />
</div>

おめでとうございます。エージェント型ダンジョンアドベンチャーゲームの構築とデプロイが完了しました! 🎉🎉🎉