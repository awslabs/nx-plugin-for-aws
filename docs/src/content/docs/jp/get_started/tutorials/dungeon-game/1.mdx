---
title: AIダンジョンゲーム
description: '@aws/nx-pluginを使用してAI駆動のダンジョンアドベンチャーゲームを構築する方法のチュートリアル。'
---

import { Aside, Code, FileTree, Steps, Tabs, TabItem } from '@astrojs/starlight/components';
import { Image } from 'astro:assets';
import Link from '@components/link.astro';
import Drawer from '@components/drawer.astro';
import RunGenerator from '@components/run-generator.astro';
import NxCommands from '@components/nx-commands.astro';
import InstallCommand from '@components/install-command.astro';
import CreateNxWorkspaceCommand from '@components/create-nx-workspace-command.astro';
import dungeonAdventureArchitecturePng from '@assets/dungeon-game-architecture.png'
import dungeonAdventureErPng from '@assets/dungeon-adventure-er.png'
import baselineWebsitePng from '@assets/baseline-website.png'
import baselineGamePng from '@assets/baseline-game.png'
import nxGraphPng from '@assets/nx-graph.png'
import gameSelectPng from '@assets/game-select.png'
import gameConversationPng from '@assets/game-conversation.png'

## モジュール1：モノレポのセットアップ

新しいモノレポを作成することから始めましょう。任意のディレクトリから、以下のコマンドを実行します：

<CreateNxWorkspaceCommand workspace="dungeon-adventure" />

これにより、`dungeon-adventure`ディレクトリ内にNXモノレポがセットアップされ、VSCodeで開くことができます。以下のようになっているはずです：

<FileTree>
- .nx/
- .vscode/
- node_modules/
- packages/ サブプロジェクトが配置される場所
- .gitignore
- .npmrc
- .prettierignore
- .prettierrc
- nx.json Nx CLIとモノレポのデフォルト設定を構成
- package.json すべてのNode依存関係がここで定義される
- pnpm-lock.yaml またはパッケージマネージャーに応じてbun.lock、yarn.lock、package-lock.json
- pnpm-workspace.yaml pnpmを使用している場合
- README.md
- tsconfig.base.json すべてのNodeベースのサブプロジェクトがこれを拡張
- tsconfig.json
</FileTree>

モノレポに`@aws/nx-plugin`からコンポーネントを追加し始めるには、`dungeon-adventure`モノレポのルートから以下のコマンドを実行して、開発依存関係としてインストールする必要があります：

<InstallCommand dev pkg="@aws/nx-plugin" />

これで、`@aws/nx-plugin`を使用して異なるサブプロジェクトを作成する準備が整いました。

<Aside type="tip">ジェネレーターを実行する前に、ステージングされていないファイルをすべてGitにコミットすることをお勧めします。これにより、`git diff`を通じてジェネレーター実行後に何が変更されたかを確認できます。</Aside>

### ゲームAPI

まず、ゲームAPIを作成しましょう。以下の手順に従って、`GameApi`というtRPC APIを作成します：

<RunGenerator generator="ts#trpc-api" requiredParameters={{apiName:"GameApi"}} noInteractive />

<br />

ファイルツリーに新しいファイルが表示されているはずです。

<Aside>
ルートの`package.json`は、`type`が`module`に設定されており、これはESMが`@aws/nx-plugin`によって提供されるすべてのNodeベースのサブプロジェクトのデフォルトモジュールタイプであることを意味します。TypeScriptプロジェクトの操作の詳細については、<Link path="guides/typescript-project">ts#projectガイド</Link>を参照してください。
</Aside>

<Drawer title="ts#trpc-apiで更新されたファイル" trigger="これらのファイルをより詳細に調べるにはここをクリックしてください。">
以下は、`ts#trpc-api`ジェネレーターによって生成されたすべてのファイルのリストです。ファイルツリーでハイライトされているいくつかの主要なファイルを調べてみましょう：
<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - app/ アプリ固有のcdkコンストラクト
          - http-apis/
            - **game-api.ts** tRPC APIを作成するcdkコンストラクト
            - index.ts
            - ...
          - index.ts
        - core/ 汎用cdkコンストラクト
          - http-api.ts HTTPベースのAPIの基本cdkコンストラクト
          - index.ts
          - runtime-config.ts
        - index.ts
      - project.json
      - ...
    - types/ 共有型
      - src/
        - index.ts
        - runtime-config.ts CDKとウェブサイトの両方で使用されるインターフェース定義
      - project.json
      - ...
  - game-api/
    - backend/ tRPC実装コード
      - src/
        - client/ 通常はtsマシン間通信に使用されるバニラクライアント
          - index.ts
          - sigv4.ts
        - middleware/ powertoolsの計装
          - error.ts
          - index.ts
          - logger.ts
          - metrics.ts
          - tracer.ts
        - procedures/ APIプロシージャ/ルートの特定の実装
          - **echo.ts**
        - index.ts
        - init.ts コンテキストとミドルウェアを設定
        - local-server.ts tRPCサーバーをローカルで実行する際に使用
        - **router.ts** すべてのプロシージャを定義するlambdaハンドラーのエントリーポイント
      - project.json
      - ...
    - schema/
      - src/
        - procedures/
          - **echo.ts**
        - index.ts
      - project.json
      - ...
- eslint.config.mjs
- vitest.workspace.ts
</FileTree>

いくつかの主要なファイルを見てみましょう：

```ts {5,12}
// packages/game-api/backend/src/router.ts
import {
  awsLambdaRequestHandler,
  CreateAWSLambdaContextOptions,
} from '@trpc/server/adapters/aws-lambda';
import { echo } from './procedures/echo.js';
import { t } from './init.js';
import { APIGatewayProxyEventV2WithIAMAuthorizer } from 'aws-lambda';

export const router = t.router;

export const appRouter = router({
  echo,
});

export const handler = awsLambdaRequestHandler({
  router: appRouter,
  createContext: (
    ctx: CreateAWSLambdaContextOptions<APIGatewayProxyEventV2WithIAMAuthorizer>,
  ) => ctx,
});

export type AppRouter = typeof appRouter;
```
ルーターはtRPC APIのエントリーポイントを定義し、すべてのAPIメソッドを宣言する場所です。上記のように、`echo`というメソッドがあり、その実装は`./procedures/echo.ts`ファイルにあります。

```ts {2-5}
// packages/game-api/backend/src/procedures/echo.ts
import { publicProcedure } from '../init.js';
import {
  EchoInputSchema,
  EchoOutputSchema,
} from ':dungeon-adventure/game-api-schema';

export const echo = publicProcedure
  .input(EchoInputSchema)
  .output(EchoOutputSchema)
  .query((opts) => ({ result: opts.input.message }));
```

このファイルは`echo`メソッドの実装であり、入力と出力のデータ構造を宣言することで強く型付けされています。これらの定義は、スキーマプロジェクトの[エイリアス](https://www.typescriptlang.org/tsconfig/paths.html)である`:dungeon-adventure/game-api-schema`プロジェクトからインポートしています。

<Aside type="tip">IDE内でインポートエラーが表示されている場合、これはバックエンドのtsconfig.jsonにまだタイプスクリプト参照が設定されていないためです。Nxはビルド/コンパイルが実行されるたびに、または`nx sync`コマンドを手動で実行するたびに、これらの参照を*動的に*作成するように[設定](https://nx.dev/nx-api/js/generators/typescript-sync)されています。詳細については、<Link path="guides/typescript-project#importing-your-library-code-in-other-projects">Typescriptガイド</Link>を参照してください。</Aside>

```ts
// packages/game-api/schema/src/procedures/echo.ts
import { z } from 'zod';

export const EchoInputSchema = z.object({
  message: z.string(),
});

export type IEchoInput = z.TypeOf<typeof EchoInputSchema>;

export const EchoOutputSchema = z.object({
  result: z.string(),
});

export type IEchoOutput = z.TypeOf<typeof EchoOutputSchema>;
```

すべてのtRPCスキーマ定義は[Zod](https://zod.dev/)を使用して定義され、`z.TypeOf`構文を通じてタイプスクリプトの型としてエクスポートされます。

```ts
// packages/common/constructs/src/app/http-apis/game-api.ts
import { Construct } from 'constructs';
import * as url from 'url';
import { HttpApi } from '../../core/http-api.js';
import { HttpIamAuthorizer } from 'aws-cdk-lib/aws-apigatewayv2-authorizers';
import { Runtime } from 'aws-cdk-lib/aws-lambda';

export class GameApi extends HttpApi {
  constructor(scope: Construct, id: string) {
    super(scope, id, {
      defaultAuthorizer: new HttpIamAuthorizer(),
      apiName: 'GameApi',
      runtime: Runtime.NODEJS_LATEST,
      handler: 'index.handler',
      handlerFilePath: url.fileURLToPath(
        new URL(
          '../../../../../../dist/packages/game-api/backend/bundle',
          import.meta.url,
        ),
      ),
    });
  }
}
```

これは私たちのGameApiを定義するCDKコンストラクトです。ご覧のように、tRPCバックエンド実装の生成されたバンドルへのハンドラーファイルパスが既に設定されています。これは、バックエンドプロジェクトのビルドターゲットの一部として既にバンドルされているため、`cdk synth`時にバンドリングが発生しない（[NodeJsFunction](https://docs.aws.amazon.com/cdk/api/v2/docs/aws-cdk-lib.aws_lambda_nodejs.NodejsFunction.html)とは対照的に）ことを意味します。

</Drawer>

### ストーリーAPI

次に、ストーリーAPIを作成しましょう。以下の手順に従って、`StoryApi`というFast APIを作成します：

<RunGenerator generator="py#fast-api" requiredParameters={{name:"StoryApi"}} noInteractive />

ファイルツリーに新しいファイルが表示されているはずです。
<Drawer title="py#fast-apiで更新されたファイル" trigger="これらのファイルをより詳細に調べるにはここをクリックしてください。">
以下は、`py#fast-api`ジェネレーターによって生成されたすべてのファイルのリストです。ファイルツリーでハイライトされているいくつかの主要なファイルを調べてみましょう：
<FileTree>
- .venv/ モノレポ用の単一仮想環境
- packages/
  - common/
    - constructs/
      - src/
        - app/ アプリ固有のcdkコンストラクト
          - http-apis/
            - **story-api.ts** Fast APIを作成するcdkコンストラクト
            - index.ts 新しいstory-apiをエクスポートするように更新
      - project.json story_apiにビルド依存関係を追加するように更新
    - types/ 共有型
      - src/
        - **runtime-config.ts** StoryApiを追加するように更新
  - story_api/
    - story_api/ pythonモジュール
      - init.py powertools、FastAPI、ミドルウェアを設定
      - **main.py** すべてのルートを含むlambdaのエントリーポイント
    - tests/
    - .python-version
    - project.json
    - pyproject.toml
    - project.json
- .python-version 固定されたuvのpythonバージョン
- pyproject.toml
- uv.lock
</FileTree>

```ts
// packages/common/constructs/src/app/http-apis/story-api.ts
import { Construct } from 'constructs';
import * as url from 'url';
import { HttpApi } from '../../core/http-api.js';
import { HttpIamAuthorizer } from 'aws-cdk-lib/aws-apigatewayv2-authorizers';
import { Runtime } from 'aws-cdk-lib/aws-lambda';

export class StoryApi extends HttpApi {
  constructor(scope: Construct, id: string) {
    super(scope, id, {
      defaultAuthorizer: new HttpIamAuthorizer(),
      apiName: 'StoryApi',
      runtime: Runtime.PYTHON_3_12,
      handler: 'story_api.main.handler',
      handlerFilePath: url.fileURLToPath(
        new URL(
          '../../../../../../dist/packages/story_api/bundle',
          import.meta.url,
        ),
      ),
    });
  }
}
```

これは私たちのStoryApiを定義するCDKコンストラクトです。ご覧のように、Fast APIバックエンド実装の生成されたバンドルへのハンドラーファイルパスが既に設定されています。これは、バックエンドプロジェクトのビルドターゲットの一部として既にバンドルされているため、`cdk synth`時にバンドリングが発生しない（[PythonFunction](https://docs.aws.amazon.com/cdk/api/v2/docs/@aws-cdk_aws-lambda-python-alpha.PythonFunction.html)とは対照的に）ことを意味します。

```diff lang="ts"
// packages/common/types/src/runtime-config.ts
export type ApiUrl = string;
// eslint-disable-next-line @typescript-eslint/no-empty-object-type, @typescript-eslint/no-empty-interface
export interface IRuntimeConfig {
  httpApis: {
    GameApi: ApiUrl;
+    StoryApi: ApiUrl;
  };
}
```

これはジェネレーターがASTトランスフォームを実行して、既存のコードをすべて保持しながら更新を行う例です。ここでは、`StoryApi`が`IRuntimeConfig`定義に追加されており、これがフロントエンドで最終的に消費されるときに型安全性を強制することを意味します！

```py
// packages/story_api/story_api/main.py
from .init import app, lambda_handler, tracer

handler = lambda_handler

@app.get("/")
@tracer.capture_method
def read_root():
    return {"Hello": "World"}
```

ここはすべてのAPIメソッドが定義される場所です。ご覧のように、`GET /`ルートにマッピングされた`read_root`メソッドがあります。型安全性を確保するために、メソッドの入力と出力を宣言するために[Pydantic](https://docs.pydantic.dev/latest/)を使用できます。

</Drawer>

### ゲームUI：ウェブサイト

次に、ゲームとやり取りするためのUIを作成しましょう。以下の手順に従って、`GameUI`というウェブサイトを作成します：

<RunGenerator generator="ts#cloudscape-website" requiredParameters={{name:"GameUI"}} noInteractive />

ファイルツリーに新しいファイルが表示されているはずです。

<Drawer title="ts#cloudscape-websiteで更新されたファイル" trigger="これらのファイルをより詳細に調べるにはここをクリックしてください。">
以下は、`ts#cloudscape-website`ジェネレーターによって生成されたすべてのファイルのリストです。ファイルツリーでハイライトされているいくつかの主要なファイルを調べてみましょう：
<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - app/ アプリ固有のcdkコンストラクト
          - static-websites/
            - **game-ui.ts** Game UIを作成するcdkコンストラクト
        - core/
          - static-website.ts 汎用静的ウェブサイトコンストラクト
  - game-ui/
    - public/
    - src/
      - components/
        - AppLayout/
          - index.ts 全体的なページレイアウト：ヘッダー、フッター、サイドバーなど
          - navitems.ts サイドバーナビゲーション項目
      - hooks/
        - useAppLayout.tsx 通知やページスタイルなどを動的に設定できます
      - routes/ @tanstack/react-routerファイルベースのルート
        - index.tsx ルート'/'ページは'/welcome'にリダイレクト
        - __root.tsx すべてのページはこのコンポーネントをベースとして使用
        - welcome/
          - **index.tsx**
        - config.ts
        - **main.tsx** Reactエントリーポイント
        - routeTree.gen.ts これは@tanstack/react-routerによって自動的に更新されます
        - styles.css
    - index.html
    - project.json
    - vite.config.ts
    - ...
</FileTree>

```ts
// packages/common/constructs/src/app/static-websites/game-ui.ts
import * as url from 'url';
import { Construct } from 'constructs';
import { StaticWebsite } from '../../core/index.js';

export class GameUI extends StaticWebsite {
  constructor(scope: Construct, id: string) {
    super(scope, id, {
      websiteFilePath: url.fileURLToPath(
        new URL(
          '../../../../../../dist/packages/game-ui/bundle',
          import.meta.url,
        ),
      ),
    });
  }
}
```

これは私たちのGameUIを定義するCDKコンストラクトです。ご覧のように、ViteベースのUIの生成されたバンドルへのファイルパスが既に設定されています。これは、`build`時にgame-uiプロジェクトのビルドターゲット内でバンドリングが行われ、その出力がここで使用されることを意味します。

```tsx
// packages/game-ui/src/main.tsx
import React from 'react';
import { createRoot } from 'react-dom/client';
import { I18nProvider } from '@cloudscape-design/components/i18n';
import messages from '@cloudscape-design/components/i18n/messages/all.en';
import { RouterProvider, createRouter } from '@tanstack/react-router';
import { routeTree } from './routeTree.gen';

import '@cloudscape-design/global-styles/index.css';

const router = createRouter({ routeTree });

// Register the router instance for type safety
declare module '@tanstack/react-router' {
  interface Register {
    router: typeof router;
  }
}

const root = document.getElementById('root');
root &&
  createRoot(root).render(
    <React.StrictMode>
      <I18nProvider locale="en" messages={[messages]}>
        <RouterProvider router={router} />
      </I18nProvider>
    </React.StrictMode>,
  );
```

これはReactがマウントされるエントリーポイントです。示されているように、最初は[`ファイルベースのルーティング`](https://tanstack.com/router/v1/docs/framework/react/routing/file-based-routing)設定で`@tanstack/react-router`を構成するだけです。これは、開発サーバーが実行されている限り、`routes`フォルダ内にファイルを作成するだけで、`@tanstack/react-router`がボイラープレートファイルのセットアップを作成し、`routeTree.gen.ts`ファイルを更新することを意味します。このファイルは型安全な方法ですべてのルートを維持するため、`<Link>`を使用するとき、`to`オプションは有効なルートのみを表示します。詳細については、[`@tanstack/react-router`のドキュメント](https://tanstack.com/router/v1/docs/framework/react/quick-start)を参照してください。

```tsx
// packages/game-ui/src/routes/welcome/index.tsx
import {
  ContentLayout,
  Header,
  SpaceBetween,
  Container,
} from '@cloudscape-design/components';
import { createFileRoute } from '@tanstack/react-router';

export const Route = createFileRoute('/welcome/')({
  component: RouteComponent,
});

function RouteComponent() {
  return (
    <ContentLayout header={<Header>Welcome</Header>}>
      <SpaceBetween size="l">
        <Container>Welcome to your new Cloudscape website!</Container>
      </SpaceBetween>
    </ContentLayout>
  );
}
```

`/welcome`ルートにナビゲートするときにレンダリングされるコンポーネントです。`@tanstack/react-router`は、このファイルを作成/移動するたびに（開発サーバーが実行されている限り）`Route`を管理します。これはこのチュートリアルの後半で示されます。

</Drawer>

### ゲームUI：認証

次に、Amazon Cognitoを通じて認証されたアクセスを要求するようにGame UIを設定しましょう：

<RunGenerator generator="ts#cloudscape-website#auth" requiredParameters={{cognitoDomain:"game-ui", project:"@dungeon-adventure/game-ui", allowSignup:true}} noInteractive />

ファイルツリーに新しいファイルが表示/変更されているはずです。

<Drawer title="ts#cloudscape-website#authで更新されたファイル" trigger="これらのファイルをより詳細に調べるにはここをクリックしてください。">
以下は、`ts#cloudscape-website#auth`ジェネレーターによって生成/更新されたファイルのリストです。ファイルツリーでハイライトされているいくつかの主要なファイルを調べてみましょう：
<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - core/
          - user-identity.ts ユーザー/アイデンティティプールを作成するcdkコンストラクト
    - types/
      - src/
        - runtime-config.ts cognitoPropsを追加するように更新
  - game-ui/
    - src/
      - components/
        - AppLayout/
          - index.tsx ログインしたユーザー/ログアウトをヘッダーに追加
        - CognitoAuth/
          - index.ts Cognitoへのログインを管理
        - RuntimeConfig/
          - index.tsx `runtime-config.json`を取得し、コンテキストを通じて子に提供
      - hooks/
        - useRuntimeConfig.tsx
      - **main.tsx** Cognitoを追加するように更新
</FileTree>

```diff lang="tsx"
// packages/game-ui/src/main.tsx
+import CognitoAuth from './components/CognitoAuth';
+import RuntimeConfigProvider from './components/RuntimeConfig';
import React from 'react';
import { createRoot } from 'react-dom/client';
import { I18nProvider } from '@cloudscape-design/components/i18n';
import messages from '@cloudscape-design/components/i18n/messages/all.en';
import { RouterProvider, createRouter } from '@tanstack/react-router';
import { routeTree } from './routeTree.gen';
import '@cloudscape-design/global-styles/index.css';
const router = createRouter({ routeTree });
// Register the router instance for type safety
declare module '@tanstack/react-router' {
  interface Register {
    router: typeof router;
  }
}
const root = document.getElementById('root');
root &&
  createRoot(root).render(
    <React.StrictMode>
      <I18nProvider locale="en" messages={[messages]}>
+        <RuntimeConfigProvider>
+          <CognitoAuth>
            <RouterProvider router={router} />
+          </CognitoAuth>
+        </RuntimeConfigProvider>
      </I18nProvider>
    </React.StrictMode>,
  );
```

`RuntimeConfigProvider`と`CognitoAuth`コンポーネントがASTトランスフォームを通じて`main.tsx`ファイルに追加されました。これにより、`CognitoAuth`コンポーネントは、バックエンド呼び出しを正しい宛先に行うために必要なcognito接続設定を含む`runtime-config.json`を取得することで、Amazon Cognitoで認証できるようになります。

</Drawer>

### ゲームUI：ストーリーAPIへの接続

次に、以前に作成したストーリーAPIに接続するようにGame UIを設定しましょう：

<RunGenerator generator="api-connection" requiredParameters={{sourceProject:"@dungeon-adventure/game-ui", targetProject:"dungeon_adventure.story_api"}} noInteractive />

ファイルツリーに新しいファイルが表示/変更されているはずです。

<Drawer title="UI -> FastAPI api-connectionで更新されたファイル" trigger="これらのファイルをより詳細に調べるにはここをクリックしてください。">
以下は、`api-connection`ジェネレーターによって生成/更新されたファイルのリストです。ファイルツリーでハイライトされているいくつかの主要なファイルを調べてみましょう：
<FileTree>
- packages/
  - game-ui/
    - src/
      - hooks/
        - useSigV4.tsx StoryApiがリクエストに署名するために使用
        - useStoryApiClient.tsx StoryApiクライアントを構築するフック
        - useStoryApi.tsx TanStack Queryを使用してStoryApiと対話するフック
      - components/
        - QueryClientProvider.tsx TanStack Queryクライアントプロバイダー
        - StoryApiProvider.tsx StoryApi TanStack Queryフックのプロバイダー
      - main.tsx QueryClientProviderとStoryApiProviderを設定
    - .gitignore 生成されたクライアントファイルを無視
    - project.json openapi hooksを生成するためのターゲットを追加するように更新
    - ...
  - story_api/
    - scripts/
      - generate_open_api.py
    - project.json openapi.jsonファイルを出力するように更新

</FileTree>

```tsx {1,12-15}
// packages/game-ui/src/hooks/useStoryApiClient.tsx
import { StoryApi } from '../generated/story-api/client.gen';
import { useSigV4 } from './useSigV4';
import { useRuntimeConfig } from './useRuntimeConfig';
import { useMemo } from 'react';

export const useStoryApi = (): StoryApi => {
  const runtimeConfig = useRuntimeConfig();
  const apiUrl = runtimeConfig.httpApis.StoryApi;
  const sigv4Client = useSigV4();
  return useMemo(
    () =>
      new StoryApi({
        url: apiUrl,
        fetch: sigv4Client,
      }),
    [apiUrl, sigv4Client],
  );
};
```

このフックは、`StoryApi`に対して認証されたAPIリクエストを行うために使用できます。実装を見ると、ビルド時に生成される`StoryApi`を使用しているため、コードをビルドするまでIDEにエラーが表示されます。クライアントの生成方法やAPIの消費方法の詳細については、<Link path="guides/api-connection/react-fastapi">React to FastAPIガイド</Link>を参照してください。

```tsx
// packages/game-ui/src/components/StoryApiProvider.tsx
import { createContext, FC, PropsWithChildren, useMemo } from 'react';
import { useStoryApiClient } from '../hooks/useStoryApiClient';
import { StoryApiOptionsProxy } from '../generated/story-api/options-proxy.gen';

export const StoryApiContext = createContext<StoryApiOptionsProxy | undefined>(
  undefined,
);

export const StoryApiProvider: FC<PropsWithChildren> = ({ children }) => {
  const client = useStoryApiClient();
  const optionsProxy = useMemo(
    () => new StoryApiOptionsProxy({ client }),
    [client],
  );

  return (
    <StoryApiContext.Provider value={optionsProxy}>
      {children}
    </StoryApiContext.Provider>
  );
};

export default StoryApiProvider;
```

上記のプロバイダーコンポーネントは`useStoryApiClient`フックを使用し、TanStack Queryフックのオプションを構築するために使用される`StoryApiOptionsProxy`をインスタンス化します。対応するフック`useStoryApi`を使用してこのオプションプロキシにアクセスできます。これにより、tRPC APIと一貫した方法でFastAPIと対話する方法が提供されます。

`useStoryApiClient`はストリーミングAPI用の非同期イテレータを提供するため、このチュートリアルではバニラクライアントを直接使用します。

<Aside type="caution">
`src/generated/story-api/*.gen.ts`ファイルは、APIをビルドするたびに再生成されるため、手動で変更しないでください。
</Aside>

</Drawer>

### ゲームUI：ゲームAPIへの接続

次に、以前に作成したゲームAPIに接続するようにGame UIを設定しましょう：

<RunGenerator generator="api-connection" requiredParameters={{sourceProject:"@dungeon-adventure/game-ui", targetProject:"@dungeon-adventure/game-api-backend"}} noInteractive />

ファイルツリーに新しいファイルが表示/変更されているはずです。

<Drawer title="UI -> tRPC api-connectionで更新されたファイル" trigger="これらのファイルをより詳細に調べるにはここをクリックしてください。">
以下は、`api-connection`ジェネレーターによって生成/更新されたファイルのリストです。ファイルツリーでハイライトされているいくつかの主要なファイルを調べてみましょう：
<FileTree>
- packages/
  - game-ui/
    - src/
      - components/
        - TrpcClients/
          - index.tsx
          - TrpcApis.tsx すべての設定されたtRPC API
          - TrpcClientProviders.tsx tRPC APIごとにクライアントプロバイダーを作成
          - TrpcProvider.tsx
      - hooks/
        - **useGameApi.tsx** GameApiを呼び出すためのフック
      - **main.tsx** trpcクライアントプロバイダーを注入
- package.json

</FileTree>

```tsx
// packages/game-ui/src/hooks/useGameApi.tsx
import { TrpcApis } from '../components/TrpcClients';

export const useGameApi = () => TrpcApis.GameApi.useTRPC();
```

このフックはtRPCの最新の[React Query統合](https://trpc.io/blog/introducing-tanstack-react-query-client)を使用し、ユーザーが追加の抽象化レイヤーなしに直接`@tanstack/react-query`と対話できるようにします。tRPC APIの呼び出し方の例については、<Link path="guides/api-connection/react-trpc#using-the-generated-code">tRPCフックの使用ガイド</Link>を参照してください。

<Aside>
`useGameApi`フックは`useStoryApi`フックとは異なり、tRPCの[Typescript推論](https://trpc.io/docs/concepts)の使用のおかげで、変更が反映されるためにビルドを必要としません。これにより、開発者はバックエンドに変更を加えると、それがフロントエンドに即座に反映されます！
</Aside>

```diff lang="tsx"
// packages/game-ui/src/main.tsx
+import TrpcClientProviders from './components/TrpcClients';
+import QueryClientProvider from './components/QueryClientProvider';
import CognitoAuth from './components/CognitoAuth';
import RuntimeConfigProvider from './components/RuntimeConfig';
import React from 'react';
import { createRoot } from 'react-dom/client';
import { I18nProvider } from '@cloudscape-design/components/i18n';
import messages from '@cloudscape-design/components/i18n/messages/all.en';
import { RouterProvider, createRouter } from '@tanstack/react-router';
import { routeTree } from './routeTree.gen';
import '@cloudscape-design/global-styles/index.css';
const router = createRouter({ routeTree });
// Register the router instance for type safety
declare module '@tanstack/react-router' {
  interface Register {
    router: typeof router;
  }
}
const root = document.getElementById('root');
root &&
  createRoot(root).render(
    <React.StrictMode>
      <I18nProvider locale="en" messages={[messages]}>
        <RuntimeConfigProvider>
          <CognitoAuth>
+            <QueryClientProvider>
+              <TrpcClientProviders>
                <RouterProvider router={router} />
+              </TrpcClientProviders>
+            </QueryClientProvider>
          </CognitoAuth>
        </RuntimeConfigProvider>
      </I18nProvider>
    </React.StrictMode>,
  );
```

`main.tsx`ファイルはASTトランスフォームを通じて更新され、tRPCプロバイダーが注入されました。

</Drawer>

### ゲームUI：インフラストラクチャ

最後に作成する必要があるサブプロジェクトはCDKインフラストラクチャ用です。これを作成するには、以下の手順に従います：

<RunGenerator generator="ts#infra" requiredParameters={{name:"infra"}} noInteractive />

ファイルツリーに新しいファイルが表示/変更されているはずです。

<Drawer title="ts#infraで更新されたファイル" trigger="これらのファイルをより詳細に調べるにはここをクリックしてください。">
以下は、`ts#infra`ジェネレーターによって生成/更新されたファイルのリストです。ファイルツリーでハイライトされているいくつかの主要なファイルを調べてみましょう：
<FileTree>
- packages/
  - common/
    - constructs/
      - src/
        - core/
          - cfn-guard-rules/
            - *.guard
          - cfn-guard.ts
          - index.ts
  - infra
    - src/
      - stacks/
        - **application-stack.ts** cdkリソースがここで定義される
      - index.ts
      - **main.ts** すべてのスタックを定義するエントリーポイント
    - cdk.json
    - project.json
    - ...
  - package.json
  - tsconfig.json 参照を追加
  - tsconfig.base.json エイリアスを追加

</FileTree>

```ts
// packages/infra/src/main.ts
import { ApplicationStack } from './stacks/application-stack.js';
import {
  App,
  CfnGuardValidator,
  RuleSet,
} from ':dungeon-adventure/common-constructs';

const app = new App({
  policyValidationBeta1: [new CfnGuardValidator(RuleSet.AWS_PROTOTYPING)],
});

// Use this to deploy your own sandbox environment (assumes your CLI credentials)
new ApplicationStack(app, 'dungeon-adventure-infra-sandbox', {
  env: {
    account: process.env.CDK_DEFAULT_ACCOUNT,
    region: process.env.CDK_DEFAULT_REGION,
  },
  crossRegionReferences: true,
});

app.synth();
```

これはCDKアプリケーションのエントリーポイントです。

設定されたルールセットに基づいてインフラストラクチャの検証を実行するために[`cfn-guard`](https://github.com/cdklabs/cdk-validator-cfnguard)を使用するように構成されています。これは合成後に計装されます。

<Aside type="tip">
リソースに対して特定のルールを抑制したい場合があります。これを行うには2つの方法があります：

###### 特定のコンストラクトに対してルールを抑制する

```typescript
import { suppressRule } from ':dungeon-adventure/common-constructs';

...
// 指定されたコンストラクトに対してRULE_NAMEを抑制します。
suppressRule(construct, 'RULE_NAME');
```

###### 子孫コンストラクトに対してルールを抑制する

```typescript
import { suppressRule } from ':dungeon-adventure/common-constructs';

...
// コンストラクトまたはその子孫がBucketのインスタンスである場合、RULE_NAMEを抑制します
suppressRule(construct, 'RULE_NAME', (construct) => construct instanceof Bucket);
```
</Aside>

```ts
// packages/infra/src/stacks/application-stack.ts
import * as cdk from 'aws-cdk-lib';
import { Construct } from 'constructs';

export class ApplicationStack extends cdk.Stack {
  constructor(scope: Construct, id: string, props?: cdk.StackProps) {
    super(scope, id, props);

    // The code that defines your stack goes here
  }
}
```

ここでCDKコンストラクトをインスタンス化して、ダンジョンアドベンチャーゲームを構築します。

</Drawer>

#### インフラストラクチャの更新

`packages/infra/src/stacks/application-stack.ts`を更新して、すでに生成されたコンストラクトをいくつかインスタンス化しましょう：

```diff lang="ts"
+import {
+  GameApi,
+  GameUI,
+  StoryApi,
+  UserIdentity,
+} from ':dungeon-adventure/common-constructs';
import * as cdk from 'aws-cdk-lib';
import { Construct } from 'constructs';

export class ApplicationStack extends cdk.Stack {
  constructor(scope: Construct, id: string, props?: cdk.StackProps) {
    super(scope, id, props);

-    // The code that defines your stack goes here
+    const userIdentity = new UserIdentity(this, 'UserIdentity');
+
+    const gameApi = new GameApi(this, 'GameApi');
+    const storyApi = new StoryApi(this, 'StoryApi');
+
+    // grant our authenticated role access to invoke our APIs
+    [storyApi, gameApi].forEach((api) =>
+      api.grantInvokeAccess(userIdentity.identityPool.authenticatedRole),
+    );
+
+    // Ensure this is instantiated last so our runtime-config.json can be automatically configured
+    new GameUI(this, 'GameUI');
  }
}

```

### コードのビルド

<Drawer title="Nxコマンド" trigger="初めてコードをビルドする時が来ました">
###### 単一ターゲットと複数ターゲット

`run-many`コマンドは、複数のリストされたサブプロジェクトでターゲットを実行します（`--all`はすべてを対象とします）。依存関係が正しい順序で実行されることを保証します。

また、プロジェクトターゲットで直接ターゲットを実行することで、単一プロジェクトターゲットのビルド（または他のタスク）をトリガーすることもできます。例えば、`@dungeon-adventure/infra`プロジェクトをビルドしたい場合は、次のコマンドを実行できます：

<NxCommands commands={['run @dungeon-adventure/infra:build']} />
###### 依存関係の可視化

以下のコマンドで依存関係を可視化することもできます：

<NxCommands commands={['graph']} />
<br/>

<Image src={nxGraphPng} alt="nx-graph.png" width="800" height="600" />

###### キャッシング

Nxは[キャッシング](https://nx.dev/concepts/how-caching-works)に依存しており、開発を高速化するために以前のビルドからのアーティファクトを再利用できます。これを正しく機能させるにはいくつかの設定が必要であり、**キャッシュを使用せずに**ビルドを実行したい場合もあるかもしれません。そのためには、コマンドに`--skip-nx-cache`引数を追加するだけです。例えば：

<NxCommands commands={['run @dungeon-adventure/infra:build --skip-nx-cache']} />
何らかの理由でキャッシュ（`.nx`フォルダに保存されている）をクリアしたい場合は、次のコマンドを実行できます：

<NxCommands commands={['reset']} />

</Drawer>

<NxCommands commands={['run-many --target build --all']} />

以下のようなプロンプトが表示されるはずです：

```bash
 NX   The workspace is out of sync

[@nx/js:typescript-sync]: Some TypeScript configuration files are missing project references to the projects they depend on or contain outdated project references.

This will result in an error in CI.

? Would you like to sync the identified changes to get your workspace up to date? …
Yes, sync the changes and run the tasks
No, run the tasks without syncing the changes
```

このメッセージは、NXが自動的に更新できるファイルを検出したことを示しています。この場合、参照プロジェクトにTypeScript参照が設定されていない`tsconfig.json`ファイルを指しています。**Yes, sync the changes and run the tasks**オプションを選択して続行してください。同期ジェネレーターが不足しているTypeScript参照を自動的に追加するため、IDEに関連するインポートエラーがすべて自動的に解決されることに気づくでしょう！

<Aside type="tip">
リントエラーが発生した場合は、次のコマンドを実行して自動的に修正できます。

<NxCommands commands={['run-many --target lint --configuration=fix --all']} />
</Aside>

<Aside type="caution" title="Windowsビルド失敗">
<Drawer trigger="Windowsでビルドエラーが発生した場合は、ここをクリックしてください。" title="Windowsビルド失敗">
`@dungeon-adventure/infra`プロジェクトのビルド/合成エラーが発生した場合、これは予想されることです。`cfn-guard`を計装するライブラリは現在Windowsをサポートしていないためです。これを追跡する機能リクエストがありますが、それまでの間、`packages/infra/src/main.ts`ファイルを次のように変更することで`cfn-guard`を無効にすることができます：

```diff lang="ts"
// packages/infra/src/main.ts
import { ApplicationStack } from './stacks/application-stack.js';
import {
   App,
-  CfnGuardValidator,
-  RuleSet,
} from ':dungeon-adventure/common-constructs';
-
-const app = new App({
-  policyValidationBeta1: [new CfnGuardValidator(RuleSet.AWS_PROTOTYPING)],
-});
+const app = new App();

// Use this to deploy your own sandbox environment (assumes your CLI credentials)
new ApplicationStack(app, 'dungeon-adventure-infra-sandbox', {
  env: {
    account: process.env.CDK_DEFAULT_ACCOUNT,
    region: process.env.CDK_DEFAULT_REGION,
  },
  crossRegionReferences: true,
});

app.synth();
```
</Drawer>
</Aside>

すべてのビルドアーティファクトは、モノレポのルートにある`dist/`フォルダ内で利用可能になりました。これは、生成されたファイルでファイルツリーが汚染されないように、`@aws/nx-plugin`によって生成されたプロジェクトを使用する際の標準的な方法です。ファイルをクリーンアップしたい場合は、生成されたファイルがファイルツリー全体に散らばっていることを心配せずに、単に`dist/`フォルダを削除するだけです。

おめでとうございます！ダンジョンアドベンチャーゲームのコアを実装するために必要なすべてのサブプロジェクトを作成しました。🎉🎉🎉
